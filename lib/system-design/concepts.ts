export interface SystemDesignConcept {
  id: number;
  section: string;
  title: string;
  tagline: string;
  description: string;
  note?: string;
  whyItMatters: string;
  diagramNote: string;
  example: string;
  interviewTip: string;
  tags: string[];
}

export const SYSTEM_DESIGN_SECTIONS = [
  "Foundation",
  "Requirements",
  "Communication",
  "Data Layer",
  "Scaling & Distribution",
  "Reliability",
  "Case Study",
  "Advanced Topics",
] as const;

export const SYSTEM_DESIGN_CONCEPTS: SystemDesignConcept[] = [
  // ─────────────────────────────────────────────
  // FOUNDATION
  // ─────────────────────────────────────────────
  {
    id: 1,
    section: "Foundation",
    title: "What is System Design",
    tagline: "Building systems that work at scale",
    description:
      "A system is a group of components working together to achieve a common goal. System design is the process of deciding what those components should be and how they should interact to solve a problem reliably and at scale.\n\nThink of a bank. At first, there's only one cashier serving customers (a single server). When the line gets long, the first step is to make the cashier work faster (optimize the code and algorithms). If that's not enough, you give the cashier better tools, like a cash-counting machine (vertical scaling — adding more CPU or RAM).\n\nWhen customers keep increasing, you open more counters (horizontal scaling). But now each counter has its own vault, which can lead to inconsistent data. To solve this, all counters share a single central vault (a centralized database).\n\nAs the bank grows, customers may crowd around one counter while others sit idle. To distribute traffic evenly, a guard at the entrance directs each customer to the least busy counter (a load balancer).\n\nEach time a new problem appeared, a new system design concept was introduced to solve it. That's the essence of system design: identifying bottlenecks and choosing the right solutions as the system grows.",
    whyItMatters:
      "Anyone can write code that works for 10 users. System design is what makes it work for 10 million at once. Interviews test whether you can evolve a simple design as constraints change — exactly what the bank story demonstrates.",
    diagramNote:
      "Fig. 1.1 — Each problem at the bank introduced a new system design concept. The five-step evolution mirrors how every real system grows.",
    example:
      "WhatsApp handles 2–10 million messages per day using distributed servers, a centralized database, and load balancers — the exact same evolution as the alien bank, just at internet scale.",
    interviewTip:
      "When given any design prompt, state: 'A system is components + a common goal. Let me identify the components.' Then evolve from the simplest design step by step — single server → scale out → add DB → add cache → add load balancer. This structured evolution shows depth.",
    tags: ["Architecture", "Scale", "Components", "High Level Design", "Bank Analogy"],
  },
  {
    id: 2,
    section: "Foundation",
    title: "Data vs Compute Intensive",
    tagline: "Identify where the bottleneck actually lives",
    description:
      "Every system is mainly either data-intensive or compute-intensive.\n\nBefore comparing the two, it helps to understand what a bottleneck is. A bottleneck is the slowest part of a system — the single point that limits overall performance. Think of a highway with 6 lanes that suddenly narrows to 1. No matter how fast cars move on the wide section, everyone slows down at the narrow point. In system design, the bottleneck is where your system gets stuck. Fixing anything else won't help until you fix that one part first.\n\nData-intensive systems spend most of their time storing, retrieving, and moving data. The CPU is usually not the bottleneck — it's idle most of the time, just waiting for data to arrive. Examples include Instagram feeds, WhatsApp messages, banking transactions, and analytics dashboards. Performance is improved through better database design, caching, replication, sharding, indexing, and optimized queries.\n\nCompute-intensive systems spend most of their time performing calculations. The database is usually not the bottleneck — data comes in fine, but the CPU or GPU is always busy crunching numbers. Examples include video rendering, image processing, machine learning training, flight simulators, and cryptography. Performance is improved by adding more CPU or GPU power, using parallel processing, and optimizing algorithms.\n\nA simple way to tell the difference: if the system is slow because it's waiting for data to be stored, fetched, or transferred, it's data-intensive. If the system is slow because it's busy performing calculations, it's compute-intensive.\n\nMany real-world applications contain both. YouTube is data-intensive when serving videos to users, but compute-intensive when generating video recommendations. Understanding which part of the system is the bottleneck helps you choose the right fix — and avoid wasting effort on the wrong layer.",
    whyItMatters:
      "Throwing GPU at a database bottleneck wastes money. Throwing more databases at a CPU bottleneck also wastes money. Identifying the bottleneck type first saves engineering time and cost by pointing you at the right layer to fix.",
    diagramNote:
      "Fig. 2.1 — Data-intensive systems bottleneck at the database; compute-intensive systems bottleneck at the CPU/GPU.",
    example:
      "Instagram is data-intensive: millions of images/videos moving from CDN to users. The bottleneck is data movement, so they use aggressive caching and distributed CDN — not more CPU. ML recommendation features on the same platform are compute-intensive — they run on GPU farms doing matrix math, not on extra databases.",
    interviewTip:
      "Before drawing any architecture, classify the feature: 'This is data-intensive so I'll focus on caching, CDN, and read replicas.' Or: 'This is compute-intensive so I'll design parallel GPU workers with a job queue.' Using this distinction immediately signals engineering maturity.",
    tags: ["Bottleneck", "Data Intensive", "Compute Intensive", "GPU", "Performance"],
  },

  // ─────────────────────────────────────────────
  // REQUIREMENTS
  // ─────────────────────────────────────────────
  {
    id: 3,
    section: "Requirements",
    title: "Functional Requirements",
    tagline: "What the system must do",
    description:
      "Functional requirements are simply the features a system must provide — in other words, what the user can actually do in the application.\n\nFor an e-commerce app, functional requirements include things like:\n\n• Signing up for a new account\n• Logging into the system\n• Viewing a list of products\n• Searching for specific products\n• Filtering products by category, price, or other attributes\n• Adding products to a shopping cart\n• Applying discount coupons\n• Placing an order\n• Processing payment\n• Tracking order status after purchase\n\nEach of these represents a clear user action or feature. If a feature doesn't describe what a user can do, it's not a functional requirement.\n\nIn system design, functional requirements are the starting point of everything. They help you define what you are building before thinking about how to build it. Once these are clear, you design the system components (like APIs, databases, services, and queues) to directly support these features.\n\nA good rule of thumb:\nIf a component or service doesn't support at least one functional requirement, it is likely unnecessary or over-engineered.\n\nIn short, functional requirements keep your system focused, simple, and directly tied to real user needs.",
    whyItMatters:
      "You can't design what you haven't defined. Functional requirements prevent scope creep, keep the interview structured, and ensure every architectural decision traces back to a real user need.",
    diagramNote:
      "Fig. 3.1 — Functional requirements mapped as a sequential user journey, from registration to order tracking.",
    example:
      "Amazon's functional requirements include product catalog browsing, search with filters, cart management, order placement, payment processing, and delivery tracking. Each maps to a dedicated microservice in their actual architecture.",
    interviewTip:
      "Spend the first 2–3 minutes clarifying functional requirements with the interviewer before drawing anything. Ask: 'Should I include user auth? Payment processing? Notifications?' This shows maturity, sets scope, and prevents designing the wrong system.",
    tags: ["Requirements", "Features", "Scope", "Use Cases", "API Design"],
  },
  {
    id: 4,
    section: "Requirements",
    title: "Non-Functional Requirements",
    tagline: "How the system must perform",
    description:
      "Non-functional requirements describe how well a system works, not what features it has.\n\nWhile functional requirements are about what the user can do, non-functional requirements are about how the system behaves when those features are used.\n\nHere are the main types explained simply:\n\n<b>Scalability</b>\nThe system should handle growth in users and traffic. For example, if traffic increases 10× during a sale, the system should still work smoothly. This is usually achieved using vertical scaling (bigger machines) or horizontal scaling (more machines).\n\n<b>Availability</b>\nThe system should stay up and running most of the time. For example, 99.9% uptime means very little downtime in a year. This is often defined using SLAs (Service Level Agreements) or SLOs (Service Level Objectives).\n\n<b>Reliability</b>\nThe system should not lose data and should behave correctly even when things fail. If a server crashes, the system should recover without breaking user data or transactions.\n\n<b>Performance</b>\nThe system should respond quickly. For example, a request should be processed in under 200ms (often measured using p99 latency, meaning 99% of requests are faster than this limit).\n\n<b>Security</b>\nThe system should protect user data and prevent unauthorized access. This includes login systems (authentication), permissions (authorization), and encryption for data both in storage and while being sent over the network.\n\n<b>Observability</b>\nThe system should be easy to monitor and debug. This means having logs, metrics, and tracing so engineers can understand issues without directly accessing production or guessing what went wrong.",
    note:
      "In system design, these are not extra features — they are engineering constraints. They shape how the system is built.\n\nIf you ignore them, the result is not just missing functionality — it can lead to serious problems like outages, slow systems, data loss, or security breaches.",
    whyItMatters:
      "A feature-complete app that crashes under load, leaks user data, or takes 10 seconds to respond fails in production. NFRs define the quality bar the system must meet — and they directly drive architectural decisions.",
    diagramNote:
      "Fig. 4.1 — Non-functional requirements define the quality constraints every architecture component must satisfy.",
    example:
      "Amazon's NFRs drive their entire architecture: < 200ms p99 latency → CDN + cache. 99.999% uptime → multi-region with automatic failover. 10× traffic on Black Friday → auto-scaling groups. Each NFR is a SLA commitment backed by engineering design.",
    interviewTip:
      "Before drawing any boxes, state your NFRs: 'I'll target 99.9% availability, p99 latency under 500ms, and the system should handle 1M daily active users with peaks of 10× that.' Then justify every component in terms of these. Interviewers score heavily on this.",
    tags: ["SLA", "SLO", "Availability", "Scalability", "Performance", "Security", "Reliability"],
  },

  // ─────────────────────────────────────────────
  // COMMUNICATION
  // ─────────────────────────────────────────────
  {
    id: 5,
    section: "Communication",
    title: "DNS",
    tagline: "Translates domain names to IP addresses",
    description:
      "Computers don't understand website names like \"telesco.com\". They only understand IP addresses like 192.168.1.1.\n\nSo we use DNS (Domain Name System) to translate human-friendly names into IP addresses.\n\n<b>How DNS lookup works (step by step)</b>\n\nWhen you type a website in your browser, DNS follows a chain:\n\n<b>Root Server</b>\nThis is the starting point. There are 13 root server groups (A–M), managed by different organizations. They don't know the exact IP of the website. They only know where to find the next level (like which server handles .com, .net, .org, etc.).\n\n<b>TLD Server (Top-Level Domain Server)</b>\nThis handles domains like .com, .net, .in, etc. It doesn't know the final IP either. It points you to the authoritative name server for the domain.\n\n<b>Authoritative Name Server</b>\nThis is the final source of truth for a domain. It is managed by the domain provider (like GoDaddy or Google Domains). It stores the actual mapping: telesco.com → 192.168.1.1. It returns the real IP address.\n\n<b>What happens after the first lookup (caching)</b>\n\nTo make things faster, DNS results are saved (cached):\n\n• Browser cache (Chrome, Firefox, etc.)\n• Operating System cache\n• DNS resolver cache (usually your ISP or public DNS like Google DNS)\n\nSo next time you visit the site, it often skips the full lookup process.\n\n<b>TTL (Time To Live)</b>\n\nEach DNS record has a TTL value, which tells how long it can be cached before it must be refreshed. Short TTL = more frequent updates, long TTL = faster repeat visits.\n\n<b>Subdomains</b>\n\nSubdomains like docs.telesco.com and api.telesco.com are handled by the same system. The authoritative server uses a DNS zone, which contains records for the main domain and all its subdomains.",
    note:
      "DNS is like a multi-step phonebook system for the internet:\n\n• Root → finds direction\n• TLD → narrows it down\n• Authoritative server → gives exact IP\n• Cache → makes everything faster\n\nWithout DNS, we'd have to remember IP addresses instead of simple website names.",
    whyItMatters:
      "Every web request starts with a DNS lookup. Understanding DNS explains geo-routing (send Indian users to Indian servers via DNS), CDN edge selection, failover, and why domain changes can take hours to propagate (TTL).",
    diagramNote:
      "Fig. 5.1 — A DNS lookup walks four servers in sequence; subsequent lookups short-circuit via cache at each layer.",
    example:
      "When you type google.com, your browser checks its own cache first. On miss, asks the OS. On miss, asks your ISP's DNS resolver. On miss, the resolver walks the full tree: root → .com TLD → google's authoritative NS → gets 216.58.x.x. This entire process completes in ~50ms and is then cached.",
    interviewTip:
      "Mention DNS when discussing global distribution: 'I'd configure DNS geo-routing so users in India resolve to the Mumbai server IP, users in the US resolve to the Virginia server IP — cutting latency by reducing physical distance.'",
    tags: ["DNS", "Domain", "IP Address", "TTL", "Root Server", "TLD", "Authoritative NS"],
  },
  {
    id: 6,
    section: "Communication",
    title: "API Types",
    tagline: "Different protocols for different communication needs",
    description:
      "There are five common types of APIs, and each one is used for different kinds of communication between systems.\n\n<b>1. REST (Most commonly used)</b>\nUses normal HTTP methods like GET, POST, PUT, DELETE. Data is usually sent in JSON format. Works in a stateless way — each request is independent. Easy to understand and works with almost any client (web, mobile, etc.).\n\nUsed for most public APIs like e-commerce, social media, etc.\n\n<b>2. SOAP (Older enterprise standard)</b>\nUses XML, which is more heavy and verbose than JSON. Very strict rules and structure. Common in banks, insurance, and government systems. Still used in legacy systems, but less common in modern apps.\n\n<b>3. GraphQL</b>\nHas a single endpoint instead of multiple ones. The client asks for exactly what it needs — no more, no less. Prevents over-fetching (getting unnecessary data). Great for apps where bandwidth matters, like mobile apps.\n\nExample: instead of getting 50 fields, you can request only 3.\n\n<b>4. gRPC</b>\nCreated by Google for fast internal communication. Uses Protocol Buffers (binary format) instead of JSON. Much smaller and faster than REST (3–10× more efficient). Best used for communication between microservices.\n\n<b>5. WebSockets</b>\nCreates a persistent connection between client and server. Allows real-time two-way communication. Server can push updates instantly without the client asking again.\n\nUsed for: Chat apps, Live notifications, Stock prices or sports scores, Collaborative tools.",
    note:
      "Simple rule of thumb:\n\n• REST → Public APIs (most common)\n• gRPC → Internal microservices (fast communication)\n• WebSockets → Real-time features\n• GraphQL → Flexible frontend data fetching (especially mobile)\n• SOAP → Legacy enterprise systems\n\nIn short, each API type is just a different way for systems to talk, optimized for different needs like speed, flexibility, or structure.",
    whyItMatters:
      "Choosing the wrong API type adds unnecessary latency or complexity. gRPC is 3–10× faster than REST for internal calls. WebSockets eliminate wasteful polling for real-time features. Knowing when to use each separates junior from senior engineers.",
    diagramNote:
      "Fig. 6.1 — Five API protocols compared by communication style, payload format, and primary use case.",
    example:
      "Uber: REST for the public mobile app API. gRPC between internal microservices (driver matching, surge pricing engine). WebSockets to push live driver location to the rider's app every second without the app asking repeatedly.",
    interviewTip:
      "Default to REST for client-server. Say: 'For internal microservice calls I'd use gRPC for lower latency and smaller payloads.' Only propose WebSockets when the feature genuinely requires real-time push from server to client (chat, notifications, live collaboration).",
    tags: ["REST", "gRPC", "GraphQL", "SOAP", "WebSockets", "Protobuf", "HTTP"],
  },
  {
    id: 7,
    section: "Communication",
    title: "REST APIs in Depth",
    tagline: "Methods, status codes, and design best practices",
    description:
      "<b>HTTP Methods (What action you are doing)</b>\n\n<b>GET</b> → Read data\nExample: Get user details. Safe and does not change data.\n\n<b>POST</b> → Create new data\nExample: Create a new user or order. Not idempotent — calling twice may create duplicates.\n\n<b>PUT</b> → Replace the entire resource\nExample: Update a user profile fully. If you send only partial data, missing fields may become null.\n\n<b>PATCH</b> → Update part of a resource\nExample: Update only the user's email. Safer for partial updates.\n\n<b>DELETE</b> → Remove data\nExample: Delete a user or post.\n\n<b>Ways to send data in APIs</b>\n\n<b>Path parameters</b> → Identify a specific resource\nExample: /users/123 → user with ID 123\n\n<b>Query parameters</b> → Filter, sort, or paginate data\nExample: /users?sort=asc&q=java → sort users and search for \"java\"\n\n<b>Request body</b> → Send actual data\nUsed for: creating user details, updating profile info, passwords (sensitive data), complex structured data.\n\n<b>HTTP Status Codes (Server response meaning)</b>\n\n200 OK → Request successful\n201 Created → New resource created\n204 No Content → Success but no response body (common for DELETE)\n301 Permanent Redirect → URL has permanently changed\n302 Temporary Redirect → Temporary redirect to another URL\n400 Bad Request → Client sent invalid data\n401 Unauthorized → User not logged in\n403 Forbidden → User logged in but not allowed\n404 Not Found → Resource does not exist\n500 Internal Server Error → Server crashed or unexpected error\n\n<b>API Response Best Practice</b>\n\nAlways return responses inside an object, not a raw array.\n\nBad: `[1, 2, 3]`\n\nGood: `{ \"data\": [1, 2, 3], \"count\": 3 }`\n\nThis way you can later add pagination info, metadata, or error messages without breaking existing clients.\n\n<b>Resource Design Best Practices</b>\n\nNested resources show relationships — `/blogs/1/comments` → comments of blog 1\n\nQuery params for complex filtering — `/products?category=shoes&price_lt=100`",
    note:
      "HTTP methods define what action you perform. Params define how you send data. Status codes tell the client what happened.\n\nIn short: good API design is about making communication between systems clear, predictable, and scalable.",
    whyItMatters:
      "REST is the dominant API style you'll build and consume throughout your career. Getting methods, status codes, and response format right from the start prevents breaking changes and client bugs at scale.",
    diagramNote:
      "Fig. 7.1 — REST API design: standard HTTP methods mapped to resource URLs and their expected behavior.",
    example:
      "GitHub API: GET /repos/{owner}/{repo} returns repo data (200). POST /repos/{owner}/{repo}/issues creates issue (201). PATCH /repos/{owner}/{repo} updates repo settings (200). All responses are objects with nested fields — never bare arrays. Invalid auth returns 401, missing repo returns 404.",
    interviewTip:
      "Use plural nouns for resources (/users, /orders). Nest related resources (/orders/1/items) for clear relationships. Query params for filtering (/products?color=blue&max_price=100). Always wrap responses in objects. Return 404 for missing resources, 400 for bad input, 401 for missing auth, 403 for insufficient permissions, 500 for server bugs.",
    tags: ["HTTP Methods", "Status Codes", "REST", "JSON", "Path Params", "Query Params", "API Design"],
  },

  // ─────────────────────────────────────────────
  // DATA LAYER
  // ─────────────────────────────────────────────
  {
    id: 8,
    section: "Data Layer",
    title: "SQL Databases",
    tagline: "Structured data with enforced relationships",
    description:
      "SQL databases store data in tables, similar to spreadsheets.\n\n• Rows = individual records (like one user, one order)\n• Columns = fields or properties (like name, email, price)\n\nA `users` table might store: `id`, `name`, `email` — each row is one user.\n\n<b>Tables represent real-world things</b>\n\nIn SQL, you usually create one table per entity:\n\n• `users` → user data\n• `posts` → blog posts\n• `orders` → purchases\n\n<b>Constraints (rules that protect data quality)</b>\n\n<b>PRIMARY KEY</b> → Uniquely identifies each row\nExample: `user_id`\n\n<b>UNIQUE</b> → No duplicate values allowed\nExample: usernames must be different\n\n<b>NOT NULL</b> → Field cannot be empty\nExample: `first_name` must exist\n\n<b>FOREIGN KEY</b> → Connects two tables together\nExample: `posts.author_id` → `users.id`\n\n<b>CHECK</b> → Adds validation rules\nExample: `balance >= 0`, `password_length > 8`\n\n<b>DEFAULT</b> → Gives a value if none is provided\nExample: `role = 'student'`\n\n<b>Types of relationships between tables</b>\n\n<b>1. One-to-Many</b>\nOne record in a table relates to many in another.\nExample: One user → many posts. Each post has an `author_id`.\n\n<b>2. Many-to-Many</b>\nBoth sides can relate to many records.\nExample: Students ↔ Courses. Requires a junction table `students_courses` with `student_id` and `course_id`.\n\n<b>3. One-to-One</b>\nOne record maps to exactly one other record.\nExample: `content` → `video_details`. Often stored in separate tables but linked by the same ID.\n\n<b>When to use SQL databases</b>\n\n• Data has clear structure and relationships\n• You need strong consistency and safety (ACID transactions) — e.g., payments, banking, bookings\n• The schema is stable and predictable",
    note:
      "SQL databases are best for structured, reliable, relationship-heavy data. They work by storing data in tables, enforcing rules using constraints, connecting data using relationships, and ensuring data consistency through transactions.\n\nSQL = structured + reliable + strongly consistent data storage system.",
    whyItMatters:
      "Most business data has natural relationships. SQL enforces those at the database level — preventing orphaned records, duplicate usernames, and corrupted data. ACID guarantees mean money never disappears mid-transaction.",
    diagramNote:
      "Fig. 8.1 — Relational data modeling: one-to-many (FK column) and many-to-many (junction table) patterns.",
    example:
      "A bank uses PostgreSQL: accounts table has PRIMARY KEY on account_id, NOT NULL and CHECK (balance >= 0) on balance. transactions table has two FOREIGN KEYs referencing accounts. ACID ensures that a transfer debits one account and credits another atomically — either both happen or neither does.",
    interviewTip:
      "Choose SQL when: the schema is fixed, data has relationships, consistency is critical (payments, inventory), or you need complex queries with joins. Explain which constraints you'd use and why — this signals you think about data integrity, not just features.",
    tags: ["SQL", "PostgreSQL", "MySQL", "ACID", "Foreign Key", "Joins", "Constraints"],
  },
  {
    id: 9,
    section: "Data Layer",
    title: "NoSQL Databases",
    tagline: "Flexible storage for unstructured or massive data",
    description:
      "NoSQL is not one single type of database. It's a group of different database models designed for flexibility, speed, and large-scale systems.\n\nUnlike SQL (tables and fixed schema), NoSQL systems are more flexible and scale horizontally very well.\n\nThere are 4 main types:\n\n<b>1. Key–Value Stores</b>\nExamples: Redis, DynamoDB\n\nData is stored as `key → value`. Very simple and extremely fast.\n\nUse cases:\n• Caching\n• User sessions (logged-in users)\n• Real-time counters (likes, views)\n\nThink of it like a dictionary or map.\n\n<b>2. Column-Family / Columnar Databases</b>\nExamples: Cassandra, BigQuery, Redshift\n\nData is stored by columns instead of rows. Optimized for reading large amounts of data from specific columns.\n\nUse cases:\n• Analytics\n• Reporting systems\n• Big data queries\n\nNot good for frequent row-by-row updates. Example: \"Give me total sales for last 2 years\" across millions of records.\n\n<b>3. Graph Databases</b>\nExample: Neo4j\n\nData is stored as nodes (entities like users, posts) and edges (relationships between them). Both nodes and relationships can have properties.\n\nUse cases:\n• Social networks (friends, followers)\n• Fraud detection\n• Recommendation systems (\"people also liked…\")\n\nBest when relationships matter more than raw data.\n\n<b>4. Document Databases</b>\nExamples: MongoDB, CouchDB\n\nData is stored as JSON-like documents. Each document can have a different structure (flexible schema).\n\nUse cases:\n• User profiles\n• Content management systems\n• Logging systems\n\nExample: one user might have extra fields like hobbies, another might not.\n\n<b>When to use NoSQL</b>\n\n• Data is unstructured or changing often\n• You need high scalability (horizontal scaling)\n• You don't need complex joins\n• You care more about speed and availability than strict consistency\n\n<b>Real-world usage</b>\n\n• Netflix → Cassandra for large-scale activity data\n• Amazon → DynamoDB for scalable key-value storage\n• Twitter → Redis for caching and fast timeline delivery",
    note:
      "NoSQL is a collection of databases designed for different needs:\n\n• Key–Value → fast lookups and caching\n• Columnar → big analytics and reporting\n• Graph → relationship-heavy data\n• Document → flexible JSON data\n\nIn short: NoSQL trades strict structure for speed and scalability at massive scale.",
    whyItMatters:
      "SQL schema rigidity becomes a bottleneck at scale — adding a column to a 500M-row table takes hours. NoSQL's flexibility and horizontal scalability are why every major tech company uses it for at least one service.",
    diagramNote:
      "Fig. 9.1 — The four NoSQL data models, each optimized for a different access pattern.",
    example:
      "Instagram stores user account data in PostgreSQL (relational, consistent), activity feed in Cassandra (high write throughput, horizontally scalable), and hot timeline data in Redis (key-value, microsecond reads). Three different NoSQL types for three different access patterns.",
    interviewTip:
      "Don't default to MongoDB for everything. Ask: 'What's the access pattern?' Sessions/cache → key-value (Redis). Analytics on huge data → columnar (Cassandra/BigQuery). Social graph/recommendations → graph DB. Flexible content/profiles/logs → document (MongoDB). Match the model to the access pattern.",
    tags: ["NoSQL", "MongoDB", "Redis", "Cassandra", "DynamoDB", "Key-Value", "Graph DB", "Columnar"],
  },
  {
    id: 10,
    section: "Data Layer",
    title: "Caching",
    tagline: "Store hot data in fast memory to avoid database hits",
    description:
      "A cache is a fast temporary memory layer that stores frequently used data so you don't have to repeatedly fetch it from a slow database.\n\nThink of it like a \"shortcut memory\" for your system.\n\n<b>Why caching is used</b>\n\nDatabases are relatively slow compared to memory. So instead of hitting the database every time, we store popular data in cache and serve it directly from there. This makes the system much faster.\n\n<b>Cache hit vs Cache miss</b>\n\n<b>Cache hit</b>\nData is found in cache. Response is extremely fast (milliseconds or less).\nExample: User requests profile → data already in cache → return immediately.\n\n<b>Cache miss</b>\nData is NOT in cache. System goes to the database (slower), then stores the result in cache for future use.\nExample: First time fetching a product → DB lookup → store in cache.\n\n<b>TTL (Time To Live)</b>\n\nEvery cached item has an expiry time. After TTL expires, the data is automatically removed.\n\nWhy? Because data can become outdated.\nExample: Product price cached for 10 minutes → after 10 minutes, refresh from database.\n\n<b>Cache size rule</b>\n\nCache is kept small on purpose. A large cache becomes slow to search and defeats the purpose of being fast. So we only store hot data — frequently accessed data.\n\n<b>Types of caching layers</b>\n\n<b>1. Browser cache</b>\nStores static files (images, CSS, JS). Makes websites load faster on repeat visits.\n\n<b>2. DNS cache</b>\nStores domain → IP mappings. Avoids repeated DNS lookups.\n\n<b>3. Server-side cache (most important)</b>\nExamples: Redis, Memcached. Stores application data, fully controlled by backend engineers. Major performance booster.\n\n<b>4. Database cache / query cache</b>\nStores results of expensive queries. Reduces load on database.\n\n<b>Simple flow</b>\n\n1. Request comes in\n2. Check cache first\n3. If found → return instantly\n4. If not → go to DB\n5. Save result in cache\n6. Return response\n\n<b>Key idea</b>\n\nCaching works because most systems follow the 80/20 rule — 20% of data is accessed 80% of the time. So we cache only the frequently used data.",
    note:
      "A cache stores hot data to reduce database load and improve response time dramatically.\n\nCache hit = instant response from memory\nCache miss = fetch from DB, then store in cache\nTTL = controls how long cached data stays fresh",
    whyItMatters:
      "A DB query taking 100ms costs 100ms every time without cache. The same query served from Redis takes < 1ms. Caching is typically the first optimization that multiplies system capacity by 10–100× with minimal infrastructure change.",
    diagramNote:
      "Fig. 10.1 — Cache hit vs. miss paths: a hit returns instantly from memory; a miss fetches from the DB and populates the cache.",
    example:
      "telesco.com homepage shows 6 featured courses, each requiring 4 database table joins. Without cache: 24 DB queries per page load × 10,000 daily users = 240,000 DB queries. With Redis cache + 10-min TTL: 24 queries total per cache window, regardless of user count.",
    interviewTip:
      "Always discuss cache invalidation — the hard part. Options: TTL (simplest, tolerates brief staleness), event-driven invalidation (update cache when DB changes), versioned keys (cache key includes a version number). Also mention cold start: what happens when the cache is empty right after a deploy?",
    tags: ["Redis", "Memcached", "Cache Hit", "Cache Miss", "TTL", "Latency", "Server-Side Cache"],
  },
  {
    id: 11,
    section: "Data Layer",
    title: "Cache Read/Write Strategies",
    tagline: "When to read from and write to cache",
    description:
      "When you add a cache to your system, you need a strategy for how data flows between the cache and the database. There are four main approaches.\n\n<b>Read Through Cache (RTC)</b>\n\nEvery read goes to the cache first. If the data is missing (cache miss), the cache itself fetches it from the database, stores it, and returns it.\n\n• Writes go directly to the database, bypassing cache\n• Cache fills gradually as data is requested\n• Simple to use — the app always talks to the cache, never directly to the DB\n\nGood for: read-heavy systems where most data is eventually needed.\n\n<b>Write Through Cache (WTC)</b>\n\nEvery write goes to the cache first, then immediately to the database — both happen before the response is sent.\n\n• Cache is always up to date\n• Slightly slower writes (must wait for both cache and DB to confirm)\n• No stale data risk\n\nGood for: systems where reading fresh data right after a write is critical, like stock prices or live inventory.\n\n<b>Write Around Cache (WAC)</b>\n\nWrites go directly to the database, skipping the cache entirely. Data only enters the cache when it is read.\n\n• Cache won't hold data that was just written but never read\n• Avoids filling the cache with data that may never be needed again\n\nGood for: data that is written once and read rarely, like audit logs or archival records.\n\n<b>Write Back Cache (WBC)</b>\n\nWrites go to the cache only. The database is updated asynchronously in the background, after the response has already been sent.\n\n• Fastest write path — the user gets a response instantly\n• Risk of data loss if the cache crashes before the async DB write completes\n\nGood for: high-write systems where speed matters more than strict consistency, like real-time order status updates or live game scores.",
    note:
      "The right strategy depends on your access pattern:\n\n• Read-heavy → Read Through\n• Need fresh reads after writes → Write Through\n• Rare reads of written data → Write Around\n• Very high write volume, can tolerate some data loss → Write Back",
    whyItMatters:
      "The wrong strategy either makes your cache stale (users see old data) or makes writes a bottleneck (defeats the performance purpose). Matching strategy to access pattern is key.",
    diagramNote:
      "Fig. 11.1 — Four caching strategies: read-through, write-through, write-around, and write-behind differ in when the DB is updated.",
    example:
      "Swiggy order status changes rapidly (confirmed → packed → picked up → out for delivery → delivered). They use WBC — write status to cache instantly (user sees fast updates), flush to database asynchronously. Speed matters more than strict consistency here. If cache crashes, the last N status updates might be lost, but the order itself is safe in DB.",
    interviewTip:
      "Match strategy to access pattern: read-heavy with occasional writes → RTC. Writes that must be immediately readable → WTC. Write-then-read-later data → WAC. Very high write throughput with acceptable loss risk → WBC. In an interview, state which you chose and why — don't just say 'I'd use caching'.",
    tags: ["RTC", "WTC", "WAC", "WBC", "Read Through", "Write Through", "Write Back", "Cache Strategy"],
  },
  {
    id: 12,
    section: "Data Layer",
    title: "Cache Eviction Policies",
    tagline: "Which data gets removed when cache is full",
    description:
      "A cache has limited memory. When it fills up, something has to be removed to make room for new data. An eviction policy decides which item gets removed.\n\n<b>LRU — Least Recently Used</b>\n\nRemoves the item that has not been accessed for the longest time.\n\n• If you searched \"iPhone 17\" today but last searched \"iPhone 11\" three weeks ago, the iPhone 11 entry gets evicted first\n• Most commonly used default — works well for most applications\n\n<b>MRU — Most Recently Used</b>\n\nRemoves the most recently accessed item — the opposite of LRU.\n\n• Sounds counter-intuitive, but makes sense for video streaming\n• The segment you just watched won't be needed again, so evict it first to free space for upcoming segments\n\n<b>LFU — Least Frequently Used</b>\n\nRemoves the item that has been accessed the fewest total times across its entire history.\n\n• A product you searched once on a whim gets evicted before a product searched every day\n• Good for capturing long-term popularity rather than recency\n\n<b>FIFO — First In, First Out</b>\n\nRemoves the oldest item added to the cache, regardless of how often it has been accessed.\n\n• Simple and predictable — no extra tracking needed\n• Does not consider whether the data is still being used\n\n<b>LIFO — Last In, First Out</b>\n\nRemoves the most recently added item.\n\n• Useful when older data is more stable and valuable than newly added data\n• Example: historical reference tables or configuration data that rarely changes",
    note:
      "LRU is the safe default for most caches. Use MRU for media buffers where recently played content won't be replayed. Use LFU when long-term popularity matters more than recency, like a product recommendation cache.",
    whyItMatters:
      "The wrong eviction policy keeps cold data and evicts hot data — a cache full of stale entries is worse than no cache. LRU is the safe default but each policy fits specific patterns.",
    diagramNote:
      "Fig. 12.1 — Five cache eviction policies: each defines a different rule for which entry to remove when the cache is full.",
    example:
      "Redis defaults to LRU. A social feed cache fits LRU perfectly — yesterday's viral post gradually cools and gets evicted. A video streaming buffer fits MRU — segments you've already played won't be replayed. An e-commerce product cache fits LFU — products viewed once during a random browse are less valuable than daily bestsellers.",
    interviewTip:
      "Default to LRU for general-purpose caches. Call out MRU specifically for media streaming buffers (segments already played). Mention LFU when long-term access frequency matters more than recency (recommendation engines, product catalogs). Interviewers appreciate knowing why, not just which.",
    tags: ["LRU", "MRU", "LFU", "FIFO", "LIFO", "Eviction Policy", "Cache"],
  },

  // ─────────────────────────────────────────────
  // SCALING & DISTRIBUTION
  // ─────────────────────────────────────────────
  {
    id: 13,
    section: "Scaling & Distribution",
    title: "Load Balancer",
    tagline: "Routes traffic across servers and monitors health",
    description:
      "A load balancer sits in front of your servers and does two jobs: distribute incoming requests evenly and monitor which servers are healthy.\n\n<b>Layer 4 vs Layer 7</b>\n\n<b>Layer 4 (L4) — Transport Layer</b>\nRoutes traffic based on IP address and port number. It doesn't look at the content of the request at all. Very fast and simple.\n\n• Example: AWS NLB (Network Load Balancer)\n• Use when raw speed is the priority, like game servers or real-time streaming\n\n<b>Layer 7 (L7) — Application Layer</b>\nRoutes based on HTTP headers, URL paths, and cookies. Smarter routing decisions.\n\n• Example: AWS ALB (Application Load Balancer)\n• Can route `/api/*` to your app servers and `/images/*` to a CDN\n• Use when you need content-aware routing\n\n<b>Routing Algorithms</b>\n\n• `Round Robin` — send requests to servers in order, one by one\n• `Least Connections` — send to the server with the fewest active requests\n• `Least Time` — send to the server that responds fastest\n• `IP Hash` — same client always goes to the same server (useful for sessions)\n• `Weighted Round Robin` — more powerful servers receive more traffic\n• `Geo-Based` — route users to the server closest to their location\n\n<b>Health Checks</b>\n\nLoad balancers need to know which servers are alive:\n\n• `Passive` — observe real traffic; if a server returns too many errors, mark it unhealthy\n• `Active` — send synthetic test requests on a schedule; remove the server if it doesn't respond\n\n<b>The SPOF Problem</b>\n\nThe load balancer itself can become a single point of failure. If it goes down, all traffic stops.\n\n• `Active-Passive` — two load balancers share a virtual IP (VIP). The active one handles all traffic. The passive one watches via heartbeat. If the active one fails, the passive takes over the VIP in seconds.\n• `Active-Active` — both handle traffic simultaneously. Higher throughput, but more complex to set up.",
    note:
      "The load balancer solves two problems: distributing load across servers and detecting failures automatically. Without it, adding more servers does nothing — clients only know one address.\n\nAlways make the load balancer itself redundant with an Active-Passive setup to avoid a single point of failure.",
    whyItMatters:
      "Without a load balancer, all traffic hits one server — if it crashes, the app goes down. If traffic spikes, that one server is overwhelmed. A load balancer distributes traffic and removes failed servers automatically.\n\nIf you choose wrong:\n• Using L4 when you need URL routing = messy workarounds in your app code\n• No redundancy on the load balancer itself = it becomes the single point of failure that defeats the whole purpose",
    diagramNote:
      "Fig. 13.1 — Active/passive load balancer failover with VIP handoff; L4 routes by IP/port, L7 routes by request content.",
    example:
      "<b>1. Web app (L7 Load Balancer)</b>\nThree servers behind an AWS ALB. Requests to `/api/*` go to backend servers, `/images/*` go to S3. If Server 2 crashes, health checks detect it and traffic routes to Servers 1 and 3. Users notice nothing.\n\n<b>2. Redundant load balancer (Active-Passive)</b>\nTwo load balancers share one virtual IP. LB-A handles all traffic. LB-B watches via heartbeat. If LB-A crashes, LB-B takes over the IP in seconds. Without this, the load balancer itself is the single point of failure.",
    interviewTip:
      "\"I'd use an L7 load balancer here because I need to route by URL path — `/api/*` to backend servers and `/images/*` to storage.\"\n\n\"To avoid the load balancer being a single point of failure, I'd run two in active-passive mode, sharing a virtual IP with automatic failover.\"\n\nAlways address L4 vs L7 and always address the SPOF question when discussing load balancers.",
    tags: ["Round Robin", "Least Connections", "L4", "L7", "Health Check", "SPOF", "Active-Passive", "ALB", "NLB"],
  },
  {
    id: 14,
    section: "Scaling & Distribution",
    title: "Horizontal & Vertical Scaling",
    tagline: "Two ways to add capacity to your system",
    description:
      "When your system gets more traffic than a single server can handle, you have two options for adding capacity.\n\n<b>Vertical Scaling — Scale Up</b>\n\nUpgrade the existing machine with more CPU, more RAM, or faster disk.\n\n• No code changes needed — the simplest option\n• Has a hard ceiling — you can only fit so much RAM in one machine\n• Still a single point of failure — if that one machine goes down, everything goes down\n• Good for: databases that are hard to distribute, simple applications with moderate traffic\n\nUsing the bank analogy: vertical scaling is giving your one cashier a faster computer and a cash-counting machine.\n\n<b>Horizontal Scaling — Scale Out</b>\n\nAdd more machines and distribute traffic across all of them.\n\n• Near-unlimited capacity — just keep adding commodity servers\n• No single point of failure — if one server crashes, others keep running\n• Requires a load balancer to distribute requests\n• Requires stateless services — user sessions must be stored in a shared store like Redis, not in server memory\n\nUsing the bank analogy: horizontal scaling is opening more cashier counters.\n\n<b>Key Prerequisite: Stateless Services</b>\n\nFor horizontal scaling to work, your servers must be stateless. This means:\n\n• User session data goes in Redis, not in the server's RAM\n• Any server can handle any request — they are interchangeable\n• If a server restarts, no user data is lost\n\n<b>Which to choose?</b>\n\n• Start with vertical scaling — it's simpler and requires no architecture changes\n• Move to horizontal scaling when you hit the hardware ceiling or need fault tolerance\n• Most production systems at scale use horizontal scaling as the primary strategy",
    note:
      "Vertical scaling is fast to set up but has limits. Horizontal scaling is the foundation of internet-scale systems but requires stateless architecture.\n\nThe rule of thumb: scale up first, scale out when needed.",
    whyItMatters:
      "Every app eventually outgrows a single server. Knowing when to scale up vs scale out determines how far your system can grow — and how resilient it is when things go wrong.\n\nIf you choose wrong:\n• Only vertical scaling = you'll eventually hit a hardware ceiling, and one crash takes everything down\n• Horizontal scaling without stateless services = requests fail when users land on a server that doesn't have their session",
    diagramNote:
      "Fig. 14.1 — Vertical scaling grows one machine until it hits a hardware ceiling; horizontal scaling adds stateless machines behind a load balancer.",
    example:
      "<b>1. Small startup (Vertical first)</b>\nYou start on one server. Traffic grows, so you upgrade to a bigger machine. No code changes, works fine at small scale. But eventually you can't upgrade any further — every machine has a limit.\n\n<b>2. Netflix at scale (Horizontal)</b>\nThousands of stateless servers. User sessions and preferences are stored in Redis, not on any server. Any server can handle any user. During peak hours, thousands more servers spin up automatically. If one crashes, traffic goes to the rest.\n\nKey: stateless services let you add as many servers as you need.",
    interviewTip:
      "\"I'd start with vertical scaling because it requires no architecture changes. Once I hit the hardware ceiling, I'd move to horizontal scaling with a load balancer and stateless services — storing sessions in Redis.\"\n\nDesign services to be stateless from day one. Waiting until you need to scale makes the refactor painful and risky.",
    tags: ["Vertical Scaling", "Horizontal Scaling", "Stateless", "Auto Scaling", "Load Balancer", "Elasticity"],
  },
  {
    id: 15,
    section: "Scaling & Distribution",
    title: "Replication",
    tagline: "Copy data across nodes for reliability and read scale",
    description:
      "Replication means keeping copies of the same data on multiple servers. If one server goes down, others still have the data. It also lets you spread read traffic across multiple servers instead of hammering one.\n\nThere are three main models of replication:\n\n<b>1. Single Leader (Most common)</b>\n\nOne server is the leader (also called primary or master). All writes go to the leader only. The leader then copies those writes to follower servers (also called replicas or standbys).\n\n• `Async replication` — the leader responds to the client immediately and updates followers in the background. Faster, but followers may briefly be slightly behind.\n• `Sync replication` — the leader waits for all followers to confirm before responding. Data is always consistent, but writes are slower.\n• Most systems choose async replication — the small lag is acceptable in exchange for better write performance.\n\nWhat happens if the leader crashes? A follower with the most recent data is promoted to become the new leader. This is called failover.\n\n<b>2. Multi-Leader</b>\n\nMultiple servers can accept writes at the same time. Each one replicates changes to the others.\n\n• Useful for multi-region setups — each region has a leader for low latency writes\n• The challenge: if two leaders accept different writes for the same data at the same time, you get a conflict\n• Conflict resolution strategies: last write wins (by timestamp), higher-ranked server wins, or prompt the user (like a Git merge conflict)\n\n<b>3. Leaderless (Dynamo-style)</b>\n\nAny server can accept writes. There is no single leader.\n\n• Writes go to multiple nodes simultaneously\n• A write is only considered successful when a majority (quorum) of nodes confirm it\n• Quorum rule: if there are N nodes, require W nodes to confirm a write and R nodes to confirm a read, where W + R > N\n• This overlap guarantees at least one node has the latest data on every read\n\nUsed by: Cassandra, DynamoDB",
    note:
      "Replication protects your data and spreads read traffic. Single-leader async replication is the most common choice — it balances performance and reliability.\n\nAsync = faster writes, possible brief stale reads\nSync = always consistent, slower writes\nLeaderless = no single point of failure, uses quorum for consistency",
    whyItMatters:
      "A single database is a single point of failure and a read bottleneck. Replication keeps copies on multiple servers so reads can be spread out and the system can survive a crash.\n\nIf you choose wrong:\n• Async replication for financial data = a crash between write and replication can lose a confirmed transaction\n• Sync replication for everything = slower writes and worse user experience for data that doesn't need strict consistency",
    diagramNote:
      "Fig. 15.1 — Three replication models: single-leader (one writer), multi-leader (multiple writers), and leaderless (quorum-based writes).",
    example:
      "<b>1. Read-heavy app (Single Leader + Async)</b>\nOne primary, three replicas. All writes go to the primary. All reads spread across the three replicas. If the primary crashes, a replica is promoted in under 30 seconds. Read capacity is 4× a single server.\n\n<b>2. Multi-region app (Multi-Leader)</b>\nUS region has a leader for US users. Europe region has a leader for European users. Each replicates to the other for low-latency writes. Challenge: if both sides accept an edit to the same record at the same moment, you get a conflict that needs a resolution strategy.",
    interviewTip:
      "\"I'd use single-leader async replication — writes go to the primary, reads spread across replicas. The brief lag is fine for a social feed.\"\n\n\"For payment data I'd use sync replication — a write must be confirmed on multiple nodes before the user gets a success response.\"\n\nAlways state whether you need async (fast, possibly brief stale reads) or sync (consistent, slower writes).",
    tags: ["Single Leader", "Multi-Leader", "Leaderless", "Quorum", "Async", "Sync", "Failover", "WAL"],
  },
  {
    id: 16,
    section: "Scaling & Distribution",
    title: "Partitioning / Sharding",
    tagline: "Split large data across multiple nodes",
    description:
      "When a single database gets too large to fit on one machine, or receives more writes than one server can handle, you split the data across multiple servers. Each server holds a piece (called a shard or partition). Together, all shards hold the complete dataset.\n\nThe most important goal of sharding: distribute data evenly so no single shard becomes a bottleneck.\n\n<b>Two main partitioning methods</b>\n\n<b>Key-Range Partitioning</b>\nAssign ranges of data to each shard.\n\n• Example: users 1–50,000 → shard 1, users 50,001–100,000 → shard 2\n• Simple and easy to understand\n• Risk: hotspots — if most traffic comes from one range, that shard gets overloaded while others are idle\n\n<b>Hash Partitioning</b>\nUse a hash function to assign data to shards: `shard = hash(id) % N`\n\n• Distributes data more evenly — no predictable range to get overloaded\n• Downside: if you add a new shard (change N), most keys remap to different shards, requiring data migration\n• Consistent hashing solves this migration problem (see concept 49)\n\n<b>Secondary Indexes on Sharded Data</b>\n\nQuerying sharded data by something other than the shard key is tricky.\n\n<b>Local Secondary Index</b>\nEach shard maintains its own index covering only its data.\n\n• Fast writes — each shard updates only its own index\n• Queries spanning multiple shards require a scatter-gather — send the query to all shards and merge results. Slower.\n\n<b>Global Secondary Index</b>\nOne dedicated partition holds an index covering data across all shards.\n\n• Fast reads — one lookup finds which shard has the data\n• Slower writes — every write must update the global index partition too\n\n<b>Hotspots</b>\n\nA hotspot is when one shard receives far more traffic than others.\n\n• Example: sharding by country puts most traffic on the US shard while the Maldives shard sits mostly idle\n• Solution: choose shard keys that distribute writes evenly — user ID hash is usually a safe choice",
    note:
      "Sharding lets your database scale beyond a single machine by splitting data across multiple servers. Choose your shard key carefully — a bad key creates hotspots and defeats the purpose.\n\nHash partitioning gives the most even distribution. Key-range partitioning is simpler but prone to hotspots.",
    whyItMatters:
      "A single database has limits on how much data it can store and how many writes per second it can handle. Sharding lets you break past those limits by splitting data across multiple databases.\n\nIf you choose wrong:\n• Bad shard key (like timestamp) = all new writes hit one shard, others sit idle — a hotspot\n• No planning for cross-shard queries = expensive scatter-gather on every read",
    diagramNote:
      "Fig. 16.1 — Database sharding splits data by shard key; a hotspot forms when one partition receives disproportionate traffic.",
    example:
      "<b>1. User database (Hash Sharding — good)</b>\n100 million users across 10 shards. Each user maps to a shard via `hash(user_id) % 10`. User A always goes to shard 3, User B to shard 7. Writes spread evenly. Works because most queries are for a specific user — no cross-shard joins needed.\n\n<b>2. The hotspot problem (Bad shard key)</b>\nYou shard by country. The US shard gets 70% of all traffic. The Maldives shard handles almost nothing. The US shard is overloaded while others are wasted. Hash sharding avoids this because hash values distribute data evenly regardless of real-world skew.",
    interviewTip:
      "\"I'd shard by `user_id` hash — it distributes writes evenly and most queries are per-user, so no cross-shard joins are needed.\"\n\nAvoid timestamp as a shard key — it creates hotspots where the latest time bucket receives all the writes. Always plan for cross-shard queries — they require scatter-gather and are slow.",
    tags: ["Sharding", "Partitioning", "Consistent Hashing", "Hotspot", "Shard Key", "Global Index", "Local Index"],
  },
  {
    id: 17,
    section: "Scaling & Distribution",
    title: "CAP Theorem",
    tagline: "In distributed systems, choose two of three guarantees",
    description:
      "The CAP Theorem states that a distributed database can only guarantee two out of three properties at the same time.\n\n<b>The three properties</b>\n\n<b>C — Consistency</b>\nEvery read returns the most recent write. No node ever returns stale data.\n\n<b>A — Availability</b>\nEvery request gets a response, even if the data might be slightly out of date. The system never returns an error just because it can't verify freshness.\n\n<b>P — Partition Tolerance</b>\nThe system keeps working even if some servers can't talk to each other (a network partition).\n\n<b>Why the real choice is always CP vs AP</b>\n\nIn any real distributed system, network partitions happen. A cable gets cut, a data center has an outage, a server is temporarily unreachable. You cannot avoid partitions — so Partition Tolerance is not optional.\n\nThis means the actual trade-off you make is between Consistency and Availability:\n\n<b>CP Systems — Consistency over Availability</b>\nDuring a network partition, the system would rather return an error than return stale data.\n\n• Examples: HBase, ZooKeeper\n• Use when: data must always be correct — banking, inventory, reservations\n• \"I'd rather show an error than show you the wrong balance\"\n\n<b>AP Systems — Availability over Consistency</b>\nDuring a network partition, the system keeps responding but may return slightly stale data.\n\n• Examples: Cassandra, DynamoDB, CouchDB\n• Use when: brief inconsistency is acceptable — social feeds, activity logs, analytics\n• \"I'd rather show you a post from 200ms ago than show an error page\"\n\n<b>CA — Does not exist in distributed systems</b>\nYou cannot guarantee both consistency and availability without partition tolerance. Once data is spread across multiple nodes, partitions are inevitable.",
    note:
      "You cannot avoid network partitions in a distributed system. So the real decision is:\n\n• CP — correct data, possible downtime during a partition (banking, payments)\n• AP — always available, possibly slightly stale data (social feeds, analytics)\n\nMatch your choice to how serious inconsistency would be for your use case.",
    whyItMatters:
      "Every database you pick is a CAP decision. Understanding why you choose Cassandra vs PostgreSQL — not just that you know the names — is what separates junior from senior engineers.\n\nIf you choose wrong:\n• CP where you need AP = system returns errors during network issues instead of serving slightly stale data\n• AP where you need CP = users see wrong data (wrong balance, duplicate bookings)",
    diagramNote:
      "Fig. 17.1 — CAP theorem triangle: since partitions are inevitable in distributed systems, the real trade-off is always CP vs AP.",
    example:
      "<b>1. Payment system (CP — Consistency over Availability)</b>\nTwo data centres temporarily lose network connection. A CP system stops accepting new transactions until the connection is restored. Why: processing on both sides during a split could charge the user twice. A brief error is acceptable. A wrong charge is not.\n\n<b>2. Social media feed (AP — Availability over Consistency)</b>\nSame network split. An AP system keeps running on both sides, showing slightly outdated feed data. Why: a post appearing a few seconds late is completely fine. No one is harmed by briefly stale data in a social feed.",
    interviewTip:
      "\"For payment processing I need CP — consistency is non-negotiable. I'd use PostgreSQL.\"\n\n\"For the activity feed I'd use AP — availability and speed matter more. I'd use Cassandra.\"\n\nRemember: CA (Consistent + Available, no Partition Tolerance) does not exist in distributed systems. Partitions always happen. The real choice is always CP vs AP.",
    tags: ["CAP Theorem", "Consistency", "Availability", "Partition Tolerance", "CP", "AP", "Eventual Consistency"],
  },

  // ─────────────────────────────────────────────
  // RELIABILITY
  // ─────────────────────────────────────────────
  {
    id: 18,
    section: "Reliability",
    title: "Message Queues",
    tagline: "Async communication between services via a broker",
    description:
      "Some operations in a system need to happen immediately. Others can happen in the background. A message queue is how you handle the second category.\n\n<b>Synchronous vs Asynchronous</b>\n\n• `Synchronous` — the caller waits for a response before continuing. Example: checking inventory before confirming an order. Must happen right now.\n• `Asynchronous` — the caller fires off a task and moves on. The work happens later. Example: sending an order confirmation email. The user doesn't need to wait for the email to be sent.\n\nA message queue sits between two services — a producer that creates work and a consumer that processes it. The producer drops a message in, and the consumer picks it up when it's ready.\n\n<b>Key Concepts</b>\n\n<b>Dead Letter Queue (DLQ)</b>\nIf a message fails to be processed after several retries, it gets moved to a separate DLQ instead of being dropped silently. This lets you inspect and debug failed messages later.\n\n<b>Poison Messages</b>\nA poison message is one that always causes the consumer to crash or fail. Without a DLQ, it would keep getting retried forever, blocking everything else. The DLQ contains it.\n\n<b>Priority Queue</b>\nMessages can be given a priority number. High-priority messages are processed first, even if they arrived later.\n\n<b>Pull vs Push</b>\n• `Pull` — the consumer asks the queue for new messages when it's ready\n• `Push` — the queue pushes messages to the consumer as soon as they arrive\n\n<b>Pub/Sub Model</b>\nInstead of one producer sending to one consumer, a publisher sends a message to a topic. Multiple independent subscribers all receive that same message.\n\n• Example: an order is placed → order service publishes to a topic → email service, SMS service, and analytics service all receive the event independently\n\n<b>Deduplication</b>\nQueues may deliver the same message more than once (network retries, failures). Your consumer must track which messages it has already processed to avoid doing the same work twice.",
    note:
      "A message queue decouples services so they can scale and fail independently. Without a queue, a slow email service would block your entire checkout flow.\n\nKey tools: AWS SQS, RabbitMQ, Kafka. Always configure a Dead Letter Queue to catch failed messages.",
    whyItMatters:
      "Without a message queue, a slow email service blocks your entire checkout flow. Queues let services scale independently and handle traffic spikes by buffering work.\n\nIf you choose wrong:\n• Doing everything synchronously = one slow service makes your whole app slow for users\n• No Dead Letter Queue = failed messages disappear silently, impossible to debug",
    diagramNote:
      "Fig. 18.1 — Message queue (point-to-point with DLQ) vs. pub/sub (one publisher, many independent subscribers).",
    example:
      "<b>1. E-commerce order (Sync + Async together)</b>\nWhen a user places an order, the inventory check must be synchronous — you need to confirm stock before saying \"Order placed.\" But sending the confirmation email, notifying the delivery partner, and updating analytics can all be async. Three messages go into the queue. If the email service is down, messages wait. When it recovers, it processes the backlog. The user's order was already confirmed.\n\n<b>2. Pub/Sub for events</b>\nA user posts a photo. The photo service publishes one \"PhotoUploaded\" event to a topic. Three independent subscribers receive it — thumbnail generator, feed updater, notification service. Each does its job without knowing about the others. Adding a 4th subscriber (like an AI moderation service) requires zero changes to existing code.",
    interviewTip:
      "\"I'd use a message queue here because the email sending is slow and non-critical — the user shouldn't wait for it.\"\n\n\"I'd always configure a Dead Letter Queue so that if a message fails after 3 retries, it's moved there for investigation instead of being dropped.\"\n\nUse queues for slow operations, independent scaling, and work that can be retried. Don't use them when the user needs an immediate result.",
    tags: ["Pub/Sub", "FIFO", "Priority Queue", "DLQ", "Dead Letter Queue", "Async", "RabbitMQ", "SQS", "Producer", "Consumer"],
  },
  {
    id: 19,
    section: "Reliability",
    title: "Fault Tolerance",
    tagline: "Designing for inevitable failures",
    description:
      "No system runs perfectly forever. Hardware breaks. Bugs slip into production. Engineers make mistakes. Fault tolerance is about designing your system so that when something goes wrong, it fails gracefully — not catastrophically.\n\nThere are three main categories of faults, each requiring a different type of fix.\n\n<b>1. Hardware Faults</b>\n\nPhysical failures that are random and unpredictable.\n\n• Disk fills up or fails\n• Server runs out of memory\n• Network cable is damaged\n• Power outage in a data center\n• Server overheats and shuts down\n\nSolution: redundancy. Run multiple servers. Replicate your database. Use RAID for disk redundancy. Add UPS (uninterruptible power supply) for power.\n\nYou can't predict these failures — you prepare for them by making every component replaceable.\n\n<b>2. Software Faults</b>\n\nBugs and bad code that are deterministic — if it happened once, it will happen again under the same conditions.\n\n• Unhandled exceptions crashing the server\n• A bad deployment breaking a feature\n• Config mismatch between development and production\n• A merge conflict introducing a regression\n• An N+1 query slowing the database under load\n\nSolution: process improvements. Thorough testing, code reviews, staged rollouts (deploy to 1% of users first), environment parity (staging matches production), and load testing before release.\n\n<b>3. Human Faults</b>\n\nMistakes made by engineers — the hardest to prevent and the most unpredictable.\n\n• Wrong config pushed to production\n• Accidentally deleting production data\n• Running a database migration on the wrong environment\n• A quick hotfix made without testing\n\nSolution: guard rails and process. Mandatory code reviews, staging environments, confirmation dialogs for destructive actions, dry-run mode for migrations, and detailed audit logs so you can trace what happened.",
    note:
      "Fault tolerance means the system degrades gracefully — one failure doesn't take down everything.\n\n• Hardware faults → redundancy (more servers, replicated DB)\n• Software faults → process (testing, staged rollouts, code review)\n• Human faults → guard rails (staging, audit logs, confirm-before-delete)",
    whyItMatters:
      "No system is 100% reliable. Hardware breaks, deployments go wrong, and engineers make mistakes. A fault-tolerant system handles each type of failure differently instead of treating them all the same.\n\nIf you choose wrong:\n• Only handling hardware faults = software bugs and human mistakes still take you down\n• No staged rollouts = a bad deployment hits 100% of users at once",
    diagramNote:
      "Fig. 19.1 — Three fault categories — hardware, software, and human — each requiring a different class of mitigation.",
    example:
      "<b>1. Hardware fault (redundancy)</b>\nYour database server's hard drive fails. Because you have a replica, the replica is promoted to primary in under 30 seconds. Users experience a brief slowdown, not a total outage. Without the replica, the database would be completely down until a new disk is installed.\n\n<b>2. Human fault (guard rails)</b>\nAn engineer accidentally runs a destructive database migration on production instead of staging. Without guard rails, all production data is gone. With guard rails — a staging environment, a confirmation prompt, and an audit log — the mistake is caught before it causes damage.",
    interviewTip:
      "\"I'd add redundancy for every critical component — primary and replica for the database, active-passive for the load balancer.\"\n\n\"For deployments I'd use a canary rollout — send 1% of traffic to the new version first, monitor error rate for 10 minutes, then gradually roll out to 100%.\"\n\nThink in three categories: hardware (fix with redundancy), software (fix with testing and staged rollouts), human (fix with guard rails and staging environments).",
    tags: ["Redundancy", "Resilience", "Hardware Faults", "Software Bugs", "Human Error", "Recovery", "Availability"],
  },
  {
    id: 20,
    section: "Reliability",
    title: "Monitoring & Observability",
    tagline: "Logs, metrics, and percentiles to understand system health",
    description:
      "Monitoring is how you know your system is working correctly. Observability is your ability to understand what is happening inside a system by looking at its outputs.\n\nYou monitor two layers: your APIs and your machines.\n\n<b>API Monitoring</b>\n\n<b>Throughput</b>\nHow many requests per second is your system handling?\n\n• Alert when you're approaching your server's capacity limit (commonly set at 80%)\n• A sudden spike in throughput could mean a traffic surge — good — or a bot attack — bad\n\n<b>Error Rates</b>\nTrack how often requests return error status codes.\n\n• `5xx` errors (500, 503) mean your server is broken\n• `4xx` errors (400, 404) mean clients are sending bad requests\n• A sudden spike in error rate usually means a bad deployment or a failing dependency\n\n<b>Latency Percentiles — Never Use Averages</b>\nAverages lie. Use percentiles instead.\n\n• `P50` — 50% of requests complete faster than this value\n• `P90` — 90% of requests complete faster than this value\n• `P99` — 99% of requests complete faster than this value\n\nExample: P50 = 50ms, P99 = 8 seconds. The average might look like 100ms and seem fine. But 1 in every 100 users is waiting 8 full seconds. Percentiles reveal this; averages hide it.\n\nP99 is the gold standard — it represents your worst real-user experience.\n\n<b>Machine Monitoring</b>\n\nTrack the health of the servers running your application:\n\n• `CPU` — alert at 70–80% sustained usage\n• `Memory` — alert at 85–90% (leaving headroom prevents crashes)\n• `Disk I/O` — alert when disk is consistently at capacity\n• `Network bandwidth` — alert when approaching the limit of the network interface\n\n<b>SLOs — Service Level Objectives</b>\n\nAn SLO formalizes your performance target. Example: \"P99 latency must be under 500ms for 99.9% of minutes in a month.\"\n\nSLOs turn vague goals like \"the system should be fast\" into concrete, measurable engineering requirements.",
    note:
      "Never use average latency. Always use percentiles. P99 is the number that represents your real worst-case user experience.\n\nSet alerts on P99 latency, error rate, and machine resource usage before users start complaining — not after.",
    whyItMatters:
      "Average latency hides problems. If P50 is 50ms but P99 is 8 seconds, the average looks fine — but 1 in every 100 users is waiting 8 full seconds. Without percentile monitoring, you wouldn't notice until users start complaining.\n\nIf you choose wrong:\n• Using only average latency = tail latency issues stay hidden until users complain loudly\n• No alerts on machine resources = you find out the disk is full when the app crashes",
    diagramNote:
      "Fig. 20.1 — Observability dashboards: API metrics (latency percentiles, error rate, throughput) alongside machine metrics (CPU, memory, disk, network).",
    example:
      "<b>1. Catching a hidden slow query</b>\nP50 = 50ms, P99 = 8 seconds. The average = ~100ms — looks fine on a dashboard. But a P99 alert fires. You trace the issue to a database query doing a full table scan on 100 million rows because an index is missing. You add the index. P99 drops to 80ms. Without percentile monitoring, this would have stayed hidden.\n\n<b>2. Detecting a bad deployment</b>\nYou deploy a new version at 2pm. Your error rate alert fires — 4xx errors jump from 0.1% to 15%. You roll back the deployment within 5 minutes. Without error rate monitoring, you'd find out from user complaints hours later.",
    interviewTip:
      "\"I'd track P99 latency, error rate, and throughput for APIs. I'd alert when P99 goes above 500ms for 5 consecutive minutes.\"\n\n\"On the machine side I'd alert at 80% CPU and 85% memory — leaving headroom before a crash.\"\n\nNever say 'average latency' in a system design interview. Always say P99 or percentiles. Mention SLOs to show production-grade thinking.",
    tags: ["P99", "P95", "P90", "Percentile", "Latency", "Throughput", "SLO", "Metrics", "Alerting", "Observability"],
  },

  // ─────────────────────────────────────────────
  // CASE STUDY
  // ─────────────────────────────────────────────
  {
    id: 21,
    section: "Case Study",
    title: "Video Streaming Architecture",
    tagline: "How YouTube-style streaming actually works end to end",
    description:
      "Video streaming works by splitting a video into small segments and delivering them one at a time — never making the user download the whole file upfront.\n\n<b>The Problem with Naive Downloading</b>\n• Downloading a full 2GB file before playback starts causes huge latency\n  ↳ A user who stops after 30 seconds wasted bandwidth downloading 90% of nothing\n• The real solution: split the video into 1-second segments and stream them individually\n\n<b>How Segments Work</b>\n• The `RTMP` protocol streams segments over TCP, which maintains order\n• Client pulls one segment at a time — playback starts after just the first few segments arrive\n• A 20-minute video at 50GB becomes ~1,200 segments of about 40MB each\n\n<b>Adaptive Bitrate Streaming (ABR)</b>\n• Each segment is pre-encoded at multiple resolutions: 4K, 1080p, 720p, 480p, 240p\n• The video player monitors your available bandwidth in real time\n• If bandwidth drops from 300Mbps to 10Mbps, the player switches from 4K to 480p seamlessly\n  ↳ The switch happens at segment boundaries — no buffering, no interruption\n\n<b>The Upload Pipeline</b>\n• User uploads raw video → Transformation Service detects metadata and splits into segments\n• A message queue distributes segments to parallel encoding workers\n  ↳ One worker per resolution encodes simultaneously — 5 resolutions = 5 workers running in parallel\n• Finished segments are pushed to a CDN with edge servers near users worldwide\n• Client requests segments from the nearest CDN edge — not from the origin server",
    whyItMatters:
      "Video is the most bandwidth-intensive workload on the internet. Designing it wrong means either buffering for users or wasting enormous infrastructure costs.\n\nIf you choose wrong:\n• No segmentation — users wait minutes before playback starts\n• No ABR — users on mobile data buffer constantly; users on fiber get unnecessary low quality\n• No parallel workers — encoding queue grows faster than it drains, videos take hours to go live\n• No CDN — every segment request hits the origin server, which collapses under load\n\nThis concept ties together CDN, message queues, parallel workers, and protocol design — making it one of the richest system design questions you can be asked.",
    diagramNote:
      "Fig. 21.1 — Video pipeline: upload triggers parallel encoding workers per resolution; outputs are distributed via a multi-region CDN with adaptive bitrate switching.",
    example:
      "YouTube processes 500 hours of video uploaded every minute.\n\n<b>1. The Setup</b>\n• Every upload triggers a transformation service that splits the video and publishes encoding jobs to a message queue\n• Parallel workers encode each segment at 6+ quality levels simultaneously\n\n<b>2. What happens</b>\n• Finished segments are distributed to CDN edge servers in hundreds of locations worldwide\n• When you hit play, your player fetches segments from the edge server closest to you\n\n<b>3. The result</b>\n• 2 billion users each receive a different quality level based on their exact connection speed — all handled automatically by ABR with no manual switching",
    interviewTip:
      "\"When they ask you to design a video streaming platform, start by explaining why you never send the full file.\"\n\n• <b>1. Segmentation</b> — split into 1-second chunks so playback starts immediately and users only download what they watch\n• <b>2. ABR</b> — pre-encode every segment at multiple resolutions; the player picks quality based on available bandwidth\n• <b>3. Parallel encoding</b> — one worker per resolution, all running simultaneously; a message queue distributes the work\n• <b>4. CDN</b> — segments are cached at edge servers globally; clients never hit the origin directly\n• <b>5. Quick math</b> — \"a 20-min video at 50GB splits into ~1,200 segments; across 5 quality levels, that's ~72MB per segment group\"\n\nFinish by mentioning `RTMP` for the upload protocol and HLS/DASH for the client streaming protocol — interviewers often ask what protocol connects the pieces.",
    tags: ["Video Streaming", "CDN", "Adaptive Bitrate", "RTMP", "RTSP", "Encoding", "Segments", "FFmpeg", "ABR"],
  },

  // ─────────────────────────────────────────────
  // ADVANCED TOPICS
  // ─────────────────────────────────────────────
  {
    id: 22,
    section: "Advanced Topics",
    title: "CDN",
    tagline: "Serves static content from locations close to users",
    description:
      "A CDN (Content Delivery Network) is a global network of edge servers that cache your static assets close to users so requests travel a few milliseconds instead of thousands of miles.\n\n<b>How it works</b>\nWhen a user requests a file, DNS routes them to the nearest edge server (Point of Presence). If the edge has it cached, it responds immediately. If not (cache miss), the edge fetches the file from your origin server, caches it, and serves it — future requests for the same file never touch your origin.\n\n<b>What gets cached</b>\n• Images, videos, JS, CSS, fonts\n• HTML pages with a short TTL\n• API responses (when safe to cache)\n\n<b>Cache control</b>\nThe `Cache-Control` header tells edge servers how long to hold a file. The `ETag` header lets the edge revalidate stale content without re-downloading it. For versioned filenames like `bundle.a3f8.js`, you can set infinite TTL — the filename changes on every deploy, so old cached files are never stale.\n\n<b>CDN providers</b>\n• Cloudflare, Fastly, Akamai — major commercial options\n• AWS CloudFront, Google Cloud CDN — cloud-native options",
    whyItMatters:
      "CDNs offload 80–95% of traffic from your origin server. Without one, every static file request crosses the globe to your server — wasteful and slow.\n\nIf you choose wrong:\n• No CDN on a global app: users in Tokyo hit a Virginia server and wait ~200ms extra per request\n• Wrong TTL (too short): CDN misses every time, origin gets full traffic anyway\n• Wrong TTL (too long): users see stale assets after a deploy\n\nThis concept connects to caching, DNS routing, and HTTP headers.",
    diagramNote:
      "Fig. 22.1 — CDN topology: users hit the nearest edge server; on a cache miss the edge fetches once from origin and caches locally for all subsequent requests.",
    example:
      "<b>1. Setup</b>\nNetflix built Open Connect — their own CDN with appliances physically inside ISP data centers. Netflix video bytes never leave the local network.\n\n<b>2. What happens</b>\nA user in Chicago streams a movie. The video chunks are already cached at the Chicago ISP. The Netflix origin server in Virginia is never touched.\n\n<b>3. The result</b>\nCache hit rate exceeds 95%. YouTube uses Google's global edge network to serve 1 billion hours of video daily with under 50ms start time for most users.",
    interviewTip:
      "\"When they ask you to design a media-heavy or global app, mention CDN early and explain why.\"\n• <b>1. What to cache</b> — static assets with long TTL; versioned filenames (`bundle.a3f8.js`) get infinite TTL\n• <b>2. What not to cache</b> — dynamic, personalized data must still hit origin; CDN cannot cache a user's feed\n• <b>3. Cache invalidation</b> — versioned filenames eliminate the need for manual purges\n\nFollow up: interviewers often ask how you handle a CDN cache with stale content after a deploy — versioned filenames are the clean answer.",
    tags: ["CDN", "Edge", "Latency", "Static Assets", "Cache Control", "Cloudflare", "Fastly"],
  },
  {
    id: 23,
    section: "Advanced Topics",
    title: "Database Indexing",
    tagline: "Makes queries fast by avoiding full table scans",
    description:
      "A database index is a separate data structure that lets the database find rows without scanning every row in the table — turning a slow O(n) scan into a fast O(log n) lookup.\n\n<b>How a B-tree index works</b>\nWithout an index on `user_id`, the query `SELECT * FROM orders WHERE user_id = 123` scans all 100 million rows — that can take minutes. With a B-tree index on `user_id`, the database navigates the tree in O(log n) steps — milliseconds.\n\n<b>Types of indexes</b>\n• <b>Single-column index</b> — speeds lookups on one column\n• <b>Composite index</b> — index on `(user_id, status)` serves `WHERE user_id = 123 AND status = 'pending'` in one lookup\n  ↳ column order matters: the index only helps if you filter from left to right\n• <b>Covering index</b> — includes all columns a query needs so the database never touches the main table at all\n\n<b>The write tradeoff</b>\nEvery `INSERT`, `UPDATE`, or `DELETE` must also update every index on that table. Too many indexes on a write-heavy table slows inserts significantly. Use `EXPLAIN ANALYZE` to verify an index is actually being used.",
    whyItMatters:
      "Missing indexes are the most common cause of database performance problems at scale. An unindexed query on a 100M-row table can take minutes; the same query with a proper index takes under 1ms.\n\nIf you choose wrong:\n• No index on a heavily queried column: full table scan on every request — the DB becomes a bottleneck\n• Too many indexes on a write-heavy table: every insert slows down updating all those index structures\n• Wrong composite index column order: the index exists but the query doesn't use it\n\nThis concept connects to query optimization, schema design, and write vs read tradeoffs.",
    diagramNote:
      "Fig. 23.1 — B-tree index turns an O(n) full table scan into an O(log n) lookup; composite indexes add a second level to the tree.",
    example:
      "<b>1. Setup</b>\nAn e-commerce DB has 100 million rows in the `orders` table. A support dashboard runs: `SELECT * FROM orders WHERE user_id = 123 AND status = 'pending'`.\n\n<b>2. Without index</b>\nThe database scans all 100M rows. `EXPLAIN ANALYZE` shows `Seq Scan`. Query takes ~2 minutes. The dashboard times out.\n\n<b>3. With composite index on `(user_id, status)`</b>\nThe database jumps directly to matching rows. `EXPLAIN ANALYZE` shows `Index Scan`. Query takes ~1ms.",
    interviewTip:
      "\"When they ask you to design a query-heavy system, always ask: what are the access patterns? Then design indexes around those patterns.\"\n• <b>1. Identify hot queries</b> — which columns appear in `WHERE`, `JOIN`, and `ORDER BY` clauses most often\n• <b>2. Composite index column order</b> — most selective column first; queries must filter left-to-right\n• <b>3. Write overhead</b> — warn that indexes on write-heavy tables add cost to every insert\n\nFollow up: mention `EXPLAIN ANALYZE` to verify index usage — just having an index doesn't guarantee the query planner uses it.",
    tags: ["B-tree", "Composite Index", "Covering Index", "Query Optimization", "SQL", "EXPLAIN"],
  },
  {
    id: 24,
    section: "Advanced Topics",
    title: "Rate Limiting",
    tagline: "Prevents API abuse by capping request rates",
    description:
      "Rate limiting controls how many requests a client can make in a given time window — protecting your API from abuse, overload, and cost runaway.\n\n<b>The four algorithms</b>\n• <b>Fixed Window</b> — counter resets every minute. Simple, but allows doubling at window boundaries: 100 requests in the last second of minute 1 + 100 in the first second of minute 2 = 200 requests in 2 seconds\n• <b>Sliding Window Log</b> — store a timestamp for every request; count entries in the last 60 seconds. Precise, but memory-heavy for high-traffic clients\n• <b>Token Bucket</b> — tokens refill at a constant rate (e.g. 10/sec), bucket holds max 100. Each request costs 1 token. Empty bucket = reject. Allows controlled bursts. Most common algorithm\n• <b>Leaky Bucket</b> — requests enter a fixed-size queue. A worker drains the queue at a constant rate (e.g. 10/sec) regardless of how fast requests arrive. If the queue is full, new requests are dropped\n  ↳ key difference: Token Bucket allows bursts up to bucket size; Leaky Bucket enforces a strictly constant output rate\n  ↳ use Leaky Bucket to smooth bursty traffic before it hits a downstream service that can't handle spikes\n\n<b>Implementation</b>\nState lives in Redis. Use atomic `INCR` + `EXPIRE` for Fixed Window. Return `HTTP 429` with a `Retry-After` header so clients know when to try again.",
    whyItMatters:
      "Without rate limiting, one bad actor — or a buggy client in a retry loop — exhausts server resources and starves everyone else.\n\nIf you choose wrong:\n• Fixed Window with a boundary exploit: a client gets 2x the allowed requests by straddling the window edge\n• Token Bucket for a downstream vendor: bursts get through and overwhelm the third-party API\n• No Redis atomicity: race conditions cause two requests to both pass a limit that should only allow one\n\nThis concept connects to Redis, circuit breakers, and API gateway design.",
    diagramNote:
      "Fig. 24.1 — Token bucket allows controlled bursts by accumulating tokens; leaky bucket enforces a strict constant output rate regardless of incoming traffic.",
    example:
      "<b>1. Token Bucket — Stripe API</b>\nStripe allows short bursts for legitimate batch operations (e.g. creating 50 charges at midnight). Token Bucket lets a client burst up to their bucket size, then throttles to the refill rate.\n\n<b>2. Leaky Bucket — outbound payment calls</b>\nAn API gateway calls a third-party payment provider that allows max 50 requests/sec. No matter how many orders arrive simultaneously at the gateway, Leaky Bucket drains the queue at exactly 50/sec — the vendor never sees a spike.\n\n<b>3. The result</b>\nStripe API users get responsive burst handling. The payment vendor never gets overloaded. Both sides protected by different algorithms tuned to different needs.",
    interviewTip:
      "\"When they ask you to design an API gateway or protect a downstream service, walk through which algorithm fits the constraint.\"\n• <b>1. Token Bucket</b> — use when clients legitimately need burst capacity (batch operations, mobile apps catching up)\n• <b>2. Leaky Bucket</b> — use when a downstream service needs a constant, predictable request rate regardless of upstream spikes\n• <b>3. Redis implementation</b> — atomic `INCR` + `EXPIRE` for counters; return `HTTP 429` with `Retry-After` header\n\nFollow up: interviewers often ask how you handle distributed rate limiting across 10 API servers — Redis as a shared counter store is the answer.",
    tags: ["Token Bucket", "Leaky Bucket", "Sliding Window", "Fixed Window", "Redis", "HTTP 429", "Throttling"],
  },
  {
    id: 25,
    section: "Advanced Topics",
    title: "WebSockets",
    tagline: "Persistent bidirectional real-time communication",
    description:
      "WebSockets establish a persistent, full-duplex connection between client and server so either side can send messages at any time — without a new HTTP request for each exchange.\n\n<b>How the handshake works</b>\nThe connection starts as a normal HTTP request with an `Upgrade: websocket` header. The server agrees, the protocol switches, and from that point on both sides communicate over the same open TCP connection with no HTTP overhead.\n\n<b>Comparing real-time options</b>\n• <b>Long Polling</b> — client asks \"anything new?\" every few seconds. Simple but wasteful: most polls return nothing\n  ↳ latency: depends on poll interval (1–5 seconds typically)\n• <b>SSE (Server-Sent Events)</b> — server pushes data to client over a regular HTTP connection. Client cannot send back. Simple, built into browsers\n  ↳ use when you only need server-to-client push: live scores, activity feeds, progress bars\n• <b>WebSockets</b> — full bidirectional. Either side can send at any moment\n  ↳ use when you need client-to-server and server-to-client: chat, collaborative editing, live cursors, gaming\n\n<b>When NOT to use WebSockets</b>\nIf the data only flows one way (server → client), SSE is simpler, cheaper, and works through standard HTTP proxies and load balancers with no special configuration.",
    whyItMatters:
      "Polling wastes bandwidth — most polls return nothing, but you're still paying for every request. WebSockets eliminate that overhead and drop latency below 50ms.\n\nIf you choose wrong:\n• WebSockets for a one-way notification feed: unnecessarily complex; SSE is simpler and sufficient\n• Long polling for a real-time game: ~2–5 second lag makes it unplayable\n• WebSockets without sticky sessions: a user's messages go to one server, but their WebSocket connection is on another — messages get lost\n\nThis concept connects to pub/sub, load balancing, and Redis for multi-server fan-out.",
    diagramNote:
      "Fig. 25.1 — WebSockets maintain a persistent full-duplex connection; HTTP closes after each response; SSE is server-to-client push only.",
    example:
      "<b>1. Setup</b>\nFigma has 10 users collaborating on the same design file. Every cursor move and shape edit must appear on all 10 screens within 50ms.\n\n<b>2. What happens</b>\nEach user's browser opens a WebSocket connection to a Figma server. When User A moves a shape, the browser sends a message over the WebSocket. The server broadcasts it to all 9 other open connections. No HTTP requests, no polling.\n\n<b>3. The result</b>\nAll 10 editors see the shape move in under 50ms. Slack uses the same pattern for real-time message delivery — you see a message appear without refreshing the page.",
    interviewTip:
      "\"When they ask about real-time features, immediately map the data direction to the right protocol.\"\n• <b>1. One-way push</b> — use SSE: simpler, no special load balancer config, works over HTTP/2\n• <b>2. Bidirectional</b> — use WebSockets: chat, gaming, collaborative editing, live cursors\n• <b>3. Scaling WebSockets</b> — sticky sessions at the load balancer OR a Redis pub/sub layer so any server can fan out messages to any connected client\n\nFollow up: interviewers often ask how you scale WebSockets to 1 million connections — the answer involves horizontal scaling + Redis pub/sub for cross-server message delivery.",
    tags: ["WebSockets", "Real-time", "SSE", "Long Polling", "Bidirectional", "Full-duplex", "TCP"],
  },
  {
    id: 26,
    section: "Advanced Topics",
    title: "Idempotency",
    tagline: "The same request shouldn't cause duplicate effects",
    description:
      "An idempotent operation produces the same result whether you call it once or ten times — making it safe to retry without causing duplicate side effects.\n\n<b>Which HTTP methods are naturally idempotent</b>\n• `GET` — reading data never changes state; call it 100 times, same result\n• `PUT` — setting a value to X twice is the same as setting it once\n• `DELETE` — deleting a resource that's already gone returns the same outcome\n• `POST` is NOT idempotent by default — submitting a payment form twice creates two charges\n\n<b>How to make POST idempotent</b>\nThe client generates a unique UUID and sends it as an `Idempotency-Key` header with every request. The server stores the result in Redis keyed by that UUID with a 24-hour TTL. If the same key arrives again (because the client retried after a timeout), the server returns the stored result instead of processing again — no second charge, no second email.\n\n<b>Why this is critical</b>\nNetworks are unreliable. A client sends a payment request, the network times out, and the client doesn't know if the server processed it or not. The safe move is to retry. Without idempotency, that retry causes a duplicate. The formula: at-least-once delivery + idempotent consumers = exactly-once semantics.",
    whyItMatters:
      "Networks drop connections. Clients must retry on timeout. Without idempotency, every retry is a potential duplicate side effect — a double charge, a duplicate order, or a second welcome email sent to the same user.\n\nIf you choose wrong:\n• No idempotency on payment endpoints: a mobile app retries after a network drop and the user gets charged twice\n• No TTL on idempotency keys: Redis fills up with keys from years ago\n• Wrong scope (per user instead of per request): two different requests by the same user incorrectly deduplicate\n\nThis concept connects to retries, circuit breakers, and message queue delivery guarantees.",
    diagramNote:
      "Fig. 26.1 — Idempotency key flow: the server stores the result of the first request so any retry returns the same response without re-processing.",
    example:
      "<b>1. Setup</b>\nStripe processes payments via a POST to their charges API. Your backend calls Stripe, but the HTTP connection drops before you receive a response. You don't know if Stripe charged the card.\n\n<b>2. What happens</b>\nYour backend generated a UUID (`idem-key-abc123`) before the first attempt and stored it. On retry, it sends the same `Idempotency-Key: idem-key-abc123` header. Stripe looks up that key in their store and finds the original charge.\n\n<b>3. The result</b>\nStripe returns the original charge object — no second charge created. The customer's card was only billed once, even though your server called Stripe twice.",
    interviewTip:
      "\"When they ask you to design a payment system or any mutation with real-world consequences, bring up idempotency before they ask.\"\n• <b>1. Client generates key</b> — UUID created before the first attempt; reused on every retry for the same operation\n• <b>2. Server stores result</b> — Redis with 24-hour TTL; key maps to the full response of the original request\n• <b>3. The formula</b> — at-least-once delivery + idempotent consumers = exactly-once semantics\n\nFollow up: interviewers ask where to store idempotency keys — Redis is common, but a database table works too if you need durability beyond Redis TTL.",
    tags: ["Idempotency", "Retries", "Payments", "Exactly-Once", "Idempotency-Key", "Stripe"],
  },
  {
    id: 27,
    section: "Advanced Topics",
    title: "Kafka",
    tagline: "High-throughput distributed event streaming",
    description:
      "Kafka is a distributed event streaming platform that stores events in an ordered, append-only log — making it fundamentally different from traditional message queues where messages disappear after consumption.\n\n<b>The key difference from traditional queues</b>\nIn SQS or RabbitMQ, a message is deleted once a consumer processes it. In Kafka, events are retained in an immutable log for a configurable period (days, weeks, or forever). This enables:\n• Multiple independent consumers reading the same events at their own pace\n• Replay — rewind to any point in history to reprocess data or debug\n• Audit logs with no extra work\n\n<b>How consumers work</b>\nEach consumer tracks its own offset (its position in the log). Two different services — say an analytics pipeline and a notification service — can both consume the same `order_placed` topic without interfering with each other. Each maintains its own offset independently.\n\n<b>Partitions for parallelism</b>\nA topic is split into partitions, each an independent ordered log. Within a consumer group, each partition is assigned to one consumer — so 10 partitions = 10 consumers processing in parallel. Partitions are replicated across brokers for fault tolerance.\n\n<b>When to use Kafka vs SQS/RabbitMQ</b>\n• Use Kafka when multiple consumers need the same event, throughput is very high (millions/sec), or you need replay\n• Use SQS/RabbitMQ for simple task queues where one consumer handles each message once",
    whyItMatters:
      "At massive scale, traditional queues become bottlenecks because you can't fan out the same event to multiple systems. Kafka solves this with a single event stream that any number of consumers can read independently.\n\nIf you choose wrong:\n• SQS for a system where 5 services need the same event: you'd have to duplicate the message into 5 separate queues\n• Kafka for a simple background job queue: massive operational overhead for no benefit; use SQS\n• No partition strategy: all events go to one partition, one consumer handles everything — no parallelism\n\nThis concept connects to event sourcing, CDC (change data capture), and stream processing.",
    diagramNote:
      "Fig. 27.1 — Kafka topics are partitioned logs; each consumer group tracks its own offset, enabling independent replay and parallel consumption.",
    example:
      "<b>1. Setup</b>\nUber fires a `trip_completed` event when a ride ends. Three services need this event: billing, driver analytics, and surge pricing recalculation.\n\n<b>2. What happens</b>\nAll three services are separate consumer groups subscribed to the `trip_completed` Kafka topic. Each maintains its own offset. Billing processes events at 50/sec, analytics at 200/sec, surge pricing at 1000/sec — all independent.\n\n<b>3. The result</b>\nLinkedIn (Kafka's creator) processes 7 trillion messages per day using this pattern. No service blocks another. If analytics falls behind, it just replays — it hasn't missed any events.",
    interviewTip:
      "\"When they ask you about event-driven architecture or high-throughput data pipelines, position Kafka by contrasting it with a traditional queue.\"\n• <b>1. Log vs queue</b> — Kafka retains events; SQS deletes them after consumption; this unlocks replay and fan-out\n• <b>2. Consumer groups</b> — each group gets all events independently; within a group, partitions distribute load\n• <b>3. When NOT to use Kafka</b> — simple background jobs with one consumer? Use SQS; Kafka is overkill\n\nFollow up: interviewers often ask how Kafka handles ordering — events within a partition are strictly ordered; across partitions there is no global order.",
    tags: ["Kafka", "Event Streaming", "Log", "Partitions", "Consumer Groups", "Replay", "CDC"],
  },
  {
    id: 28,
    section: "Advanced Topics",
    title: "Circuit Breaker & Retry",
    tagline: "Stop cascading failures and handle transient errors safely",
    description:
      "A circuit breaker wraps calls to a downstream service and automatically stops sending traffic when that service is struggling — preventing a single slow dependency from taking down your entire system.\n\n<b>The three circuit breaker states</b>\n• <b>Closed</b> — normal operation; requests pass through; failures are counted\n• <b>Open</b> — failure threshold exceeded; requests fail immediately without calling the downstream service, giving it time to recover\n• <b>Half-Open</b> — after a timeout, one probe request is allowed through; if it succeeds, the circuit closes again; if it fails, it re-opens\n\n<b>Why this prevents cascading failures</b>\nWithout a circuit breaker, a slow payment service holds connections open. Thread pool fills up waiting. Other services (unrelated to payments) can't get threads and start failing too — one bad service takes down everything.\n\n<b>Exponential backoff for retries</b>\nImmediately retrying a failed request under load creates a thundering herd — thousands of clients all hammer a recovering service at the same moment, preventing recovery. Formula: `wait = base × 2^attempt` gives waits of 1s, 2s, 4s, 8s. Add jitter (randomness) so clients don't all wake up at exactly the same second.\n\n<b>What to retry vs not retry</b>\n• Retry: 503 (service unavailable), 429 (rate limited), network timeout — transient failures\n• Never retry: 400 (bad request), 404 (not found) — permanent failures that won't succeed regardless",
    whyItMatters:
      "In microservices, one slow downstream service can hold every thread in your system waiting for a response. Circuit breakers contain the blast radius. Backoff prevents retry storms from making a partial outage into a total outage.\n\nIf you choose wrong:\n• No circuit breaker: one slow database query holds all your threads; every service in the system starts timing out\n• Immediate retry without backoff: 1,000 clients all retry simultaneously; the recovering service gets hammered and can't recover\n• Retrying a 400 error: the request will never succeed; you just add load for no reason\n\nThis concept connects to idempotency, fallbacks, and microservice resilience.",
    diagramNote:
      "Fig. 28.1 — Circuit breaker state machine: trips open after repeated failures, probes with half-open, and recovers to closed on success.",
    example:
      "<b>1. Setup</b>\nNetflix's homepage calls a recommendation service. The recommendation service is struggling — 50% of requests time out.\n\n<b>2. Circuit breaker in action</b>\nNetflix uses Hystrix/resilience4j wrapping every inter-service call. After 50% failure rate in 10 seconds, the circuit opens. The recommendation service stops receiving traffic entirely. The homepage immediately returns cached top genres as a fallback instead.\n\n<b>3. The result</b>\nThe recommendation service gets breathing room to recover. The homepage stays up showing cached data. Thread pool never exhausts. AWS SDK uses the same pattern — DynamoDB throttled requests get retried after 1s, 2s, 4s with random jitter.",
    interviewTip:
      "\"When they ask you to design for resilience in a microservices system, pair circuit breakers with fallbacks and retries with idempotency.\"\n• <b>1. Circuit breaker + fallback</b> — \"if the recommendation circuit is open, return the user's most-watched genres from cache\"\n• <b>2. Exponential backoff formula</b> — `wait = base × 2^attempt`: 1s, 2s, 4s, 8s; add jitter to desync clients\n• <b>3. Retry budget</b> — cap at 3 retries maximum to prevent infinite loops that compound tail latency\n\nFollow up: interviewers often ask about the thundering herd problem — jitter is the specific fix; without it all retried requests arrive at the same millisecond.",
    tags: ["Circuit Breaker", "Retry", "Exponential Backoff", "Jitter", "Thundering Herd", "Resilience", "Fallback"],
  },
  {
    id: 29,
    section: "Advanced Topics",
    title: "Microservices",
    tagline: "Decompose applications into independent deployable services",
    description:
      "Microservices split an application into small, independently deployable services — each owning its own data and business logic, communicating with others via APIs or events.\n\n<b>The core principle</b>\nEach service owns its domain end-to-end: its own database, its own deployment pipeline, its own team. A bug in the payments service only requires redeploying the payments service — not the entire app.\n\n<b>Key infrastructure patterns</b>\n• <b>API Gateway</b> — single entry point for all clients; handles auth, routing, and rate limiting before requests reach any service\n• <b>Service Discovery</b> — services register their IP/port dynamically; clients look up addresses instead of hardcoding them (services restart and change addresses frequently)\n• <b>Service Mesh</b> — infrastructure layer (e.g. Istio) that handles inter-service TLS, retries, circuit breaking, and observability without changing application code\n\n<b>The real tradeoffs</b>\n• Each inter-service call is now a network call — latency adds up across a chain of 5 services\n• Data consistency across service boundaries requires distributed transactions or eventual consistency\n• Debugging a bug that spans 3 services requires distributed tracing (Jaeger, Zipkin) — you can't just read one log file\n\n<b>Start with a modular monolith</b>\nExtract services only when you have clear domain boundaries, independent scaling needs, or separate team ownership. Premature microservices create distributed system complexity without the benefits.",
    whyItMatters:
      "Monoliths become deployment bottlenecks at scale — a one-line change in payments requires redeploying and re-testing the entire app. Microservices let teams ship independently and scale only what's under load.\n\nIf you choose wrong:\n• Microservices too early: you get distributed system complexity (network calls, tracing, consistency) without enough scale to justify it\n• Shared databases across services: two services coupling through the same DB tables defeats the whole purpose\n• No distributed tracing: a request fails somewhere across 5 services and you have no idea which one\n\nThis concept connects to API Gateway, Service Mesh, circuit breakers, and event-driven architecture.",
    diagramNote:
      "Fig. 29.1 — Microservices communicate synchronously via gRPC and asynchronously via Kafka; each service owns its own database.",
    example:
      "<b>1. Setup</b>\nNetflix has over 700 microservices: search, recommendations, billing, streaming, device management, and hundreds more. Each team owns their service completely.\n\n<b>2. What happens</b>\nThe recommendations team deploys a new model 15 times today. The billing team deploys a tax fix. Neither team coordinates with the other or risks breaking each other's service.\n\n<b>3. The result</b>\nNetflix deploys thousands of times per day across all services. Each team moves at their own pace. Streaming service can be scaled to 10x capacity during a major release without touching billing or search.",
    interviewTip:
      "\"When they ask you to design a large system, don't jump to microservices — explain when you'd start with a monolith and when you'd decompose.\"\n• <b>1. Start with a modular monolith</b> — clean internal module boundaries, easier to operate and debug early on\n• <b>2. Extract services when</b> — clear domain boundaries exist, independent scaling is needed, or teams need to ship without coordinating\n• <b>3. Essential tooling</b> — API Gateway for routing, Service Discovery for dynamic addresses, distributed tracing (Jaeger/Zipkin) for debugging across services\n\nFollow up: interviewers often ask how you debug in a microservices system — the answer is a correlation ID propagated through every service and aggregated logs/traces in a tool like Datadog or Jaeger.",
    tags: ["Microservices", "API Gateway", "Service Mesh", "gRPC", "Domain-Driven Design", "Distributed Tracing"],
  },
  {
    id: 30,
    section: "Advanced Topics",
    title: "Pagination",
    tagline: "Loads large result sets in small chunks",
    description:
      "Pagination splits large query results into pages so your server never loads millions of rows into memory at once — and your clients never receive a response too large to process.\n\n<b>Offset pagination</b>\nUses `LIMIT 20 OFFSET 100` — skip the first 100 rows, return the next 20. Simple to implement and lets users jump to \"page 5\" directly.\n\nThe problem: at large depth (page 5,000) the database still scans and discards all 100,000 preceding rows before returning your 20. That's O(n) — it gets slower as users go deeper. Also: if a row is inserted or deleted between page loads, rows shift and users see duplicates or skipped entries.\n\n<b>Cursor-based pagination</b>\nUses `WHERE id > last_seen_id LIMIT 20` — the database jumps directly to the cursor position using an index. O(1) regardless of how deep you are. No duplicates or skips when rows are inserted between pages.\n\n  ↳ cursors must be opaque to clients (base64 encoded) so the server can change internal implementation without breaking clients\n\n<b>When to use each</b>\n• <b>Cursor-based</b> — feeds, timelines, infinite scroll (Twitter, Instagram, TikTok)\n• <b>Offset-based</b> — admin dashboards with small datasets where users need to jump to a specific page number\n\n<b>Always set a max page size</b>\nWithout a cap, a client can request `LIMIT 1000000` and crash your server with one API call.",
    whyItMatters:
      "Returning all records in a single response is one of the most common ways to crash a server under load. Every API that returns a list needs pagination from day one — retrofitting it onto an existing API breaks all existing clients.\n\nIf you choose wrong:\n• Offset pagination on a billion-row feed: page 5,000 takes seconds because the DB scans 100,000 rows to discard\n• No max page size: a client requests 1 million rows in one call and your server runs out of memory\n• Exposing internal cursor format: you can never change the DB schema without breaking clients\n\nThis concept connects to database indexing, API design, and query optimization.",
    diagramNote:
      "Fig. 30.1 — Offset pagination scans to the page boundary on every request; cursor pagination jumps directly to the last seen ID.",
    example:
      "<b>1. Setup</b>\nTwitter's timeline API needs to serve paginated tweets. Users scroll infinitely. The tweets table has billions of rows.\n\n<b>2. Offset approach (what NOT to do)</b>\nPage 50,000 of a timeline: `SELECT * FROM tweets OFFSET 1000000 LIMIT 20`. The database scans and discards 1 million rows. Response time: several seconds. Users see duplicates when new tweets are inserted.\n\n<b>3. Cursor approach (the right way)</b>\nThe API returns `next_cursor: \"dXNlcjoxMjM0\"` (base64 encoded). The next request: `WHERE tweet_id < [decoded_cursor] LIMIT 20`. Database uses the index, jumps directly to the position. Response time: under 10ms at any depth.",
    interviewTip:
      "\"When they ask you to design a feed or timeline API, immediately default to cursor-based pagination and explain why offset breaks at scale.\"\n• <b>1. Cursor = O(1) at any depth</b> — jumps directly to position using an index; offset scans from the beginning every time\n• <b>2. Opaque cursors</b> — base64 encode the cursor value; clients never parse it; you can change internals freely\n• <b>3. Max page size</b> — always cap page size (20–100 is typical); document it in the API\n\nFollow up: interviewers ask about cursor-based pagination with sorting by something other than ID (e.g. `created_at`) — you need a composite cursor `(created_at, id)` to handle ties when timestamps collide.",
    tags: ["Cursor", "Offset", "Infinite Scroll", "API Design", "Keyset Pagination"],
  },
  {
    id: 31,
    section: "Advanced Topics",
    title: "Distributed Locking",
    tagline: "Prevents concurrent conflicts across servers",
    description:
      "A distributed lock ensures that only one server can perform a critical operation at a time — even when 10 servers are all trying to do the same thing simultaneously.\n\n<b>The problem without locks</b>\nTwo servers both check inventory: item has 1 unit left. Both see 1. Both decrement. Inventory goes to -1. You've oversold. In a single-process app, a mutex handles this. In a distributed system with 10 servers, you need a lock that all servers respect.\n\n<b>Redis SETNX — the simple approach</b>\n`SETNX` (SET if Not eXists) atomically sets a key only if it doesn't exist. Server 1 runs `SETNX lock:item:42 server1 PX 5000` — it succeeds (gets the lock). Server 2 runs the same command — it fails (key already exists, lock is taken). Server 1 releases by deleting the key when done.\n\n  ↳ always set a TTL (`PX 5000` = 5 seconds); if the lock holder crashes, the lock auto-expires instead of deadlocking forever\n\n<b>Redlock — for higher fault tolerance</b>\nAcquire the lock from a majority of N independent Redis nodes (e.g. 3 of 5). A single Redis node going down doesn't break the locking system. Used when the simple single-node approach is too risky.\n\n<b>Fencing tokens — for extra safety</b>\nEach lock acquisition returns a monotonically increasing version number. The downstream system (database, storage) rejects any write from a token older than the last accepted one — protecting against stale lock holders that wake up late and try to write after their lock expired.",
    whyItMatters:
      "Race conditions in distributed systems are silent bugs — they only appear under concurrent load in production. Two servers winning the same auction, overselling the last item in inventory, or running the same cron job twice are all real consequences of missing distributed locks.\n\nIf you choose wrong:\n• No TTL on the lock: the lock holder crashes; the lock never releases; the system deadlocks permanently\n• TTL too short: the operation takes longer than expected; the lock expires while work is in progress; a second server acquires it and both run simultaneously\n• Single Redis node for Redlock: that node goes down during an acquisition; the lock system breaks\n\nThis concept connects to Redis, race conditions, and optimistic concurrency.",
    diagramNote:
      "Fig. 31.1 — Redis distributed lock: SETNX grants the lock to the first requester; all others are denied until the key is deleted or its TTL expires.",
    example:
      "<b>1. Setup</b>\nAn e-commerce site has 10 servers. A flash sale starts — 10,000 users simultaneously try to buy the last unit of a product.\n\n<b>2. What happens</b>\nEvery server races to acquire a Redis lock: `SETNX lock:item:42 server3 PX 5000`. Only Server 3 succeeds — all 9 others get back `0` (key exists, lock taken). Server 3 decrements inventory, completes the purchase, and runs `DEL lock:item:42`.\n\n<b>3. The result</b>\nExactly one purchase goes through. If Server 3 crashes mid-decrement, the `PX 5000` TTL expires in 5 seconds and the lock auto-releases — no permanent deadlock.",
    interviewTip:
      "\"When they ask you to prevent race conditions in a distributed system, walk through the Redis lock pattern and then mention fencing tokens.\"\n• <b>1. TTL sizing</b> — long enough for the operation to complete; short enough to auto-recover if the lock holder crashes\n• <b>2. Fencing tokens</b> — monotonically increasing version numbers; the DB rejects writes from stale token holders even if their lock already expired\n• <b>3. Alternative</b> — optimistic concurrency (compare-and-swap) is simpler when conflicts are rare; use locks only when conflicts are frequent\n\nFollow up: interviewers ask why TTL alone is not enough — fencing tokens handle the case where a paused process wakes up after its TTL expired, still believing it holds the lock.",
    tags: ["Redis", "SETNX", "Redlock", "Race Condition", "Fencing Token", "Deadlock", "TTL"],
  },
  {
    id: 32,
    section: "Advanced Topics",
    title: "Consistency vs Availability",
    tagline: "The core trade-off in every distributed system",
    description:
      "When a network partition (connectivity loss between nodes) occurs in a distributed system, you must choose one of two behaviors — you cannot have both.\n\n<b>Consistency (CP)</b>\nEvery read returns the most recent write, or the system returns an error. The system refuses to serve stale data even if it means some requests fail.\n  ↳ CP systems: ZooKeeper, etcd, HBase, traditional PostgreSQL with synchronous replication\n  ↳ behavior during partition: block or return errors until connectivity is restored\n\n<b>Availability (AP)</b>\nEvery request gets a response — but that response might contain stale data. The system never returns an error, even if it can't guarantee freshness.\n  ↳ AP systems: Cassandra, DynamoDB, CouchDB\n  ↳ behavior during partition: keep serving with eventually consistent data\n\n<b>The practical insight</b>\nYou don't choose once for an entire system — you choose per feature. The same application can use a CP store for payments and an AP store for activity feeds.\n  ↳ needs strong consistency: bank balances, inventory counts, payment transactions\n  ↳ tolerates eventual consistency: social feeds, analytics, notification counts, view counters\n\n<b>What CAP theorem actually says</b>\nDuring a network partition, you must sacrifice either consistency or availability. Partition tolerance is not optional — networks fail.",
    whyItMatters:
      "This trade-off determines whether your system gives wrong answers or gives no answers during a failure. Getting it wrong in either direction is expensive.\n\nIf you choose wrong:\n• Strong consistency everywhere: a network blip causes your entire app to return errors; availability tanks\n• Eventual consistency everywhere: a user transfers money, sees the old balance for 2 seconds, transfers again — double spend\n• Not thinking per feature: one wrong default choice breaks either correctness or uptime across the system\n\nThis concept connects to database selection, replication, and distributed transactions.",
    diagramNote:
      "Fig. 32.1 — During a network partition, CP systems block to preserve consistency while AP systems serve potentially stale data.",
    example:
      "<b>1. AP — posting a tweet</b>\nWhen you post a tweet, it is fine if followers see it 200ms later. Twitter uses an AP approach for the feed — availability matters more than perfect freshness. The tweet will eventually propagate to all nodes.\n\n<b>2. CP — transferring money</b>\nWhen you transfer $500, your balance must be immediately correct on all nodes. An AP system could show the old balance momentarily — allowing a second transfer of the same $500. Banking systems use CP: the transaction either succeeds on all nodes or fails entirely.\n\n<b>3. The result</b>\nBoth decisions are correct — chosen deliberately per feature based on what \"wrong\" looks like for that feature.",
    interviewTip:
      "\"When they ask you to choose a database, frame it around the consistency vs availability trade-off for that specific feature.\"\n• <b>1. CP framing</b> — \"I'll use PostgreSQL with synchronous replication here because transfers require strong consistency — stale data means double spend\"\n• <b>2. AP framing</b> — \"I'll use Cassandra for the activity feed — availability matters more than perfect freshness; a 200ms delay in feed updates is acceptable\"\n• <b>3. Per-feature tuning</b> — the same system can be CP for one operation and AP for another; this shows architectural maturity\n\nFollow up: interviewers ask about PACELC — an extension to CAP that also considers latency vs consistency tradeoffs even when there is no partition.",
    tags: ["CAP Theorem", "Eventual Consistency", "Strong Consistency", "PACELC", "CP", "AP"],
  },
  {
    id: 33,
    section: "Advanced Topics",
    title: "Retry & Backoff",
    tagline: "Handles transient failures without creating retry storms",
    description:
      "Retries automatically re-attempt failed requests to recover from transient failures — but without a backoff strategy, retries can make a partial outage into a total one.\n\n<b>The thundering herd problem</b>\nA service briefly goes down. 10,000 clients all get an error at the same moment. Without backoff, all 10,000 immediately retry — the recovering service gets slammed before it can stabilize. The spike causes it to fail again. Retry loops forever.\n\n<b>Exponential backoff formula</b>\n`wait = base × 2^attempt`: 1s, 2s, 4s, 8s, 16s. Each retry waits twice as long as the previous one, giving the downstream service time to recover.\n\n<b>Jitter — the critical addition</b>\nWithout jitter, all 10,000 clients wait exactly 1 second and retry simultaneously. You still get a synchronized spike. Jitter adds randomness: each client waits `random(0, wait)` milliseconds, staggering retries across a window instead of spiking at one moment.\n\n<b>What to retry vs not retry</b>\n• Retry: `503` (service unavailable), `429` (rate limited), network timeout — transient failures that may succeed on retry\n• Never retry: `400` (bad request), `404` (not found) — permanent failures; retrying wastes resources and never succeeds\n\n<b>Retry budget</b>\nCap total retries (e.g. 3 maximum) and cap total time spent retrying. Uncapped retries across a chain of 5 services compound tail latency — 3 retries per service × 5 services = up to 15x longer response time.",
    whyItMatters:
      "Without retries, any transient network blip causes a user-visible error for no good reason. Without backoff, retries make failures dramatically worse by preventing recovery. Exponential backoff with jitter is the standard used by every major cloud SDK.\n\nIf you choose wrong:\n• No backoff: immediate retries form a thundering herd; recovering service gets hammered and fails again\n• No jitter: all clients wake up simultaneously anyway; you get a synchronized spike at every backoff interval\n• Retrying a 400 error: the request has a bug and will never succeed; you waste resources on guaranteed failures\n\nThis concept connects to circuit breakers, idempotency, and distributed system resilience.",
    diagramNote:
      "Fig. 33.1 — Exponential backoff doubles the wait time between retries; jitter staggers client retry times to prevent a synchronized thundering herd.",
    example:
      "<b>1. Setup</b>\nAWS Lambda functions call a DynamoDB table. The table gets throttled — it's over capacity and returning `429` errors.\n\n<b>2. Without jitter</b>\n500 Lambda functions all get `429` at the same moment. All wait exactly 1 second (exponential backoff without jitter). All 500 retry at exactly t=1s. DynamoDB gets a spike of 500 simultaneous requests again. Throttling continues.\n\n<b>3. With exponential backoff + jitter (what AWS SDK does by default)</b>\nEach function waits `random(0, 1000)` ms before the first retry, `random(0, 2000)` ms before the second. The 500 requests spread across a 2-second window. DynamoDB gets ~250 requests/second — manageable. It stabilizes and all functions eventually succeed.",
    interviewTip:
      "\"When they ask about handling failures in a distributed system, connect retry to both backoff and idempotency — they're a package deal.\"\n• <b>1. The formula</b> — `wait = base × 2^attempt` with `random(0, wait)` jitter; 1s, 2s, 4s, 8s staggered\n• <b>2. Idempotency requirement</b> — retrying a non-idempotent `POST` creates double charges, double emails; always pair retry with idempotent operations\n• <b>3. Circuit breaker integration</b> — stop retrying when the circuit is open; the downstream is clearly down, not just hiccuping\n\nFollow up: interviewers ask about retry budgets in a microservice chain — if each of 5 services retries 3 times, total latency can compound to 15x; a global retry budget caps this.",
    tags: ["Exponential Backoff", "Jitter", "Thundering Herd", "Retry Budget", "Transient Failure", "Resilience"],
  },
  {
    id: 43,
    section: "Advanced Topics",
    title: "Distributed Unique ID Generation",
    tagline: "Generating unique IDs at scale without a central bottleneck",
    description:
      "Every distributed system needs unique IDs for records, events, and messages — and the approach you choose determines whether ID generation itself becomes a bottleneck.\n\n<b>Option 1: Auto-increment (database sequence)</b>\n• Simple, human-readable, sequential\n• Problem: a single database handles all ID generation — becomes a write bottleneck when you have multiple DB shards or high write volume\n\n<b>Option 2: UUID v4 (random)</b>\n• 128-bit random identifier, globally unique, no coordination between machines\n• Problem: completely random — NOT sortable by time. When used as a B-tree primary key, random insertions fragment the index (each new row inserts at a random position) causing slow writes and wasted disk space\n\n<b>Option 3: Snowflake ID (the standard answer)</b>\nTwitter's solution — a 64-bit integer packed with three components:\n• `[1 bit sign][41 bits timestamp ms][10 bits machine ID][12 bits sequence]`\n  ↳ 41-bit timestamp: milliseconds since a custom epoch (gives ~69 years of IDs)\n  ↳ 10-bit machine ID: assigned at worker startup (from ZooKeeper or config); supports 1,024 workers\n  ↳ 12-bit sequence: per-machine counter that resets every millisecond; allows 4,096 IDs per ms per machine\n\nTotal capacity: ~4 million unique IDs per millisecond across all machines. Chronologically sortable because the timestamp occupies the most significant bits — a higher ID always means a later event.\n\n<b>Modern alternative: ULID</b>\n128-bit, URL-safe, lexicographically sortable — a cleaner alternative to Snowflake without the bit-packing complexity.",
    whyItMatters:
      "ID generation is a hidden bottleneck that only surfaces at scale. A single auto-increment sequence serializes all writes across a distributed system. UUID v4 breaks database index performance at high write volumes.\n\nIf you choose wrong:\n• Auto-increment at scale: every write waits for the single ID-generator sequence; it becomes the global write bottleneck\n• UUID v4 as primary key: random insertions into a B-tree cause page splits, index fragmentation, and slow writes\n• No machine ID coordination: two workers generate the same ID at the same millisecond — catastrophic data collision\n\nThis concept connects to database indexing, sharding, and distributed coordination.",
    diagramNote:
      "Fig. 43.1 — Snowflake ID structure: 41-bit timestamp prefix ensures chronological sort order across 4,096 IDs per millisecond per worker.",
    example:
      "<b>1. Setup</b>\nTwitter originally used MySQL auto-increment for tweet IDs. As write volume grew, the single MySQL sequence became a global bottleneck — every tweet write waited for the sequence to increment.\n\n<b>2. Snowflake solution</b>\nTwitter built Snowflake — a lightweight service running on multiple machines. Each machine gets a unique 10-bit ID at startup. Each generates IDs from its own timestamp + machine ID + sequence counter. No coordination needed between machines during generation.\n\n<b>3. The result</b>\nDiscord uses a Snowflake variant for message IDs. You can extract the exact creation timestamp of any Discord message directly from the ID — the timestamp bits are right there. Sortable, unique, no central bottleneck, 4 million IDs per millisecond per machine.",
    interviewTip:
      "\"When a system design needs IDs, immediately go to Snowflake and walk through the bit structure — interviewers test this specifically.\"\n• <b>1. The bit breakdown</b> — 41-bit timestamp + 10-bit machine ID + 12-bit sequence = 64-bit integer; ~4M IDs/ms/machine\n• <b>2. Why sortable</b> — timestamp is the most significant bits; a higher numeric ID always means a later timestamp\n• <b>3. Machine ID assignment</b> — ZooKeeper or a startup config assigns each worker a unique 10-bit ID; this is the only coordination needed\n\nFollow up: interviewers ask \"why not UUID v4?\" — it's not sortable and random insertions fragment B-tree indexes, slowing write performance significantly at scale.",
    tags: ["Snowflake", "UUID", "Distributed ID", "ULID", "Auto-increment", "Twitter", "Timestamp", "Unique ID"],
  },

  // ─── FOUNDATION GAPS ──────────────────────────────────────────
  {
    id: 34,
    section: "Foundation",
    title: "Latency & Throughput",
    tagline: "The two core performance metrics every engineer must know",
    description:
      "Two of the most important performance metrics in system design are latency and throughput.\n\nLatency is the time it takes to complete a single request, usually measured in milliseconds (ms). Lower latency means users get responses faster.\n\nThroughput is the number of requests a system can handle in a given period, usually measured in requests per second (req/s). Higher throughput means the system can process more work.\n\nAlthough related, they measure different things. A system can have high throughput but high latency, or low latency but limited throughput. A batch-processing system might process 10,000 items per second (high throughput), but each individual item may take 2 seconds to complete (high latency). A real-time API might respond in 50ms (low latency) but handle fewer requests overall unless more servers are added.\n\nAs traffic increases, throughput usually increases as well — up to a point. Eventually the system reaches its capacity limit. Beyond that point, it cannot process requests fast enough, and latency starts increasing rapidly. This sudden spike in latency is often the first sign that a system is becoming overloaded.\n\nWhen measuring performance, don't rely only on average latency. Instead, pay attention to percentiles — especially P99. P50 latency means 50% of requests are faster than that value. P99 latency means 99% of requests are faster than that value. If P50 = 50ms but P99 = 8 seconds, 1 out of every 100 users waits 8 seconds. An average of 100ms would completely hide this problem. That's why engineers monitor tail latency (P95, P99, P99.9) instead of averages.\n\nAs a general rule: CDN edge response under 10ms, cache hit under 1ms, web API response under 200ms, database query under 50ms.\n\nThe key takeaway: throughput tells you how much work a system can do, while latency tells you how fast it responds. A good system balances both and keeps latency low even as traffic grows.",
    whyItMatters:
      "Every performance conversation in interviews uses these two terms. Knowing the difference tells you which problem you're solving: 'system is slow for each user' (latency) vs 'system can't handle enough users at once' (throughput). They require different fixes.",
    diagramNote:
      "Fig. 34.1 — Latency and throughput diverge under load: throughput plateaus at capacity while latency spikes as the queue grows.",
    example:
      "Google Search: P99 latency < 200ms while handling ~99,000 req/s throughput. Redis: < 1ms latency at 1M+ ops/sec throughput because everything stays in RAM. A slow DB query (high latency) doesn't necessarily mean low throughput — if you run 1,000 queries in parallel, throughput stays high even though each individual query is slow.",
    interviewTip:
      "State both metrics when asked about performance targets: 'I'll target P99 latency under 200ms at 10,000 req/s throughput.' Always say P99, never average. When a system is 'slow', ask yourself: is every request slow (latency problem → check DB query, add index, add cache)? Or is it fine for one user but breaks under load (throughput problem → add servers, load balancer, scale horizontally)?",
    tags: ["Latency", "Throughput", "P99", "P50", "Performance", "Capacity", "Tail Latency"],
  },
  {
    id: 35,
    section: "Foundation",
    title: "Capacity Estimation",
    tagline: "Back-of-envelope math that every interview requires",
    description:
      "Before designing any system, the first step is to estimate its scale. These estimates help you choose the right architecture, databases, caches, and infrastructure.\n\nA common approach is to start with Daily Active Users (DAU) and work through this chain: DAU → Requests per Day → Requests per Second (RPS) → Storage → Bandwidth.\n\nThe most important formula to remember is: RPS = Requests per Day ÷ 86,400. There are 86,400 seconds in a day, so dividing total daily requests by that gives the average requests per second. For example, 1 million DAU making 10 requests each = 10 million requests/day ≈ 116 RPS. 100 million DAU making 10 requests each = 1 billion requests/day ≈ 12,000 RPS.\n\nStorage estimation follows the same logic. 1 million users × 1 KB profile data = 1 GB. 1 million photos per day × 200 KB each = 200 GB/day.\n\nTraffic is rarely evenly distributed. Events like Black Friday, product launches, or viral content cause sudden spikes. A common rule of thumb is: Peak Traffic ≈ Average Traffic × 3.\n\nCaching is another key consideration. In most systems, a small portion of the data receives most of the traffic. Following the 80/20 rule, roughly 20% of the data generates 80% of the requests. A reasonable starting estimate for cache size is 20% of your active daily data.\n\nTo estimate servers needed: Servers = Peak RPS ÷ Single Server Capacity. For example, if peak traffic is 15,000 RPS and one server handles 3,000 RPS, you need 5 servers.\n\nIn interviews and real-world discussions, the goal is not perfect accuracy. The goal is to make reasonable assumptions and quickly estimate the order of magnitude. Always state your assumptions clearly, round numbers aggressively, and focus on understanding the scale rather than performing exact calculations.\n\nThe key takeaway: good system design starts with estimating traffic, storage, and growth. Once you know the scale, architectural decisions become much easier.",
    whyItMatters:
      "Capacity estimation drives every architecture decision. '12,000 req/s means one server is not enough — I need a load balancer and at least 6 servers.' Without estimation, your design has no foundation. Interviewers use it to see if you think at production scale.",
    diagramNote:
      "Fig. 35.1 — Back-of-envelope estimation: chain DAU through request rate to server count, then independently estimate daily and yearly storage.",
    example:
      "Twitter estimation: 300M DAU × 20 tweet-reads/day = 6B reads/day = 70,000 read req/s. Writes: 100M tweets/day × 280 bytes = 28GB text/day. Images: 10% of tweets have images × 200KB = 2TB/day image storage. Conclusion: read replicas are essential (70K read req/s), separate object storage for images (2TB/day can't go in SQL), cache the timeline heavily (same tweets read by millions).",
    interviewTip:
      "Use these 5 steps every interview: (1) State DAU. (2) Calculate req/s = DAU × requests per user ÷ 86,400. (3) Storage/day = req/s × data per request × 86,400. (4) Peak = average × 3. (5) Servers = peak req/s ÷ 1,000. Say all math out loud: 'If 1M DAU each make 10 requests, that's 10M per day, divide by 86,400 seconds, roughly 116 req/s, peak of about 350.' Rounding to nice numbers shows comfort with estimation.",
    tags: ["Capacity Estimation", "DAU", "RPS", "Back-of-Envelope", "Storage", "Throughput", "Scalability"],
  },

  // ─── DATA LAYER GAP ───────────────────────────────────────────
  {
    id: 36,
    section: "Data Layer",
    title: "Cache-aside Pattern",
    tagline: "The most common cache pattern — the app controls the cache",
    description:
      "Cache-aside (also called lazy loading) is the most widely used caching pattern. The application code is in charge of all cache interactions — the cache is not automatically in the data path.\n\n<b>Read Flow</b>\n\n1. The app checks the cache first\n2. `Cache HIT` — data is found → return it immediately, no DB query needed\n3. `Cache MISS` — data is not in cache → query the database\n4. Store the result in cache with a TTL (expiry time)\n5. Return the result to the caller\n\nThe key idea: data only enters the cache when it is actually requested. Cold data (never requested) never fills up the cache.\n\n<b>Write Flow</b>\n\nWhen data is updated:\n\n1. Write the new data to the database\n2. Delete (invalidate) the corresponding cache key\n3. The next read will re-populate the cache with fresh data\n\nWhy delete instead of update? Deleting is simpler and avoids the risk of writing stale computed values into the cache.\n\n<b>How it differs from Read-Through and Write-Through</b>\n\n• `Read-Through` — the cache layer itself fetches from the DB on a miss. The app only ever talks to the cache, never directly to the DB.\n• `Write-Through` — every write goes to the cache and the DB together, automatically.\n• `Cache-aside` — the application code explicitly handles all cache reads and writes. More flexible, but requires more code.\n\n<b>Why cache-aside is preferred</b>\n\n• You can cache any computed result — not just raw DB rows\n• You can combine multiple DB queries into a single cache entry\n• You have precise control over what gets cached and for how long\n• Works even when the cache and DB cannot be directly connected",
    note:
      "Cache-aside is your default answer when someone asks how you would add caching to a system. The app checks cache first, falls back to DB on miss, then populates the cache.\n\nOn writes: update the DB, then delete the cache key. Simple and reliable.",
    whyItMatters:
      "Cache-aside is the most widely used caching pattern in production. It's simple, explicit, and puts the app in control. It's what most engineers mean when they say 'add Redis caching.' The curriculum names it specifically because interviewers ask for it by name.",
    diagramNote:
      "Fig. 36.1 — Cache-aside read path: check cache first, fall back to DB on miss, populate cache; write path updates DB and invalidates the cache key.",
    example:
      "A product page: first user requests product #42 — cache miss, app fetches from DB, stores in Redis with 5-min TTL. Next 10,000 users hit Redis — zero DB queries. When product price changes, app updates DB then deletes the Redis key. Next request re-fetches fresh price from DB and re-populates cache. This delete-on-write approach is simpler than update-on-write and avoids stale data.",
    interviewTip:
      "Cache-aside is your default answer when asked about caching strategy. Say: 'I'd use cache-aside — on miss, the application fetches from DB and populates Redis with a TTL. On writes, I'd invalidate the cache key so the next read gets fresh data.' Distinguish it from read-through by saying the app (not the cache infrastructure) is responsible for loading data. This distinction matters for systems where cache and DB can't be directly connected.",
    tags: ["Cache-aside", "Lazy Loading", "Cache Invalidation", "Redis", "Cache Strategy", "Read-Through"],
  },

  // ─── ADVANCED TOPICS GAPS ─────────────────────────────────────
  {
    id: 37,
    section: "Advanced Topics",
    title: "Reverse Proxy",
    tagline: "The gatekeeper that sits in front of your backend",
    description:
      "A reverse proxy is a server that sits in front of your backend and handles every client request before it reaches your application code.\n\n<b>What a Reverse Proxy Does</b>\n• Accepts all incoming client requests at a single address\n• Forwards each request to the appropriate backend server\n• Returns the backend's response to the client\n  ↳ The client never knows backend servers exist — it only ever talks to the proxy\n\n<b>Core Responsibilities</b>\n• <b>SSL/TLS termination</b> — the proxy manages HTTPS so backend servers run plain HTTP internally\n  ↳ No certificate management on every backend server; they never touch the public internet\n• <b>Request routing</b> — send `/api/*` to app servers, `/static/*` to object storage\n• <b>Response compression</b> — gzip responses before sending to the client\n• <b>Caching</b> — serve cached responses without hitting the backend at all\n• <b>Rate limiting</b> — cap requests per IP or per API key before they reach your service\n• <b>Authentication</b> — validate tokens at the edge; backends trust that the proxy already checked\n\n<b>Reverse Proxy vs Load Balancer</b>\n• A load balancer is a specialized reverse proxy focused on distributing traffic across identical servers\n• A reverse proxy has broader responsibilities: routing, caching, auth, compression\n  ↳ Every load balancer is a reverse proxy; not every reverse proxy is a load balancer\n\n<b>Common Tools</b>\n• `NGINX` and `HAProxy` are the most widely used reverse proxies\n• In microservices, the API Gateway is the reverse proxy that routes to specific services",
    whyItMatters:
      "A reverse proxy is the single entry point to your system — every request passes through it before touching any backend. Without it, backend servers are exposed directly to the internet, each client must know which server to call, SSL must be configured on every server, and you have no central place to enforce auth, rate limiting, or caching.\n\nIf you choose wrong:\n• No reverse proxy — backend servers are public; one compromised service exposes all others\n• SSL on every backend — certificate management multiplied by every server in your fleet\n• No central rate limiting — each service must implement its own; any gap means an exposed endpoint\n• No caching layer — static assets hit the backend on every request, wasting compute\n\nThe reverse proxy pattern connects directly to load balancing, API Gateway design, CDN integration, and SSL termination.",
    diagramNote:
      "Fig. 37.1 — Reverse proxy sits in front of servers (SSL termination, routing, rate limiting); forward proxy sits in front of clients (VPN, anonymity).",
    example:
      "NGINX as reverse proxy at a mid-size startup.\n\n<b>1. Setup</b>\n• All traffic hits NGINX on port 443 (HTTPS)\n• NGINX holds the SSL certificate and terminates the TLS handshake\n• Backend Node.js servers run on port 8080 over plain HTTP — they never see a certificate\n\n<b>2. What happens</b>\n• A request to `/api/users` is forwarded to the Node.js cluster\n• A request to `/static/logo.png` is served from NGINX's disk cache — the app server is never touched\n• Rate limiting is applied: IPs sending more than 100 requests per minute are blocked at NGINX before reaching the app\n\n<b>3. The result</b>\n• Backend servers are invisible to the internet\n• Static asset serving costs zero app server compute\n• SSL, caching, and rate limiting are configured in one place — not duplicated across 8 Node.js instances",
    interviewTip:
      "\"When they ask you to design any web system, start by placing a reverse proxy as the first layer — it's where SSL terminates, routing happens, and cross-cutting concerns live.\"\n\n• <b>1. Distinguish from load balancer</b> — a load balancer is a reverse proxy specialized for traffic distribution; a full reverse proxy also handles SSL termination, routing, caching, and auth\n• <b>2. SSL termination</b> — reverse proxy holds the certificate; backends run plain HTTP internally\n• <b>3. Routing</b> — `/api/*` → app servers, `/static/*` → object storage or disk\n• <b>4. Microservices</b> — in a microservices context, call it an API Gateway — same concept, named for its role routing to many different services\n\nA common follow-up is \"what's the difference between a reverse proxy and a forward proxy?\" — answer: a reverse proxy sits in front of servers (clients don't know backend IPs exist); a forward proxy sits in front of clients (servers don't know the real client IPs, used for VPNs and anonymity).",
    tags: ["Reverse Proxy", "NGINX", "SSL Termination", "API Gateway", "HAProxy", "Load Balancer"],
  },
  {
    id: 38,
    section: "Advanced Topics",
    title: "Object Storage",
    tagline: "Where files, images, and videos actually live at scale",
    description:
      "Object storage is a flat key-value store for binary data — the right place to store images, videos, audio, documents, and any other file at scale.\n\n<b>What Object Storage Is</b>\n• Services like Amazon S3, Google Cloud Storage (`GCS`), and Azure Blob Storage store binary data as objects\n• Each object has three parts: a key (the path/filename), the data (raw bytes), and metadata\n• Unlike a SQL database (rows and columns) or a file system (folder hierarchy), object storage is a flat namespace\n  ↳ Scales to trillions of objects — no relational DB can do that with binary blobs\n\n<b>The Standard Architecture</b>\n• Your app stores the object URL in your database: `photo_url TEXT NOT NULL`\n• The actual bytes go to S3 — never into the database\n• Reads go: client → CDN edge → (on cache miss) → S3 origin\n• Uploads go: client → your app → S3 directly\n\n<b>Pre-Signed URLs for Private Files</b>\n• Never make an S3 bucket public for private content (medical records, DM attachments, paid downloads)\n• A pre-signed URL is a temporary access link signed with your credentials\n  ↳ The URL includes an expiry: `https://s3.amazonaws.com/...?Expires=1700000000&Signature=...`\n  ↳ After N minutes (typically 15), the URL stops working — no permanent exposure\n• The file stays private in S3; the signed URL grants one-time access\n\n<b>Why Not Store Files in SQL</b>\n• A 2MB image in a SQL row makes that table 10,000× larger than needed\n• Full table scans and index operations slow down for every query — not just image queries\n• CDNs cannot cache SQL column data — they can cache S3 URLs directly\n• Backups, replication, and migrations all become slower",
    whyItMatters:
      "Any system with user-uploaded content — photos, videos, documents, backups — requires object storage. Saying \"store images in the database\" in an interview is the most common red flag for senior engineering roles. Object storage is the correct answer any time binary data appears in a design.\n\nIf you choose wrong:\n• Binary blobs in SQL — table sizes explode, every query slows, CDN integration is impossible\n• Public S3 bucket for private content — all files are accessible to anyone with the URL forever\n• App servers as upload middlemen at scale — 4GB video uploads tie up all your web workers\n• No CDN in front of S3 — every image read hits S3 origin, adding latency and cost\n\nObject storage connects directly to CDN design, pre-signed URL security, and database schema decisions.",
    diagramNote:
      "Fig. 38.1 — Blob storage pattern: the DB stores only the URL; S3 stores the bytes; the CDN caches bytes at the edge for fast delivery.",
    example:
      "Instagram photo upload.\n\n<b>1. Setup</b>\n• PostgreSQL stores post metadata: `post_id`, `user_id`, `caption`, `photo_url TEXT`\n• S3 stores the actual image bytes at path `users/{user_id}/{photo_id}.jpg`\n• CloudFront CDN sits in front of S3\n\n<b>2. What happens</b>\n• User posts a photo → upload service generates a unique photo ID → image bytes go directly to S3\n• The S3 URL is written into the PostgreSQL row alongside the caption\n• CloudFront caches the image at 200+ edge locations worldwide\n\n<b>3. The result</b>\n• The database row is ~200 bytes; the image file is ~2MB\n• Storing the image in the DB would make the row 10,000× larger, killing query performance\n• The first user to view the photo fetches from S3 origin; every subsequent viewer gets it from CloudFront in under 20ms",
    interviewTip:
      "\"When they ask you to design any system with user uploads, immediately say: I'd use object storage like S3 for the file bytes, store just the URL in the database, and put a CDN in front of S3 for fast global reads.\"\n\n• <b>1. Upload path</b> — client uploads directly to S3 (bypasses your app servers); app servers only write the URL to the DB\n• <b>2. Read path</b> — CDN → S3 origin on cache miss; the database is never involved in serving bytes\n• <b>3. Private content</b> — pre-signed URLs that expire after 15 minutes; never make the bucket public for user data\n• <b>4. Never SQL for binary</b> — images, video, audio, PDFs always go to object storage; DB stores the URL only\n\nThe most common follow-up is \"how do you handle private files?\" — answer: generate a pre-signed URL server-side on each request; the URL contains an expiry timestamp and a signature; after 15 minutes it's invalid even if leaked.",
    tags: ["S3", "Object Storage", "Blob Storage", "GCS", "CDN", "Pre-signed URL", "Binary Data", "Media Storage"],
  },
  {
    id: 39,
    section: "Advanced Topics",
    title: "Authentication",
    tagline: "Who are you — JWT, OAuth, and sessions explained",
    description:
      "Authentication answers \"who are you?\" — authorization answers \"what are you allowed to do?\" These are separate problems, and mixing them up is one of the most common mistakes in system design.\n\n<b>Session-Based Auth (Stateful)</b>\n• After login, the server creates a session record and stores it in Redis\n• The client receives a session ID cookie and sends it on every request\n• The server looks up the session ID in Redis on each request to verify identity\n  ↳ Every request requires a Redis round trip — simple for monoliths, hard to scale across many servers\n\n<b>JWT (JSON Web Token) — Stateless</b>\n• After login, the server signs a token with a secret key and sends it to the client\n• The client sends the token in every request: `Authorization: Bearer <token>`\n• The server verifies the signature locally — no database or cache lookup needed\n  ↳ Scales horizontally — any server can verify any token with the same secret key\n• The JWT payload contains `user_id`, roles, and expiry — base64 encoded, not encrypted\n  ↳ Never store secrets or sensitive data in a JWT; anyone can decode the payload\n• <b>Access token</b>: short-lived (15 minutes) — limits damage if stolen\n• <b>Refresh token</b>: long-lived (7 days) — stored server-side; used to silently get a new access token\n\n<b>JWT Revocation Problem</b>\n• JWTs are stateless — once issued, they're valid until they expire\n• If a user is banned or logs out, their JWT still works for up to 15 minutes\n• Solution: maintain a small token blacklist in Redis; entries auto-expire after the token's TTL\n  ↳ Only a small set of revoked tokens needs to be tracked — not all issued tokens\n\n<b>OAuth 2.0 — Delegated Authorization</b>\n• Allows users to authenticate via a third-party identity provider (Google, GitHub, Apple)\n• \"Login with Google\" — your app never sees the user's Google password\n• Google verifies identity and gives your app a token confirming who the user is\n  ↳ Your app exchanges that token for its own session or JWT",
    whyItMatters:
      "Every API requires authentication, and getting it wrong means any user can access any other user's data. JWT is the standard for stateless, horizontally-scalable auth in microservices. OAuth is the standard for third-party identity. These appear in nearly every API design question at the senior level.\n\nIf you choose wrong:\n• Sessions without a shared Redis — each server has its own session store; a user's session only works on the server that issued it\n• Long-lived JWTs with no refresh token — a stolen token gives an attacker access for days\n• No JWT revocation strategy — banned users can still call your API for up to 15 minutes\n• Storing secrets in the JWT payload — anyone can base64-decode the payload; it is not encrypted\n\nAuthentication connects directly to API Gateway design (JWT validation at the edge), OAuth flows, and token storage on mobile and web clients.",
    diagramNote:
      "Fig. 39.1 — JWT authentication: the server signs a self-contained token once; every subsequent request is verified without a database lookup.",
    example:
      "A typical mobile app's auth flow.\n\n<b>1. Setup</b>\n• Login returns two tokens: a JWT access token (expires in 15 minutes) and a refresh token (expires in 7 days)\n• The app stores both; every API call sends the JWT in the `Authorization: Bearer` header\n\n<b>2. What happens</b>\n• After 15 minutes, the JWT expires — the app silently calls `POST /auth/refresh` with the refresh token\n• The server validates the refresh token against its database record and issues a new JWT\n• The user never sees a re-login prompt\n\n<b>3. The result</b>\n• When the user logs out, the refresh token is deleted from the database — future refresh attempts fail and the user must log in again\n• The old JWT still works for up to 15 minutes — that is the intentional tradeoff of stateless tokens; keep access token TTL short to minimize the window",
    interviewTip:
      "\"When they ask you to design an API, start by adding authentication at the gateway layer — every request validates a JWT before reaching any service.\"\n\n• <b>1. JWT at the gateway</b> — the API Gateway validates the JWT signature; individual services never re-validate auth; they trust the user context injected by the gateway\n• <b>2. Access + refresh tokens</b> — access token TTL is 15 minutes; refresh token is 7 days, stored server-side so it can be revoked\n• <b>3. Revocation</b> — maintain a small Redis blacklist for revoked tokens; entries auto-expire after the token's TTL\n• <b>4. OAuth for third-party</b> — use OAuth 2.0 when integrating with Google/GitHub/Apple; your app exchanges their token for your own JWT\n\nThe most common follow-up is \"how do you revoke a JWT?\" — answer: Redis blacklist keyed by `jti` (JWT ID) with TTL matching the token expiry; only a small set of actively-revoked tokens needs to be stored, not all issued tokens.",
    tags: ["JWT", "OAuth", "Authentication", "Authorization", "Session", "Refresh Token", "Bearer Token", "Stateless"],
  },

  // ─── CASE STUDY ADDITIONS ─────────────────────────────────────
  {
    id: 40,
    section: "Case Study",
    title: "URL Shortener Design",
    tagline: "Design bit.ly — hashing, redirection, and scale",
    description:
      "A URL shortener takes a long URL and produces a short code (like `bit.ly/abc123`). When someone visits the short URL, the system looks up the original and redirects them.\n\n<b>Scale: This Is a Read-Heavy System</b>\n• 100M shortening requests per day = ~1,200 write req/s\n• 10B redirections per day = ~116,000 read req/s\n  ↳ Reads outnumber writes by 100:1 — caching and read replicas are essential\n\n<b>Generating the Short Code</b>\n• Option 1: Hash the long URL (MD5, take 7 chars) — risk of collision when two URLs produce the same hash\n• Option 2: `Base62`-encode an auto-incremented ID — preferred\n  ↳ `Base62` uses 62 characters: a–z, A–Z, 0–9\n  ↳ 7 characters = 62^7 = 3.5 trillion unique codes, zero collision risk\n\n<b>Redirect: 301 vs 302</b>\n• `301` (Permanent Redirect) — browser caches the redirect, so future clicks go directly to the destination\n  ↳ Fewer server calls, but analytics stop working after the first click (browser never asks again)\n• `302` (Temporary Redirect) — every click hits the server\n  ↳ Accurate click analytics, but more load\n• Choose 302 when analytics matter; 301 when performance matters most\n\n<b>Database and Cache</b>\n• Single table: `short_code` (PK) → `long_url`, `user_id`, `created_at`, `click_count`\n• Read path: check Redis first → DB on cache miss → return redirect\n• 80% of traffic goes to 20% of URLs — LRU cache in Redis captures the hot links\n  ↳ A viral tweet's short URL might get 10M clicks in an hour, all served from cache",
    whyItMatters:
      "URL shortener is the most common starter system design question — almost every interview loop includes it. It looks simple but tests several real skills: unique ID generation without collision, handling 100:1 read/write skew, caching strategy, and redirect behavior.\n\nIf you choose wrong:\n• Hash-based codes without collision handling — two different URLs map to the same short code, breaking links\n• No cache — 116,000 read req/s hits the database directly, which can't handle that load\n• 301 redirect with analytics enabled — browser caches the redirect, clicks stop registering after the first visit\n• No read replicas — all read traffic goes to a single DB instance that becomes a bottleneck\n\nThe patterns here (ID generation, cache-aside for reads, redirect strategy) show up in dozens of other designs.",
    diagramNote:
      "Fig. 40.1 — URL shortener: write path generates a Base62 key and stores the mapping; read path checks Redis before falling back to the DB.",
    example:
      "bit.ly serves 10B+ clicks per month.\n\n<b>1. The Setup</b>\n• When a link is created, bit.ly generates an auto-incremented ID and encodes it in `Base62` to produce the 7-character short code\n• The mapping is stored in a database and the hot links are pre-loaded into Redis\n\n<b>2. What happens</b>\n• A viral tweet's shortened URL gets 10M clicks in one hour\n• Every click hits Redis and gets the long URL back in under 1ms — the database is never touched\n\n<b>3. The result</b>\n• Click counts are incremented in Redis and flushed to the database in batches periodically — avoiding 10M individual write operations during the spike",
    interviewTip:
      "\"When they ask you to design a URL shortener, start by establishing it's a read-heavy system — 116K read req/s vs 1,200 write req/s.\"\n\n• <b>1. Requirements</b> — clarify: custom aliases? expiry dates? analytics? (each adds complexity)\n• <b>2. Capacity</b> — 1,200 write req/s, 116K read req/s; design around the read path\n• <b>3. Short code generation</b> — `Base62` of an auto-incremented ID; 7 chars = 3.5T URLs, collision-free\n• <b>4. Database</b> — single table: `short_code` PK, `long_url`, `created_at`\n• <b>5. Cache</b> — Redis with LRU eviction; the hot 20% of links handle 80% of traffic\n• <b>6. Redirect</b> — 302 for analytics, 301 for pure performance; explain the tradeoff explicitly\n\nThe most common follow-up is \"how do you handle collision?\" — answer: `Base62` of an auto-incremented ID has no collision by definition since each ID is unique.",
    tags: ["URL Shortener", "Base62", "Hashing", "Redirect", "Cache-aside", "Read-heavy", "Case Study"],
  },
  {
    id: 41,
    section: "Case Study",
    title: "News Feed Design",
    tagline: "Design Twitter/Instagram feed — fan-out and ranking",
    description:
      "A news feed shows you a personalized list of posts from people you follow, sorted by time or relevance. The hard part is deciding when to compute that list — at write time or at read time.\n\n<b>Fan-out on Write (Push Model)</b>\n• When user A posts, immediately write the post ID into every follower's feed list\n  ↳ Reading the feed is instant — the list is pre-built and waiting\n• Problem: if a celebrity has 10M followers, one post triggers 10M writes\n  ↳ This is called the \"celebrity problem\" — fan-out cost is proportional to follower count\n\n<b>Fan-out on Read (Pull Model)</b>\n• When you open your feed, the system fetches recent posts from everyone you follow and merges them\n• Always fresh — no stale data\n• Problem: if you follow 1,000 accounts, every feed load requires 1,000 DB reads and a merge\n  ↳ Slow and expensive at scale\n\n<b>Hybrid Approach (Used in Production)</b>\n• Push for normal users (fewer than 10K followers) — fast reads, manageable write cost\n• Pull for celebrities (more than 10K followers) — too expensive to fan-out their posts\n  ↳ When you open your feed, their recent posts are fetched and merged in real time\n\n<b>Feed Storage</b>\n• Each user's feed is a Redis sorted set — scores are timestamps, values are post IDs\n  ↳ Sorted sets allow cheap range queries: \"give me the 20 newest posts\"\n• The feed stores only post IDs, not full post content\n  ↳ Full post data (text, images, metadata) is fetched separately by ID in a batch lookup\n• This keeps the feed cache small and the post store as the single source of truth",
    whyItMatters:
      "News feed design is one of the five canonical system design questions — expect it in every senior-level loop. The core challenge is a fundamental tradeoff: optimizing for fast reads (push) amplifies writes; optimizing for fresh data (pull) amplifies reads.\n\nIf you choose wrong:\n• Pure push — a celebrity with 30M followers tweets and triggers 30M Redis writes in seconds, spiking your infrastructure\n• Pure pull — every feed load queries hundreds of accounts in real time, making the feed slow under load\n• Storing full post objects in the feed cache — 10M feed entries × 1KB per post = 10GB of Redis memory just for one user's followers\n\nThe push-vs-pull tradeoff also appears in notification systems, activity streams, and recommendation feeds.",
    diagramNote:
      "Fig. 41.1 — News feed fan-out: push pre-writes to followers' caches (fast reads, expensive writes); pull reads at request time (always fresh, slow). Hybrid merges both for celebrity accounts.",
    example:
      "Twitter uses the hybrid approach.\n\n<b>1. The Setup</b>\n• Normal users (under 10K followers): when they tweet, the post ID is pushed into each follower's Redis sorted set immediately\n• Celebrities (over 10K followers): their posts are NOT pushed at write time\n\n<b>2. What happens</b>\n• When you open your timeline, Twitter serves your pre-computed Redis feed (fast)\n• It also pulls recent posts from any celebrities you follow and merges them inline\n\n<b>3. The result</b>\n• Your feed loads in milliseconds and always shows the celebrity's latest tweet — without causing millions of writes every time Elon Musk posts",
    interviewTip:
      "\"When they ask you to design a news feed, start by asking: does the feed need to be real-time or can it be slightly stale? That determines how aggressive your push strategy is.\"\n\n• <b>1. Fan-out on write</b> — fast reads, but write amplification; can't push for celebrities with 10M+ followers\n• <b>2. Fan-out on read</b> — always fresh, but slow at scale; pulling from 1,000 followees per load is expensive\n• <b>3. Hybrid</b> — push for normal users (under 10K followers), pull for celebrities; this is the production answer\n• <b>4. Feed storage</b> — Redis sorted set per user, scores = timestamps, values = post IDs only (not full post data)\n• <b>5. Post content</b> — fetched separately by post ID in a batch lookup after the feed list is assembled\n\nThe most common follow-up is \"what counts as a celebrity?\" — answer: a configurable follower threshold (e.g., 10K), stored as a flag on the user record.",
    tags: ["News Feed", "Fan-out", "Push Model", "Pull Model", "Redis", "Timeline", "Celebrity Problem", "Case Study"],
  },
  {
    id: 42,
    section: "Case Study",
    title: "Messaging App Design",
    tagline: "Design WhatsApp — real-time delivery, storage, and receipts",
    description:
      "A messaging app needs to deliver messages in real time to online users and hold them for offline users until they reconnect.\n\n<b>Real-Time Delivery with WebSockets</b>\n• Each client maintains a persistent `WebSocket` connection to a chat server\n  ↳ Unlike HTTP, `WebSocket` stays open — the server can push messages at any time without the client polling\n• When you send a message: your client → chat server → stores in DB → delivers to recipient's `WebSocket`\n• Each chat server handles ~50K concurrent `WebSocket` connections\n• Users are routed to the same server via consistent hashing on `user_id` — so the server always knows which connection belongs to which user\n\n<b>Offline Message Handling</b>\n• If the recipient is offline, the message is stored in the database\n• The server sends a push notification via `APNs` (iOS) or `FCM` (Android) to wake up the recipient's device\n  ↳ When the app opens, it fetches all undelivered messages from the server\n\n<b>Message Storage: Why Cassandra</b>\n• Messages are time-series data: high write volume, always read in time order, rarely updated\n• Cassandra handles massive write throughput and scales horizontally — ideal for this pattern\n• Schema: (`conversation_id`, `message_id`, `sender_id`, `content`, `timestamp`, `status`)\n  ↳ `message_id` uses a Snowflake-style time-sortable ID so messages are stored in chronological order\n\n<b>Group Messages and Media</b>\n• Group message: sender → message service fans out to all active member connections + stores for offline members\n  ↳ Group size should be capped (e.g., 256 members) to limit fan-out cost per message\n• Images and videos go to object storage (S3) — the message stores just the S3 URL\n• End-to-end encryption: keys live on devices only; the server stores and delivers ciphertext it cannot read",
    whyItMatters:
      "Messaging app design is one of the most common senior-level system design questions. It forces you to reason about persistent connections at scale, a database that handles millions of writes per second, and the difference between online and offline delivery paths.\n\nIf you choose wrong:\n• HTTP polling instead of `WebSocket` — the client checks for new messages every few seconds; adds latency and wastes bandwidth at scale\n• Storing messages in PostgreSQL — time-series append-only data at billions of rows/day will overwhelm a relational DB\n• No push notifications for offline users — messages are silently lost if the recipient doesn't reopen the app\n• No group size cap — a 10K-member group channel means one message triggers 10K fan-out writes\n\nThe patterns here (persistent connections, time-series storage, push notification fallback) appear in any real-time system: live sports scores, collaborative editing, trading platforms.",
    diagramNote:
      "Fig. 42.1 — Chat message flow: online recipients receive via WebSocket; offline recipients get a push notification and fetch on next open.",
    example:
      "WhatsApp handles 2 billion users and ~100 billion messages per day.\n\n<b>1. The Setup</b>\n• Every device holds a persistent `WebSocket` connection to a chat server\n• Messages are stored in a Cassandra cluster partitioned by `conversation_id`\n\n<b>2. What happens</b>\n• You send a message to a friend in Tokyo — the chat server delivers it via their open `WebSocket` in under 100ms\n• The three delivery ticks (single grey = sent to server, double grey = delivered to device, double blue = read) are implemented as acknowledgement messages sent back through the same `WebSocket` channel\n\n<b>3. The result</b>\n• The sender knows exactly where in the delivery pipeline their message is — all without polling, no wasted requests",
    interviewTip:
      "\"When they ask you to design a messaging app, start with the real-time path — how does a message get from sender to recipient in under 100ms?\"\n\n• <b>1. Real-time</b> — `WebSocket` persistent connections; each server handles 50K connections; route users to the same server via consistent hashing on `user_id`\n• <b>2. Storage</b> — Cassandra for messages (high write volume, time-series); partition by `conversation_id`\n• <b>3. Offline</b> — store messages in DB; send `APNs`/`FCM` push notification to wake the device; client fetches on reconnect\n• <b>4. Groups</b> — fan-out to all active member connections; cap group size to control fan-out cost\n• <b>5. Media</b> — S3 for file bytes; store the S3 URL in the message row\n\nThe delivery receipt flow is the most common follow-up — trace the ACK path explicitly: recipient's device sends a \"delivered\" ACK back through `WebSocket`, server updates message status, forwards to sender's connection.",
    tags: ["Messaging", "WhatsApp", "WebSockets", "Cassandra", "Real-time", "Fan-out", "Offline Queue", "Case Study"],
  },
  {
    id: 44,
    section: "Case Study",
    title: "Web Crawler Design",
    tagline: "Design a Google-scale web crawler — queues, deduplication, and politeness",
    description:
      "A web crawler automatically visits web pages, downloads their HTML, extracts links, and repeats — building an index of the entire web.\n\n<b>Core Components</b>\n• URL Frontier — the priority queue of URLs waiting to be crawled\n  ↳ Not a simple FIFO queue — URLs are ranked by importance (PageRank, freshness, domain authority)\n  ↳ High-priority pages (news sites, popular blogs) get crawled daily; low-quality pages monthly\n• Fetcher — downloads the raw HTML from each URL\n• Link Extractor — parses the HTML and pulls out all `<a href>` links\n• Deduplicator — checks if a URL has already been crawled before adding it to the frontier\n• Content Store — saves the downloaded HTML for the indexing pipeline\n\n<b>Deduplication with Bloom Filters</b>\n• The same page can be reached from thousands of different links — without deduplication, you'd crawl it thousands of times\n• Normalize URLs before checking: lowercase, strip trailing slash, sort query params\n  ↳ `http://Example.com/page/` and `https://example.com/page` should map to the same fingerprint\n• A Bloom filter checks membership in O(1) time with very low memory — perfect for billions of seen URLs\n  ↳ Bloom filters can have false positives (say \"seen\" when not), but never false negatives (never miss a duplicate)\n\n<b>Politeness: Don't Hammer Servers</b>\n• Always check `robots.txt` before crawling any URL from a domain\n  ↳ `robots.txt` specifies which paths the site owner allows or disallows crawlers to visit\n• Rate-limit crawls per domain using a per-domain delay queue\n  ↳ Respect the `Crawl-delay` directive in `robots.txt`\n• DNS caching: cache DNS lookups per domain to avoid one DNS query per URL\n  ↳ Without caching, crawling 10M pages from amazon.com triggers 10M DNS lookups\n\n<b>Scaling with Domain-Partitioned Workers</b>\n• Partition the URL frontier by domain hash — each worker owns a fixed set of domains\n• This ensures politeness (one worker per domain, so rate limits are naturally enforced) and enables parallel crawling at scale",
    whyItMatters:
      "Web crawler design tests skills that apply far beyond crawling: priority queue management, deduplication at scale, distributed worker coordination, and politeness policies. Any large-scale data ingestion system — scraping, indexing, link checking — uses the same architecture.\n\nIf you choose wrong:\n• No deduplication — the crawler loops endlessly re-visiting the same pages, wasting all capacity\n• No Bloom filter — a hash set of seen URLs at billion-page scale consumes terabytes of memory\n• No `robots.txt` respect — you overload servers, get IP-banned, and potentially face legal issues\n• No domain partitioning — multiple workers hammer the same domain simultaneously, violating rate limits\n• No DNS caching — DNS lookups become a bottleneck at millions of requests per hour\n\nThese same patterns (priority queue, deduplication, partitioned workers) appear in any system that ingests large amounts of external data.",
    diagramNote:
      "Fig. 44.1 — Web crawler loop: the frontier feeds fetchers; parsed links are deduplicated via Bloom filter before re-entering the frontier.",
    example:
      "Googlebot crawls billions of pages continuously.\n\n<b>1. The Setup</b>\n• The URL frontier is distributed and sharded by domain hash across thousands of crawler workers\n• Each worker is responsible for a set of domains and respects per-domain `robots.txt` and `Crawl-delay` directives\n\n<b>2. What happens</b>\n• Googlebot fetches a page, extracts links, normalizes each URL, and checks a distributed Bloom filter before adding new URLs to the frontier\n• DNS results are cached per domain — crawling millions of pages from one domain requires only one DNS lookup\n\n<b>3. The result</b>\n• Important pages (major news sites) are re-crawled within minutes of an update; low-quality pages are crawled once a month — crawl budget is allocated intelligently based on importance signals",
    interviewTip:
      "\"When they ask you to design a web crawler, start by explaining the URL Frontier — it's the heart of the system and most candidates skip straight to the fetcher.\"\n\n• <b>1. URL Frontier</b> — priority queue, not FIFO; rank by PageRank or last-modified header; high-value pages crawled more frequently\n• <b>2. Deduplication</b> — Bloom filter for URL seen-check (O(1), low memory); MD5 hash of page content to detect near-duplicate pages\n• <b>3. Politeness</b> — check `robots.txt` before crawling any domain; per-domain rate limiting; respect `Crawl-delay` header\n• <b>4. Scale</b> — partition the frontier by domain hash; each worker owns a shard; DNS caching per domain\n\nThe deduplication strategy is the most common deep-dive — be ready to explain why a Bloom filter is preferred over a hash set at billion-URL scale (memory: a Bloom filter uses ~1.2 bytes per URL vs. 50+ bytes for a URL string in a hash set).",
    tags: ["Web Crawler", "Bloom Filter", "Deduplication", "URL Frontier", "robots.txt", "Politeness", "Case Study"],
  },
  {
    id: 45,
    section: "Case Study",
    title: "Notification System Design",
    tagline: "Design push, email, and SMS notifications at scale",
    description:
      "A notification system delivers messages to users through multiple channels — push notifications, email, and SMS — at high volume and reliably.\n\n<b>Core Architecture: Queue Per Channel</b>\n• Notification Service receives a request: `POST /notify` with user ID, type, and content\n• It publishes the notification to a message queue, then returns immediately\n• Separate workers consume from separate queues per channel: Push Worker, Email Worker, SMS Worker\n  ↳ If SendGrid (email) is slow, it doesn't delay iOS push notifications — they run independently\n\n<b>Channels</b>\n• Push notifications: iOS via `APNs`, Android via `FCM` — requires the user's device token\n• Email: via SMTP or a provider like SendGrid — for receipts, digests, and account alerts\n• SMS: via Twilio — for critical alerts like 2FA codes or fraud warnings\n\n<b>User Preferences</b>\n• A preferences table records each user's opt-out choices per channel and per notification type\n  ↳ Always check this before sending — never send a notification a user has disabled\n• Example: user opts out of marketing emails but keeps push notifications enabled\n\n<b>Deduplication with Idempotency Keys</b>\n• Each notification gets a unique idempotency key (e.g., `notif_{user_id}_{event_id}`)\n• Before sending, the worker checks if this key was already processed\n  ↳ If yes, skip — prevents duplicate push notifications or emails when a worker retries after a timeout\n\n<b>Rate Limiting and Failure Handling</b>\n• Rate limit per user (e.g., max 3 push notifications per hour) to avoid spamming\n• Failed deliveries retry with exponential backoff\n• After N failures, the notification moves to a Dead Letter Queue (DLQ) for investigation\n• Delivery status is tracked: queued → sending → delivered / failed",
    whyItMatters:
      "Notification systems appear in nearly every product design — social apps, e-commerce, SaaS, banking. The core pattern (API → queue → per-channel worker) is one of the most reusable in system design.\n\nIf you choose wrong:\n• Synchronous delivery (no queue) — a slow SMS provider (Twilio rate limit) blocks all notifications from going out\n• Single queue for all channels — one backed-up email queue delays urgent push notifications\n• No idempotency key — retrying a failed worker sends the same email or push notification twice\n• No user preference check — users receive notifications they explicitly disabled; leads to spam complaints and uninstalls\n• No rate limiting — a sudden event (a viral post) triggers millions of push notifications simultaneously, overwhelming `APNs`/`FCM`\n\nThe queue-per-channel pattern also applies to any system with multiple async delivery targets: order confirmations, fraud alerts, CI/CD build results.",
    diagramNote:
      "Fig. 45.1 — Notification pipeline: a queue decouples ingestion from delivery workers for push, email, and SMS; failed deliveries land in a DLQ.",
    example:
      "When you like someone's Instagram post, a notification fires.\n\n<b>1. The Setup</b>\n• The like event triggers a Notification Service call with user ID and event type\n• The service checks the recipient's preferences — if push is enabled, it publishes to the push queue\n\n<b>2. What happens</b>\n• The Push Worker picks up the message, looks up the recipient's device token, and calls `APNs` (iOS) or `FCM` (Android)\n• The idempotency key ensures that even if the worker crashes and retries, the notification is only delivered once\n\n<b>3. The result</b>\n• Instagram sends billions of push notifications per day across iOS and Android simultaneously — because each channel runs its own independent worker pool with no shared bottlenecks",
    interviewTip:
      "\"When they ask you to design a notification system, start by listing the channels — push, email, SMS — and explain why each needs its own queue.\"\n\n• <b>1. Channels</b> — `APNs`/`FCM` for push, SendGrid for email, Twilio for SMS; each has its own worker pool\n• <b>2. Queue per channel</b> — slow email delivery never blocks push notifications; decoupled by design\n• <b>3. User preferences</b> — always check opt-out before calling any provider; respect per-channel and per-type settings\n• <b>4. Deduplication</b> — idempotency key per notification (`notif_{user_id}_{event_id}`); workers skip if key already processed\n• <b>5. Rate limiting</b> — max N push notifications per user per hour; prevents spam during high-traffic events\n\nThe most common follow-up is \"what if `APNs` is down?\" — answer: messages stay in the queue, the worker retries with exponential backoff, and after N failures the notification moves to a DLQ for manual review or re-queuing.",
    tags: ["Notifications", "Push Notifications", "APNs", "FCM", "Email", "SMS", "Fan-out", "Idempotency", "Case Study"],
  },
  {
    id: 46,
    section: "Foundation",
    title: "System Design Interview Framework",
    tagline: "The 4-step structure that turns knowledge into a passing interview",
    description:
      "A system design interview is a structured 45-minute problem-solving discussion. One of the biggest mistakes candidates make is jumping straight into architecture diagrams before understanding the problem.\n\nA simple way to approach any system design interview is to follow a 4-step framework.\n\nStep 1: Understand the Problem (5–10 minutes). Before designing anything, ask clarifying questions. Who are the users? What features are required? What scale are we targeting — 1 million or 100 million daily active users? Define both the functional requirements (what the system should do) and non-functional requirements (scale, latency, availability, reliability).\n\nStep 2: Create a High-Level Design (10–15 minutes). Identify the main components of the system, such as clients, APIs, servers, databases, and caches. Walk through the request flow for one or two key features. Focus on the overall architecture and avoid unnecessary complexity at this stage.\n\nStep 3: Deep Dive into Bottlenecks (15–20 minutes). Identify the most challenging part of the design and explore it in detail. Discuss topics such as database scaling, caching strategies, data partitioning, fault tolerance, and performance optimization. For example: \"At this scale, the database becomes a bottleneck, so I would introduce read replicas and shard data by user_id.\"\n\nStep 4: Wrap Up and Discuss Trade-offs (5 minutes). Summarize your design, explain the trade-offs you made, and discuss potential failure scenarios. For example: \"This design prioritizes availability over strict consistency.\" End by mentioning how the system could evolve if given more time or higher scale requirements.\n\nThe goal of a system design interview is not to find the perfect architecture. It is to demonstrate a clear thought process, make reasonable assumptions, identify bottlenecks, and explain the trade-offs behind your decisions.",
    whyItMatters:
      "Interviewers don't grade on whether you design the perfect system — they grade on whether you think like a senior engineer. The framework shows structured thinking, asking the right questions, and knowing where to go deep vs. stay high-level.",
    diagramNote:
      "Fig. 46.1 — System design interview timeline: four phases with common mistakes to avoid at each stage.",
    example:
      "Given 'Design WhatsApp':\n\n1. Understand the Problem — ask about scale (2B users), required features (text, media, group chats), and latency target (under 1 second for message delivery).\n2. High-Level Design — draw the core flow: client → WebSocket server → message database → push notification service.\n3. Deep Dive — focus on message storage. At 100 billion messages per day, a relational database won't scale. Use Cassandra partitioned by conversation_id to handle the write volume and time-series access pattern.\n4. Trade-offs — this design delivers messages at least once. Duplicates are possible but handled by message deduplication on the client side.",
    interviewTip:
      "Practice saying 'Before I start drawing, let me clarify a few things' — this single sentence signals seniority. Never assume scale. Never assume features. Ask: users, scale, read/write ratio, latency requirements. Then restate: 'So I'm designing for 10M DAU, read-heavy, with < 200ms latency for the feed.' This confirmation prevents 30 minutes of work in the wrong direction.",
    tags: ["Interview Framework", "System Design", "Requirements", "High-level Design", "Trade-offs", "Bottlenecks"],
  },

  // ─── INTERVIEW Q&A ADDITIONS ─────────────────────────────────
  {
    id: 47,
    section: "Data Layer",
    title: "ACID vs BASE",
    tagline: "Two opposing consistency models for databases",
    description:
      "ACID and BASE are two different ways databases handle data.\n\n<b>ACID (Focuses on Data Correctness)</b>\n\nACID makes sure your data is always correct and reliable.\n\n• <b>Atomicity</b>: A transaction either completes fully or doesn't happen at all.\n  ↳ Example: When transferring $100 from Account A to Account B, both the debit and credit must happen together. If one fails, everything is rolled back.\n\n• <b>Consistency</b>: Data always stays valid.\n  ↳ Example: If a bank account cannot have a negative balance, the database prevents transactions that would break that rule.\n\n• <b>Isolation</b>: Multiple users can perform transactions at the same time without interfering with each other.\n  ↳ Example: Two people trying to buy the last ticket won't both get it.\n\n• <b>Durability</b>: Once data is saved, it stays saved even if the server crashes.\n  ↳ Example: If you receive a payment confirmation, that payment won't disappear after a restart.\n\nUse ACID when data accuracy is critical, such as:\n• Banking systems\n• Payment processing\n• Inventory management\n• Booking systems\n\n<b>BASE (Focuses on Availability and Speed)</b>\n\nBASE is designed for large distributed systems where keeping the application available is more important than showing the latest data immediately.\n\n• <b>Basically Available</b>: The system keeps working even if some servers fail.\n• <b>Soft State</b>: Different servers may temporarily have different versions of the data.\n• <b>Eventual Consistency</b>: Given enough time, all servers will eventually have the same data.\n\nExample:\nImagine you post a photo on social media. Some users might see the new post immediately, while others might see it a few seconds later. Eventually, everyone sees the same data.\n\nUse BASE when:\n• High availability is important\n• Small delays in data synchronization are acceptable\n• The system serves millions of users\n\nExamples:\n• Social media feeds\n• Analytics dashboards\n• Activity logs\n• Recommendation systems\n\n<b>Simple Rule</b>\n\n• Choose <b>ACID</b> when data correctness is the top priority.\n• Choose <b>BASE</b> when availability and performance are more important than seeing the latest data instantly.",
    whyItMatters:
      "The choice between ACID and BASE affects which database you pick and how your system behaves.\n\nIf you need perfect accuracy, you usually go with databases like PostgreSQL or MySQL (ACID).\nIf you need high speed and can tolerate small delays in syncing data, you use systems like Cassandra, DynamoDB, or CouchDB (BASE).\n\nIf you choose the wrong one:\n• Using BASE when you need accuracy can cause wrong or inconsistent data\n• Using ACID when you need massive scale can make your system slow or hard to scale",
    diagramNote:
      "Fig. 47.1 — ACID enforces strict correctness on every transaction; BASE trades consistency for availability and performance in distributed systems.",
    example:
      "<b>1. Payments system (ACID)</b>\nWhen you send $100 to someone:\n• Money should NOT disappear or get duplicated\n• Either both things happen:\n  ↳ Your account is debited\n  ↳ Their account is credited\n• Or nothing happens at all\n\nWe use PostgreSQL / MySQL here because correctness is critical.\n\n<b>2. Social media feed (BASE)</b>\nWhen someone posts something:\n• Some users might see it instantly\n• Others might see it a few seconds later\n• Eventually everyone sees it\n\nThis is fine because:\n• No money is involved\n• Slight delays don't matter\n\nWe use systems like Cassandra or DynamoDB.",
    interviewTip:
      "\"I'll use an ACID database when data must always be correct, like payments or bookings.\"\n\"I'll use a BASE system when I need to handle large-scale data and can accept small delays, like feeds or logs.\"\n\nACID and BASE are not something you use together — they represent two different design choices.",
    tags: ["ACID", "BASE", "Atomicity", "Consistency", "Eventual Consistency", "PostgreSQL", "Cassandra", "Transactions"],
  },
  {
    id: 48,
    section: "Data Layer",
    title: "Eventual Consistency",
    tagline: "All nodes converge to the same value — given enough time",
    description:
      "Eventual consistency means that in a distributed system (multiple servers), when you update data on one server, the other servers will catch up — but not immediately.\n\nThink of it like this: you like a photo on Instagram. Your friend in another country might not see your like for a second or two. But eventually, everyone sees the same data.\n\n<b>Two types of consistency</b>\n\n• <b>Strong Consistency</b>: Every server shows the same data at the same time. Slower, but always accurate.\n  ↳ Example: You update your bank balance — everyone sees it instantly, everywhere.\n\n• <b>Eventual Consistency</b>: One server gets the update first. Others catch up shortly after. Faster, but briefly out of sync.\n  ↳ Example: You post a tweet — some followers see it immediately, others see it 1–2 seconds later.\n\n<b>How it works (step by step)</b>\n\n1. You write data to Server A\n2. Server A saves it and responds immediately\n3. In the background, Server A sends the update to Server B and Server C\n4. For a brief moment, Server B and C still have old data\n5. Once the update arrives, all servers match\n\n<b>When eventual consistency is okay</b>\n\n• Social media likes and comments\n• Product view counts\n• Activity feeds\n• DNS updates — your domain change can take hours to spread worldwide, but eventually it does\n\n<b>When it is NOT okay</b>\n\n• Bank transfers — you can't show different balances to different users\n• Inventory — you can't sell the same item twice\n• Booking systems — you can't give the same seat to two people\n\n<b>Simple Rule</b>\n\n• Use eventual consistency when small delays are fine and availability matters more\n• Use strong consistency when accuracy is critical and wrong data causes real problems",
    whyItMatters:
      "Most high-scale systems like Instagram, Twitter, and Netflix use eventual consistency because it lets them stay fast and available even when millions of users are active at the same time.\n\nIf every server had to agree before replying, the system would be too slow.\nWith eventual consistency, each server responds immediately and syncs up shortly after.\n\nIf you choose wrong:\n• Using strong consistency everywhere can make your system slow at scale\n• Using eventual consistency where accuracy matters can cause real data problems",
    diagramNote:
      "Fig. 48.1 — Eventual consistency: after a write to one node, replicas receive the update asynchronously; all converge to the same value within milliseconds to seconds.",
    example:
      "<b>1. Social media likes (Eventual Consistency)</b>\nYou like a post on Instagram.\n• Your screen shows the like immediately\n• Your friend on the other side of the world sees it 1–2 seconds later\n• Eventually, all servers show the same count\n\nThis is fine — a small delay in like counts doesn't hurt anyone.\n\n<b>2. DNS updates (Eventual Consistency in real life)</b>\nYou change your website's domain settings.\n• Some users get the new address right away\n• Others still see the old address for a few hours\n• After 24–48 hours, everyone gets the correct address\n\nSmall delays are acceptable here because no money or critical data is involved.",
    interviewTip:
      "\"I'd use eventual consistency here because this is a feed — small delays in syncing are acceptable and we need the system to stay fast and available.\"\n\n\"I would NOT use eventual consistency for payments or bookings — those need to be accurate at all times.\"\n\nBonus: systems like Cassandra let you choose per request — strong consistency for critical reads, eventual consistency for high-volume writes.",
    tags: ["Eventual Consistency", "CAP Theorem", "AP", "Replication Lag", "Cassandra", "DynamoDB", "DNS"],
  },
  {
    id: 49,
    section: "Scaling & Distribution",
    title: "Consistent Hashing",
    tagline: "Add or remove servers without remapping all keys",
    description:
      "Consistent hashing solves a problem that shows up when you scale a system by adding or removing servers.\n\n<b>The problem with regular hashing</b>\nWith standard hash partitioning:\n• A key maps to a server using: `server = hash(key) % N`\n• When you add a new server (N changes from 4 to 5), most keys get a different result\n• Almost every key remaps to a different server\n• This forces a massive data migration on a live system — impractical\n\n<b>How consistent hashing works</b>\nImagine placing all servers around a circle (called a hash ring).\nSteps:\n1. Each server is placed at a position on the ring based on its own hash value\n2. Each data key is also mapped to a position on the ring\n3. A key is assigned to the first server you reach going clockwise from the key's position\n\n<b>Adding or removing servers</b>\nWhen you add a new server:\n• It only takes keys from the one neighbor behind it on the ring\n• Only ~1/N of all keys need to move — everything else stays in place\n\nWhen you remove a server:\n• Its keys are picked up by the next server clockwise\n• Again, only ~1/N of keys are affected\n\n<b>Virtual Nodes (the load balancing improvement)</b>\nOne server at one position creates uneven load if servers end up unevenly spaced. Instead:\n• Each physical server gets 100–200 positions on the ring (called virtual nodes)\n• Load distributes much more evenly\n• A more powerful server gets more virtual nodes to carry more data\n\n<b>Simple Rule</b>\n• Regular hashing: change N → almost everything remaps (bad for live systems)\n• Consistent hashing: change N → only ~1/N of keys move (safe for live systems)",
    whyItMatters:
      "When you add a server to a regular cache cluster, nearly all cached data becomes invalid because keys remap to new servers. With consistent hashing, only 1/N of keys move — the rest stay exactly where they are.\n\nThis is what makes it practical to add or remove servers on a live system without a full restart or downtime.\n\nIf you choose wrong:\n• Regular hashing at scale = adding one server causes a cache storm — everything misses at once and hammers the database\n• No virtual nodes = hot spots where some servers carry far more data than others despite the same hardware",
    diagramNote:
      "Fig. 49.1 — Consistent hash ring: servers placed at hash positions; a key maps to the first clockwise server. Adding a server transfers only the keys between it and its predecessor.",
    example:
      "<b>1. Adding a server with regular hashing (the problem)</b>\nYou have 4 cache servers. All keys use `server = hash(key) % 4`. You add a 5th server — now every key uses `hash(key) % 5`. Most keys map to a different server. Your entire cache is effectively invalidated. A huge traffic spike hits the database as everything re-fetches at once.\n\nWith consistent hashing: the 5th server joins the ring and takes keys from one neighbor only. ~20% of keys move, ~80% stay exactly where they are. No cache storm.\n\n<b>2. Cassandra</b>\nUses consistent hashing to decide which node stores which data. Adding a node only affects the keys it takes from one neighbor. No downtime, no full reshuffle. The cluster stays live the whole time.",
    interviewTip:
      "\"I'd use consistent hashing to partition keys across nodes so that adding a server only moves ~1/N of keys, not everything.\"\n\n\"To avoid hot spots and weight nodes by capacity, I'd assign each server multiple virtual nodes on the ring.\"\n\nBring up consistent hashing when discussing distributed caches, NoSQL databases (Cassandra, DynamoDB), or any system where servers need to be added or removed without a full reshuffle.",
    tags: ["Consistent Hashing", "Hash Ring", "Virtual Nodes", "Cassandra", "DynamoDB", "Partitioning", "Sharding"],
  },
  {
    id: 50,
    section: "Advanced Topics",
    title: "Bloom Filter",
    tagline: "Tells you definitely NOT in set, probably IN set",
    description:
      "A Bloom filter is a data structure that quickly answers one question: \"Is this item in the set?\"\n\nIt has one unusual property: it can tell you with certainty that something is NOT in the set, but it can only tell you that something probably IS in the set — with a small chance of being wrong.\n\n• `Definitely NOT in set` → 100% accurate — the item was never inserted\n• `Probably IN set` → usually correct, but occasionally a false positive\n\nThere are no false negatives. If something was inserted, the filter will always say it's present.\n\n<b>How it works</b>\n\nA Bloom filter is just a bit array — a large list of 0s and 1s, initially all zeros.\n\n<b>Inserting an item:</b>\n1. Run the item through K different hash functions\n2. Each hash function gives a position in the bit array\n3. Set all K positions to 1\n\n<b>Checking if an item is in the set:</b>\n1. Run the item through the same K hash functions\n2. Check those K positions in the bit array\n3. If any position is 0 → definitely not in the set\n4. If all positions are 1 → probably in the set (could be a coincidence from other inserts)\n\n<b>Why false positives happen</b>\nDifferent items can hash to the same bit positions. When the filter says \"probably present\", it might just be seeing bits that were set by other items.\n\n<b>When to use a Bloom filter</b>\n\n• `Cache layer` — before doing an expensive DB lookup, check the Bloom filter. If it says \"definitely not\", skip the DB query entirely. Saves database load.\n• `Web crawlers` — check if a URL has already been visited before fetching it\n• `Spam filters` — check if an email address is in a blocklist\n• `Database internals` — Google BigTable and Apache Cassandra use Bloom filters to skip reading files that definitely don't contain a key\n\n<b>Why it's space-efficient</b>\n\nStoring 1 billion visited URLs as full strings would require hundreds of gigabytes. A Bloom filter can track the same 1 billion URLs in about 1–2 GB of memory, with a small false positive rate.",
    note:
      "A Bloom filter never has false negatives — if it says something is not present, that's guaranteed. It trades a small false positive rate for massive memory savings.\n\nUse it anywhere you want to avoid expensive lookups for items that definitely don't exist.",
    whyItMatters:
      "Bloom filters eliminate unnecessary expensive lookups. A cache miss that hits the DB for a non-existent key wastes a DB round trip. A Bloom filter intercepts that miss in O(1) time with near-zero memory. Google BigTable, Apache Cassandra, and HBase all use Bloom filters for this exact purpose.",
    diagramNote:
      "Fig. 50.1 — Bloom filter bit array: insert sets K bits; lookup checks K bits — all 1 means probably present, any 0 means definitely absent.",
    example:
      "A web crawler maintains a Bloom filter of all visited URLs. Before fetching a page, it checks the filter. If definitely not visited (any bit is 0), fetch it and add to filter. If probably visited (all bits are 1), skip it. The filter uses 1GB of memory to track 1 billion URLs — storing 1 billion full URL strings would require 100–500GB.",
    interviewTip:
      "Mention Bloom filters when designing caches or crawlers: 'I'd use a Bloom filter in front of the cache to eliminate DB lookups for keys I know don't exist — it never has false negatives, only rare false positives, which just mean an unnecessary DB query, not a wrong result.' Also bring it up when asked about reducing cache misses or avoiding expensive downstream calls.",
    tags: ["Bloom Filter", "Probabilistic", "Membership Test", "False Positive", "Caching", "Web Crawler", "BigTable"],
  },
  {
    id: 51,
    section: "Data Layer",
    title: "Denormalization",
    tagline: "Duplicate data to eliminate joins and speed up reads",
    description:
      "First, understand what <b>Normalization</b> is:\n\nNormalization means storing each piece of data in only one place. Tables are linked together, and you use joins to combine them when reading.\n\nExample — a blog:\n• `users` table stores the author's name\n• `posts` table stores the post content\n• To show a post with the author's name, you JOIN both tables every single time\n\nThis is clean and avoids duplicate data. But joins have a cost — especially when millions of people are reading at the same time.\n\n<b>What is Denormalization?</b>\n\nDenormalization means copying data into multiple places to avoid those joins and make reads faster.\n\nInstead of joining the `users` table every time someone reads a post, you store `author_name` directly in the `posts` table.\n\n• Before (normalized): Read `posts` + JOIN `users` → 2 operations\n• After (denormalized): Read `posts` only → 1 operation, much faster\n\n<b>The trade-offs</b>\n\n• <b>Faster reads</b> — no joins needed, one query does the job\n• <b>More storage</b> — same data is stored in multiple places\n• <b>Harder to update</b> — if an author changes their name, you must update every post they wrote, not just one record\n• <b>Risk of inconsistency</b> — two copies of the same value can get out of sync if not handled carefully\n\n<b>When to use Denormalization</b>\n\n• Reads happen much more than writes (social feeds, product pages, news articles)\n• Read speed is critical for a good user experience\n• The duplicated data rarely changes (author names, category labels, product titles)\n\n<b>When NOT to use it</b>\n\n• Data changes frequently (stock prices, inventory counts)\n• You cannot afford inconsistency between copies",
    note:
      "Denormalization trades storage and write complexity for dramatically faster reads. Use it when read performance is critical and your data is read far more often than it is written.\n\nThe classic signal: a page that runs 6 table joins every time it loads.",
    whyItMatters:
      "As your app grows, joining multiple tables on millions of requests per day becomes slow and expensive.\n\nDenormalization is a deliberate choice to store some data in more than one place so that reading it is instant.\n\nIf you choose wrong:\n• Too much normalization at scale = slow reads and a struggling database\n• Too much denormalization = complicated updates and data that can get out of sync",
    diagramNote:
      "Fig. 51.1 — Normalized schema joins 3 tables to render one post; denormalized schema stores redundant data on one row, eliminating the joins at query time.",
    example:
      "<b>1. Blog post page (Normalized — slower)</b>\nTo show one post, you need to:\n• Read `posts` table (for content)\n• Join `users` table (for author name and photo)\n• Join `categories` table (for topic label)\n\nAt 10 users, this is fine. At 10 million daily readers, these joins slow everything down.\n\n<b>2. Blog post page (Denormalized — faster)</b>\nStore `author_name`, `author_avatar`, and `category_name` directly in the `posts` table.\n• Reading a post = one query, no joins\n• If the author changes their name, a background job updates every post they wrote\n\nThe tradeoff is worth it because posts are read millions of times but author names almost never change.",
    interviewTip:
      "\"I'd denormalize author_name into the posts table to eliminate a join at read time. Reads outnumber writes 100:1 on a feed, so the extra write complexity is worth it.\"\n\nAlways state the trade-off: more storage, harder writes. And mention materialized views as the database-managed way to handle denormalization automatically.",
    tags: ["Denormalization", "Normalization", "Joins", "Materialized View", "Read Performance", "Data Redundancy"],
  },
  {
    id: 52,
    section: "Advanced Topics",
    title: "Service Discovery",
    tagline: "How microservices find each other dynamically",
    description:
      "Service discovery is how microservices find each other in dynamic environments where IPs and ports change constantly.\n\n<b>The Problem</b>\n• Cloud VMs get new IPs when they restart; containers move between hosts; auto-scaling groups add and remove instances\n• Hardcoding IP addresses in config files is impractical — every change requires redeploying every caller\n  ↳ A system with 50 microservices where each calls 5 others has 250 hardcoded addresses to maintain\n\n<b>Service Registry</b>\n• A central key-value store that tracks every healthy service instance\n• Each entry records: service name, IP address, port, and health status\n• Services send <b>heartbeats</b> every few seconds to say \"I'm still alive\"\n  ↳ If 3 consecutive heartbeats are missed, the registry marks the instance unhealthy and removes it\n• Tools: Consul, Eureka (Netflix), etcd\n\n<b>Client-Side Discovery</b>\n• The calling service queries the registry directly to get a list of healthy instances\n• It then picks one using a load-balancing algorithm (round robin, least connections)\n  ↳ The registry client library is embedded in the service — each service handles its own routing\n• Example: Netflix Eureka, HashiCorp Consul with client library\n\n<b>Server-Side Discovery</b>\n• The calling service sends its request to an API Gateway or load balancer\n• The gateway queries the registry and forwards the request to a healthy instance\n  ↳ The caller only needs one stable address — the gateway\n  ↳ No registry client library needed in each service\n• Example: AWS ALB with ECS service discovery, Kubernetes `kube-proxy`\n\n<b>Self-Registration vs Third-Party Registration</b>\n• <b>Self-registration</b>: the service registers itself with the registry on startup and deregisters on shutdown\n• <b>Third-party registration</b>: a sidecar container or orchestrator (Kubernetes) handles registration",
    whyItMatters:
      "Without service discovery, every IP change requires manually reconfiguring all callers — impossible in dynamic cloud environments where containers restart and auto-scaling groups grow and shrink. Service discovery is the infrastructure layer that makes microservices work at scale.\n\nIf you choose wrong:\n• Hardcoded IPs in config — a container restarts with a new IP; every caller errors until configs are manually updated\n• No heartbeat TTL — dead instances stay in the registry; callers keep routing traffic to them and getting errors\n• Client-side discovery without a fallback — if the registry is briefly unavailable, no service can call any other\n\nService discovery connects directly to health checks, load balancing, and API Gateway routing.",
    diagramNote:
      "Fig. 52.1 — Client-side discovery: clients query the registry to get instance list. Server-side discovery: gateway queries registry; client sees only the gateway.",
    example:
      "Netflix's Recommendation Service at scale.\n\n<b>1. Setup</b>\n• Netflix uses Eureka as their service registry\n• Every microservice instance registers on startup: `{\"service\": \"recommendation\", \"ip\": \"10.0.4.22\", \"port\": 8080}`\n• Instances send heartbeats every 30 seconds\n\n<b>2. What happens</b>\n• The Homepage Service needs personalized recommendations — it queries Eureka and gets a list of 12 healthy Recommendation Service instances\n• The embedded client library picks one using round-robin load balancing and makes the call\n• One instance fails its health check — Eureka removes it from the list within 90 seconds (3 missed heartbeats)\n\n<b>3. The result</b>\n• The Homepage Service never routes traffic to dead instances\n• When a new Recommendation instance starts up during a deploy, it registers itself and begins receiving traffic — no config changes needed",
    interviewTip:
      "\"When they ask you to design a microservices system, always mention how services find each other — most candidates skip this and leave the architecture incomplete.\"\n\n• <b>1. Service registry</b> — Consul or Eureka; each instance registers name, IP, port on startup; heartbeats every 10 seconds\n• <b>2. Health checks</b> — 3 missed heartbeats marks instance unhealthy; registry removes it immediately\n• <b>3. Client-side vs server-side</b> — client-side: each service queries the registry itself (more control); server-side: gateway queries registry on behalf of callers (simpler callers, single point of failure)\n• <b>4. Kubernetes</b> — `kube-proxy` and CoreDNS handle service discovery natively via Service objects; `Service` gets a stable DNS name regardless of pod IPs\n\nA common follow-up is \"what happens if the registry goes down?\" — answer: client-side libraries cache the last known instance list and continue operating with stale data until the registry recovers; TTL on the cache trades freshness for resilience.",
    tags: ["Service Discovery", "Consul", "Eureka", "Service Registry", "Health Check", "Microservices", "Kubernetes"],
  },
  {
    id: 53,
    section: "Advanced Topics",
    title: "API Gateway",
    tagline: "Single entry point that handles cross-cutting concerns",
    description:
      "An API Gateway is the single entry point for all client traffic in a microservices architecture — it routes each request to the correct service while handling cross-cutting concerns in one place.\n\n<b>The Core Idea</b>\n• Without a gateway, clients must know the address of every microservice\n• With a gateway, clients call one address; the gateway handles routing internally\n  ↳ Services can be added, removed, or relocated without changing any client code\n\n<b>What the Gateway Does</b>\n• <b>Routing by path</b> — `/api/users/*` → User Service, `/api/orders/*` → Order Service, `/api/products/*` → Product Service\n• <b>JWT validation</b> — validates the `Authorization` header before forwarding; individual services never handle auth themselves\n• <b>Rate limiting</b> — cap requests per client per minute using token bucket or sliding window; stored in Redis\n• <b>SSL termination</b> — handles HTTPS at the edge; internal service-to-service traffic runs over plain HTTP\n• <b>Request/response transformation</b> — add headers, strip sensitive fields, convert formats\n• <b>Logging and tracing</b> — assign a correlation ID to every incoming request; pass it to all downstream services\n  ↳ One correlation ID lets you filter all logs for a single request across 10 services\n\n<b>API Gateway vs Load Balancer</b>\n• A load balancer distributes traffic across identical instances of <b>one</b> service\n• An API Gateway routes traffic across <b>many different</b> services and handles cross-cutting concerns\n  ↳ A gateway usually has a load balancer per service internally; they work together\n\n<b>Common Tools</b>\n• AWS API Gateway, Kong, NGINX, Traefik, Envoy\n• In Kubernetes, an Ingress controller (typically NGINX) acts as the API Gateway",
    whyItMatters:
      "Without an API Gateway, every microservice must independently implement authentication, rate limiting, SSL termination, and logging — duplicated across 20+ services and prone to inconsistency. The gateway centralizes all of these, so each service focuses only on its business logic.\n\nIf you choose wrong:\n• No gateway — each of 20 services implements its own auth; one service skips it and becomes an open endpoint\n• Auth in every service — a security policy change must be deployed to all services simultaneously or gaps appear\n• No rate limiting at the edge — a single misbehaving client can overwhelm any service it reaches\n• No correlation ID — a slow request spans 15 services; you have no way to filter logs to trace what happened\n\nAPI Gateway connects directly to service discovery, JWT authentication, distributed tracing, and rate limiting design.",
    diagramNote:
      "Fig. 53.1 — API Gateway: clients hit one address; the gateway authenticates, rate-limits, and routes to the correct microservice.",
    example:
      "Uber's mobile app calls a single gateway endpoint for everything.\n\n<b>1. Setup</b>\n• One gateway endpoint: `api.uber.com`\n• The gateway holds routing rules, JWT validation logic, and Redis-backed rate limits\n\n<b>2. What happens</b>\n• A driver's app sends a request to `/trip/start` with a JWT in the `Authorization` header\n• The gateway validates the JWT signature (no DB lookup — stateless verification)\n• It checks the rate limit: max 1,000 requests per minute per device; this request is within limit\n• The request is forwarded to the Trip Service with the user context injected as a header: `X-User-Id: driver_456`\n• The same request generates a log entry at the gateway with correlation ID `trace-7a9c`; the Trip Service and Payment Service both log under the same ID\n\n<b>3. The result</b>\n• The Trip Service receives a pre-authenticated request and never touches a JWT\n• The entire request flow across 4 services is traceable by filtering logs on `trace-7a9c`",
    interviewTip:
      "\"When they ask you to design a microservices system, add an API Gateway as the first layer — it's where auth, rate limiting, and routing live so no individual service has to.\"\n\n• <b>1. Routing</b> — path-based: `/api/users/*` → User Service, `/api/orders/*` → Order Service\n• <b>2. JWT validation</b> — gateway validates the token; services never re-validate; they trust the user context injected by the gateway\n• <b>3. Rate limiting</b> — token bucket in Redis; 100 req/sec per user; blocks before reaching any service\n• <b>4. Correlation ID</b> — gateway assigns a unique ID to every request; passes it as `X-Correlation-Id` to all downstream services for end-to-end log tracing\n• <b>5. vs load balancer</b> — load balancer: one service, many instances; gateway: many services, cross-cutting concerns\n\nA common follow-up is \"what if the gateway goes down?\" — answer: deploy the gateway with multiple instances behind a network load balancer; it becomes the one component that must be highly available, so treat it like critical infrastructure.",
    tags: ["API Gateway", "Routing", "Authentication", "Rate Limiting", "Microservices", "Kong", "NGINX", "Reverse Proxy"],
  },
  {
    id: 54,
    section: "Reliability",
    title: "Bulkhead Pattern",
    tagline: "Isolate failures so one service can't sink the whole ship",
    description:
      "The bulkhead pattern comes from ships — a ship is divided into watertight compartments so that if one compartment floods, the rest stay afloat. In software, it means giving each external dependency its own isolated pool of resources (threads or connections) so that a failure in one can't consume resources needed by others.\n\n<b>The problem without bulkheads</b>\nYour app calls three services: Payment, Inventory, and Email. All three share one thread pool of 50 threads.\n• The Email provider becomes slow — requests pile up waiting for responses\n• All 50 threads gradually get stuck waiting on Email\n• Now Payment requests also can't get a thread — even though Payment service is perfectly healthy\n• One slow dependency takes down your entire app\n\n<b>How bulkheads fix this</b>\nEach service gets its own dedicated thread pool:\n• Payment Service: 20 threads\n• Inventory Service: 20 threads\n• Email Service: 10 threads\n\nNow when Email goes slow:\n• Its 10 threads fill up — Email requests fail or queue\n• Payment and Inventory still have their 20 threads each, completely unaffected\n• The failure is contained to one bulkhead\n\n<b>Bulkhead + Circuit Breaker (used together)</b>\n• Bulkhead limits damage while the circuit is still open (while calls are still being attempted)\n• Circuit Breaker eventually detects failure and stops calling the service entirely\n• Together: bulkhead contains damage, circuit breaker stops it\n\n<b>When to use it</b>\n• Any service that calls multiple external dependencies\n• When one slow dependency should not be allowed to affect others\n• Especially critical when some calls are more important than others (payments vs emails)\n\n<b>Simple Rule</b>\n• Without bulkheads: one slow service can exhaust your entire thread pool\n• With bulkheads: each service has its own pool — failures stay isolated",
    whyItMatters:
      "In a system with shared resources, one slow external service can exhaust the entire thread pool and take down healthy services too. Bulkheads contain the blast radius — only the affected service's pool fills up, everything else keeps running.\n\nIf you choose wrong:\n• Shared thread pool for all dependencies = one slow email provider can make payments fail too\n• No bulkheads + no circuit breaker = a single failing service cascades into a full outage",
    diagramNote:
      "Fig. 54.1 — Without bulkhead: shared thread pool exhausted by slow email service blocks all requests. With bulkhead: per-service pools isolate failures.",
    example:
      "<b>1. E-commerce checkout (without bulkheads)</b>\nYour checkout calls Payment, Inventory, and Email using a shared pool of 50 threads. The email provider starts timing out. 50 threads pile up waiting for email responses. Now when a payment request comes in, there are no threads left. Payments fail — not because the payment service is broken, but because Email consumed all the shared resources.\n\n<b>2. E-commerce checkout (with bulkheads)</b>\nPayment gets 20 threads, Inventory gets 20, Email gets 10. Email times out and fills its 10 threads. Payment and Inventory still have their full 20 threads each. Users can complete purchases. Email notifications are delayed or dropped — a much better outcome than failed payments.",
    interviewTip:
      "\"I'd use bulkheads to give each downstream service its own connection pool — so a slow email provider can't exhaust the threads needed for payment processing.\"\n\n\"I'd combine bulkheads with a circuit breaker: the bulkhead limits damage while failures are below the threshold, the circuit breaker kicks in and stops calls entirely once failure rate gets too high.\"\n\nThe key insight: bulkheads answer 'what happens while the circuit is still closed?' — they limit damage before the breaker trips.",
    tags: ["Bulkhead Pattern", "Thread Pool", "Resource Isolation", "Fault Tolerance", "Resilience", "Circuit Breaker", "Blast Radius"],
  },
  {
    id: 55,
    section: "Advanced Topics",
    title: "Event Sourcing",
    tagline: "Store state changes as events, not current state",
    description:
      "Event sourcing stores every state change as an immutable event — instead of overwriting the current state, you append a new event that describes what changed.\n\n<b>Traditional CRUD vs Event Sourcing</b>\n• Traditional database: when an order moves from `pending` to `shipped`, you UPDATE the row — the old state is gone\n• Event sourcing: you APPEND a new event `OrderShipped` to an append-only log — nothing is ever overwritten\n  ↳ Current state is not stored directly; it is derived by replaying all events from the beginning\n\n<b>How It Works</b>\n• An order's full event history: `OrderPlaced` → `PaymentProcessed` → `OrderPacked` → `OrderShipped` → `OrderDelivered`\n• To get current state: replay all events in order\n• To get state at a specific moment: replay events up to that timestamp\n  ↳ \"What was the order status at 3pm on Tuesday?\" is a simple query — stop replaying at 3pm\n\n<b>Benefits</b>\n• <b>Complete audit trail</b> — every change is recorded with who did it and when; nothing is ever lost\n• <b>Temporal queries</b> — reconstruct state at any point in time\n• <b>Event replay</b> — if a downstream service missed events during downtime, replay from the last processed event\n• <b>Debug production issues</b> — replay events to reproduce the exact sequence that caused a bug\n\n<b>Challenges</b>\n• <b>Storage growth</b> — storing every event forever grows without bound\n  ↳ Solution: periodic snapshots — checkpoint the current state; replay starts from the snapshot, not event 1\n• <b>Event versioning</b> — changing the shape of an event after it has been stored requires migration strategies\n• <b>Eventual consistency</b> — read-side projections may lag behind the event log\n\n<b>Natural Fit With Kafka and CQRS</b>\n• Kafka stores events durably and replays them on demand — a natural event store\n• CQRS pairs with event sourcing: the write side appends events; the read side builds projections from those events",
    whyItMatters:
      "Traditional CRUD databases lose history — when a row is updated, the previous state is gone. Event sourcing makes every change observable and replayable, which is critical for financial systems, compliance audits, and debugging production incidents where you need to reconstruct exactly what happened and when.\n\nIf you choose wrong:\n• CRUD for financial data — a disputed transaction cannot be audited because the database only shows the current state\n• No snapshots — replaying 10 years of events to get an account balance takes minutes instead of milliseconds\n• No event versioning strategy — an event schema change breaks replay of older events and corrupts state reconstruction\n\nEvent sourcing connects directly to CQRS (read projections), Kafka (durable event store), and audit trail requirements.",
    diagramNote:
      "Fig. 55.1 — Event store appends immutable events; current state is a projection derived by replaying events from the log.",
    example:
      "A bank account implemented with event sourcing.\n\n<b>1. Setup</b>\n• No `balance` column exists — balance is always derived by replaying events\n• Events stored in order: `AccountOpened($1000)`, `MoneyDeposited($500)`, `MoneyWithdrawn($200)`, `MoneyTransferred($300)`\n\n<b>2. What happens</b>\n• Current balance query: replay all events → 1000 + 500 − 200 − 300 = $1,000\n• A customer disputes a transaction from last Tuesday at 2:47pm — replay events up to that timestamp to reconstruct the exact state at that moment\n• The original transaction record cannot be modified — only a corrective event can be appended\n\n<b>3. The result</b>\n• A complete, tamper-proof audit trail exists for every dollar that ever moved through the account\n• Any state reconstruction question — for compliance, debugging, or dispute resolution — can be answered by replaying events to the relevant timestamp",
    interviewTip:
      "\"When they ask you to design a payment or financial system, propose event sourcing for the transaction ledger — it gives you an audit trail for compliance and lets you replay events to reconstruct state after a bug.\"\n\n• <b>1. Immutable events</b> — every state change is appended, never overwritten; events are the source of truth\n• <b>2. Current state</b> — derived by replaying events; use a snapshot every N events so replay starts from a checkpoint, not event 1\n• <b>3. Temporal queries</b> — replay events up to timestamp T to answer \"what was the state at time T?\"\n• <b>4. Kafka integration</b> — Kafka acts as the durable, replayable event store; consumers rebuild projections on demand\n• <b>5. CQRS pairing</b> — write side appends events; read side builds denormalized projections by consuming those events\n\nThe most common follow-up is \"how do you handle growing storage?\" — answer: periodic snapshots checkpoint the current state; full replay is only needed for the events since the last snapshot.",
    tags: ["Event Sourcing", "Immutable Events", "Audit Trail", "Replay", "Temporal Query", "CQRS", "Kafka", "Append-only Log"],
  },
  {
    id: 56,
    section: "Advanced Topics",
    title: "CQRS",
    tagline: "Separate the read model from the write model",
    description:
      "CQRS (Command Query Responsibility Segregation) separates the model used for writing data from the model used for reading it — each optimized independently for its workload.\n\n<b>The Problem With a Single Model</b>\n• In a traditional system, one database serves both reads and writes\n• Writes need: normalized data, ACID transactions, strict constraints — PostgreSQL is ideal\n• Reads need: denormalized, pre-joined, cached data that returns in milliseconds — a search index or Redis is ideal\n  ↳ A single data model cannot be optimal for both; at scale they compete for the same resources\n\n<b>CQRS Split</b>\n• <b>Write side (Commands)</b>: handles state-changing operations — `PlaceOrder`, `UpdateProfile`, `PostTweet`\n  ↳ Enforces business rules and consistency; writes to a normalized database (PostgreSQL)\n• <b>Read side (Queries)</b>: handles data retrieval — serves from a denormalized projection, Redis cache, or Elasticsearch index\n  ↳ Never touches the write database; built for fast reads only\n\n<b>How the Read Model Stays in Sync</b>\n• When the write side processes a command, it publishes an event: `OrderPlaced`, `ProductPriceUpdated`\n• The read side subscribes to these events and updates its projections asynchronously\n  ↳ This introduces eventual consistency — the read model may lag by milliseconds\n• Different read models can be built for different consumers without affecting the write path\n  ↳ Mobile app: Redis cache of user feed; Admin dashboard: Elasticsearch index; Analytics: data warehouse\n\n<b>CQRS + Event Sourcing</b>\n• The write side stores immutable events (event sourcing)\n• The read side builds projections by consuming those events\n  ↳ Any read model can be rebuilt from scratch by replaying the event log\n\n<b>When to Use CQRS</b>\n• Read-to-write ratio is heavily skewed (100K reads/sec vs 1K writes/sec)\n• Different read patterns for different consumers\n• Need to scale reads and writes independently",
    whyItMatters:
      "When the same database serves 100,000 reads per second and 1,000 writes per second, those workloads compete for connections, locks, and I/O. CQRS decouples them — the write model stays in PostgreSQL for ACID guarantees; the read model moves to Redis or Elasticsearch for speed. Each scales independently.\n\nIf you choose wrong:\n• Single model at high read volume — complex join queries for the read path slow down write transactions on the same tables\n• No event propagation — the read model goes stale immediately after writes; users see outdated data with no mechanism to update it\n• CQRS on a simple CRUD app — massive added complexity (two data stores, event propagation, eventual consistency) for no benefit\n\nCQRS connects directly to Event Sourcing (events as the sync mechanism), Kafka (propagating events from write to read), and denormalization design.",
    diagramNote:
      "Fig. 56.1 — CQRS: commands go to the write model (normalized DB); events publish to the read model (denormalized projection/cache). Read queries never touch the write DB.",
    example:
      "E-commerce product catalog with 100:1 read/write ratio.\n\n<b>1. Setup</b>\n• Write side: PostgreSQL — normalized, ACID, enforces inventory constraints\n• Read side: Elasticsearch — denormalized product documents with `category`, `price`, `rating`, and `stock_status` all in one document\n\n<b>2. What happens</b>\n• A product price changes → write side updates PostgreSQL → publishes `ProductPriceUpdated` event\n• The read model consumer receives the event and updates the Elasticsearch document within 200ms\n• 99% of traffic — product search and listing pages — is served entirely from Elasticsearch\n\n<b>3. The result</b>\n• The PostgreSQL write database handles ~1,000 price/inventory updates per second\n• Elasticsearch handles ~100,000 search and listing reads per second — completely decoupled\n• If the Elasticsearch index ever needs to be rebuilt (new schema, new fields), it can be reconstructed by replaying the event log from scratch without touching the write database",
    interviewTip:
      "\"When they ask you to design a system with very different read and write patterns, propose CQRS — it lets you optimize each side independently.\"\n\n• <b>1. Write model</b> — PostgreSQL for ACID guarantees, normalized schema, strict constraints\n• <b>2. Read model</b> — Redis cache or Elasticsearch for denormalized, fast reads; built for the specific query patterns of each consumer\n• <b>3. Sync mechanism</b> — write side publishes events; read side consumes them asynchronously; lag is typically under 200ms\n• <b>4. Eventual consistency tradeoff</b> — always state this explicitly: \"the read model may show data that is up to 200ms stale\"\n• <b>5. Rebuild capability</b> — if the read model needs to change, replay events to rebuild it without downtime\n\nThe most common follow-up is \"how do you handle the read model being stale?\" — answer: for most reads (product listings, feeds), 200ms lag is acceptable; for critical reads (inventory before checkout), query the write model directly.",
    tags: ["CQRS", "Command Query Responsibility Segregation", "Read Model", "Write Model", "Event Sourcing", "Eventual Consistency", "Projection"],
  },
  {
    id: 57,
    section: "Reliability",
    title: "Heartbeat and Health Checks",
    tagline: "How distributed systems detect and recover from failures",
    description:
      "In a distributed system, services can fail silently — a process crashes, a network partition happens, or a service freezes without telling anyone. Heartbeats and health checks are how the system detects these silent failures automatically.\n\n<b>Heartbeat</b>\nA heartbeat is a regular signal a service sends to say \"I'm still alive.\"\n• The service pings a central registry or load balancer every few seconds\n• If the registry misses 3 consecutive heartbeats, it marks the service as down\n• Traffic stops being routed to that service automatically\n\n<b>Health Checks</b>\nHealth checks go deeper than just \"is the process running?\" They verify the service is actually ready to do work.\n\nTwo types (used in Kubernetes and most load balancers):\n\n• <b>Liveness probe</b> — is the process alive and not stuck?\n  ↳ If this fails, restart the container\n  ↳ Example: GET /health/live returns 200 if the process is running\n\n• <b>Readiness probe</b> — is the service ready to accept traffic?\n  ↳ If this fails, remove from load balancer but don't restart\n  ↳ Example: GET /health/ready returns 200 only when DB connection and cache are reachable\n\n<b>Active vs Passive health checks</b>\n• <b>Active</b>: the load balancer sends synthetic pings to each server on a schedule — catches silent failures faster\n• <b>Passive</b>: the load balancer monitors real traffic — if a server returns too many errors, it's marked unhealthy\n\n<b>Simple Rule</b>\n• Liveness = is it alive? → restart if not\n• Readiness = is it ready? → stop sending traffic if not (but don't restart)",
    whyItMatters:
      "Without health checks, a load balancer keeps sending traffic to crashed or unready servers — users get errors. Health checks enable automatic failover and zero-downtime deployments.\n\nIf you choose wrong:\n• No readiness probe = new deployments receive traffic before they finish starting up, causing errors for users\n• No liveness probe = a deadlocked process keeps running but never responds, silently failing forever",
    diagramNote:
      "Fig. 57.1 — Heartbeat flow: service pings registry every 10s; 3 missed heartbeats mark it unhealthy; load balancer stops routing traffic to it.",
    example:
      "<b>1. Zero-downtime deployment (readiness probe)</b>\nA new version of your API starts up. It takes 5 seconds to connect to the database and warm up the connection pool. During those 5 seconds, GET /health/ready returns 503. Kubernetes keeps the new pod out of the load balancer. After 5 seconds, /health/ready returns 200 and traffic starts flowing. Users never hit the unready pod.\n\n<b>2. Recovering from a crash (liveness probe)</b>\nYour app server hits a deadlock and stops responding. GET /health/live returns a timeout. Kubernetes detects 3 consecutive failures and restarts the container. Within 10 seconds the pod is back and serving traffic. Without the liveness probe, the dead container would stay running forever — looking healthy on the outside but never responding.",
    interviewTip:
      "\"Every service I build exposes two health endpoints: GET /health/live for liveness (is the process running?) and GET /health/ready for readiness (are all dependencies reachable?).\"\n\n\"Kubernetes uses the same endpoints — livenessProbe restarts on failure, readinessProbe removes from the load balancer until healthy.\"\n\nAlways separate liveness from readiness in your answer. Interviewers who've operated production systems will notice if you conflate them.",
    tags: ["Heartbeat", "Health Check", "Liveness Probe", "Readiness Probe", "Kubernetes", "Load Balancer", "Failover", "High Availability"],
  },
  {
    id: 58,
    section: "Advanced Topics",
    title: "Distributed Tracing",
    tagline: "Follow a request as it flows across every microservice",
    description:
      "Distributed tracing tracks a single user request as it flows through every service in a microservices system, capturing how long each hop took and where errors occurred.\n\n<b>The Problem Without Tracing</b>\n• A single checkout request might touch 10–20 services: API Gateway → Auth → Cart → Inventory → Payment → Notification\n• When that request takes 3 seconds, which service is slow?\n  ↳ Without tracing, you must grep logs across all 10 services and manually correlate timestamps — painful and slow\n\n<b>How Distributed Tracing Works</b>\n• When a request arrives at the API Gateway, a unique <b>trace ID</b> is generated (UUID)\n• The trace ID is injected into every outgoing request header: `X-Trace-ID` or the W3C `traceparent` standard\n• Every service that handles the request creates a <b>span</b> — a record with:\n  ↳ Service name, operation name\n  ↳ Start time and end time\n  ↳ Parent span ID (which span triggered this one)\n  ↳ Trace ID (same for all spans in this request)\n• All spans share one trace ID; they are collected and assembled into a <b>trace tree</b>\n\n<b>What the Trace Tree Shows</b>\n• Which services were called sequentially vs. in parallel\n• How long each service and each operation took\n• Where errors occurred and at which hop\n• The <b>critical path</b> — the longest sequential chain of spans, which determined total latency\n\n<b>Tools</b>\n• Jaeger (open-source, CNCF), Zipkin (open-source, originally from Twitter)\n• AWS X-Ray (managed), Datadog APM, Honeycomb\n\n<b>Observability Trio</b>\n• Logs — what happened (event records)\n• Metrics — how much / how fast (aggregated numbers)\n• Traces — where time was spent across services (end-to-end request flow)\n  ↳ A trace ID links all three: filter logs by trace ID, see which metric spike matches the slow trace",
    whyItMatters:
      "In a monolith, a slow database query shows up in a stack trace on one server. In microservices, the slow operation is one hop in a chain of 15 services — completely invisible without distributed tracing. Tracing is what makes distributed systems debuggable in production.\n\nIf you choose wrong:\n• No trace ID propagation — a slow request spans 15 services with no way to correlate their logs; debugging takes hours\n• No spans — you know a request was slow but cannot identify which service or which operation caused it\n• Traces without logs correlation — you see the slow span but cannot read the error details that caused it\n\nDistributed tracing connects directly to API Gateway design (trace ID generation), logging (correlation by trace ID), and observability architecture.",
    diagramNote:
      "Fig. 58.1 — Distributed trace: a single request generates spans across 5 services; the waterfall view shows which service added the most latency.",
    example:
      "A checkout request taking 3 seconds — identified with Jaeger.\n\n<b>1. Setup</b>\n• Every request gets a trace ID at the API Gateway: `trace-8f2a1c`\n• Each service creates a span and reports it to the Jaeger collector\n\n<b>2. What happens</b>\n• The trace tree is assembled from all spans sharing `trace-8f2a1c`:\n  ↳ API Gateway: 5ms\n  ↳ Auth Service: 10ms\n  ↳ Cart Service: 20ms\n  ↳ Inventory Service: 2,800ms ← critical path bottleneck\n  ↳ Payment Service: 100ms\n• The Inventory Service span shows it ran a full table scan with no index on the `product_id` column\n\n<b>3. The result</b>\n• The bottleneck is identified in 30 seconds by pulling up the trace in Jaeger\n• Without tracing, finding this would require grepping logs across 5 services and manually correlating timestamps",
    interviewTip:
      "\"When they ask you to design a microservices system, add distributed tracing as part of your observability stack — it's what makes the system debuggable in production.\"\n\n• <b>1. Trace ID at the gateway</b> — generated once when the request enters the system; injected into every downstream call as `X-Trace-ID`\n• <b>2. Spans per service</b> — each service creates a span with start time, end time, service name, and parent span ID\n• <b>3. Trace tree</b> — all spans sharing the trace ID are assembled into a waterfall view showing latency at every hop\n• <b>4. Critical path</b> — the longest sequential chain of spans; this is what determines total request latency\n• <b>5. Observability trio</b> — logs + metrics + traces; filter logs by trace ID to correlate events with the slow span\n\nA common follow-up is \"which tool would you use?\" — answer: Jaeger or Zipkin for open-source; Datadog APM for managed with built-in log and metric correlation; AWS X-Ray if already on AWS.",
    tags: ["Distributed Tracing", "Jaeger", "Zipkin", "Span", "Trace ID", "Observability", "Microservices", "Latency"],
  },
  {
    id: 59,
    section: "Reliability",
    title: "Logging vs Metrics vs Tracing",
    tagline: "The three pillars of observability",
    description:
      "Observability means being able to understand what is happening inside a system just by looking at its outputs. The three pillars each answer a different question.\n\n<b>Logging — What happened?</b>\nLogs record individual events as structured text.\n• \"User 123 placed order 456 at 14:32\"\n• \"Database query timed out after 5000ms\"\n• \"Cache miss for key product:789\"\n\nEach log entry is timestamped and describes one specific event. Logs are best for debugging exact errors and auditing user actions. They are expensive to store and search at scale.\n• Tools: ELK Stack (Elasticsearch + Logstash + Kibana), Loki, Splunk\n\n<b>Metrics — How many? How fast? How much?</b>\nMetrics are numbers measured over time.\n• Requests per second: 1,200\n• Error rate: 0.3%\n• P99 latency: 340ms\n• CPU usage: 72%\n\nMetrics are cheap to store as time series. Dashboards and alerts are built on metrics. They show trends and catch regressions early.\n• Tools: Prometheus (collection), Grafana (dashboards and alerts)\n\n<b>Tracing — Which path did it take?</b>\nA trace tracks one request as it moves through multiple services.\n• API Gateway (5ms) → Auth Service (10ms) → Cart Service (20ms) → Inventory Service (2800ms)\n• Shows exactly which service in the chain is slow\n\nEach service adds a span to the trace. All spans share one trace ID so you can reconstruct the full journey.\n• Tools: Jaeger, Zipkin, AWS X-Ray\n\n<b>How they work together</b>\n• Metrics show something is wrong: P99 latency spiked\n• Logs explain what is wrong: \"InventoryService: connection pool exhausted\"\n• Tracing shows where it's wrong: the Inventory Service span took 2.8 seconds\n\n<b>Simple Rule</b>\n• Metrics → detect the problem\n• Logs → understand the problem\n• Traces → locate the problem",
    whyItMatters:
      "Each pillar catches a different type of problem. Metrics detect regressions. Logs diagnose specific errors. Traces locate failures in distributed request chains. Without all three you are blind to different failure modes.\n\nIf you choose wrong:\n• Only logging = you can see errors but not trends or overall system health\n• Only metrics = you see something is wrong but can't tell what caused it or where\n• No tracing in microservices = a slow service in a chain of 10 is invisible without hours of manual log digging",
    diagramNote:
      "Fig. 59.1 — Observability triangle: metrics (Prometheus) detect anomalies, logs (ELK) explain them, traces (Jaeger) localize them in distributed request chains.",
    example:
      "<b>1. Diagnosing a P99 spike with all three pillars</b>\nA Prometheus alert fires — P99 latency is above 500ms. You open Grafana: the spike started at 14:32 and affects only the checkout endpoint. You filter Loki logs for 14:32 and find \"InventoryService: connection pool exhausted.\" You pull a Jaeger trace from 14:32 and confirm the Inventory Service span is 2.8 seconds. Incident resolved in 10 minutes using all three pillars together.\n\n<b>2. Only metrics (limited)</b>\nP99 latency spiked. You know something is wrong but not which service, which query, or which user is affected. You spend an hour guessing. This is why logs and tracing exist.",
    interviewTip:
      "\"For observability I'd use Prometheus + Grafana for metrics, ELK for structured logs, and Jaeger for distributed tracing.\"\n\n\"Alerts fire on metrics — P99 latency and error rate. When an alert fires, logs and traces are the tools I use to diagnose the root cause.\"\n\nName all three pillars and their tools. Saying \"I'd add logging\" is a junior answer. The senior answer names the full observability stack and explains when to use each.",
    tags: ["Logging", "Metrics", "Tracing", "Observability", "Prometheus", "Grafana", "ELK", "Jaeger", "Three Pillars"],
  },
  {
    id: 60,
    section: "Reliability",
    title: "Disaster Recovery and RPO/RTO",
    tagline: "How fast you recover and how much data you can afford to lose",
    description:
      "Disaster recovery (DR) is a plan for getting your system back online after a major failure — a data centre outage, a cyberattack, or catastrophic data loss. Two numbers define your requirements.\n\n<b>RPO — Recovery Point Objective</b>\nThe maximum amount of data you can afford to lose, measured in time.\n• RPO = 1 hour means: after a disaster, you can restore to a point up to 1 hour ago — losing at most 1 hour of data\n• RPO drives how often you back up: RPO = 15 minutes → backups every 15 minutes\n• Lower RPO = more frequent backups = more expensive\n\n<b>RTO — Recovery Time Objective</b>\nThe maximum amount of time your system can be down before it must be back online.\n• RTO = 4 hours means: the system must be fully working within 4 hours of declaring a disaster\n• RTO drives your recovery infrastructure: faster RTO = more expensive hot standbys\n• Lower RTO = faster recovery = more expensive\n\n<b>Four recovery strategies (cheapest to most expensive)</b>\n\n• <b>Backup and Restore</b>: snapshots saved to storage (like S3). RTO = hours, RPO = backup interval. Cheapest.\n• <b>Pilot Light</b>: a minimal version of critical infrastructure runs in a second region. RTO = minutes.\n• <b>Warm Standby</b>: a scaled-down production copy runs continuously in a DR region. RTO = minutes.\n• <b>Active-Active Multi-Region</b>: full production runs in two or more regions simultaneously. RTO = seconds, RPO = seconds. Most expensive.\n\n<b>Simple Rule</b>\n• The lower your RPO and RTO, the more it costs\n• Match your investment to how much downtime and data loss your business can actually tolerate",
    whyItMatters:
      "Every system fails eventually. Without defined RPO and RTO targets, you don't know if your recovery plan is good enough until disaster actually happens.\n\nIf you choose wrong:\n• No DR plan = when the data centre goes down, you have no way to recover quickly\n• Active-Active for a developer blog = massively over-engineered and expensive for a system that can tolerate hours of downtime",
    diagramNote:
      "Fig. 60.1 — RPO defines maximum data loss (backup interval); RTO defines maximum downtime (recovery speed). Multi-region active-active achieves near-zero for both.",
    example:
      "<b>1. Fintech (low RPO/RTO)</b>\nRPO = 0 seconds (no data loss), RTO = 30 seconds (back online almost immediately). Solution: synchronous replication to a second region + automated DNS failover. Cost: high. Justified because losing even one transaction is unacceptable.\n\n<b>2. Content blog (high RPO/RTO acceptable)</b>\nRPO = 24 hours (daily backup is fine), RTO = 4 hours (half a day to restore is acceptable). Solution: daily snapshots to S3, manual restore process. Cost: minimal. Justified because a blog being down for a few hours causes no financial damage.\n\nThe same business logic applies to both — match the recovery investment to the actual cost of downtime.",
    interviewTip:
      "\"For this payment service I'd target RPO = 1 minute and RTO = 5 minutes — that means synchronous replication to a hot standby in a second availability zone, automated DNS failover via health checks, and point-in-time backups to S3.\"\n\n\"For a lower-criticality service I'd accept RPO = 24 hours and RTO = 4 hours — daily snapshots to S3 with a documented manual restore runbook.\"\n\nAlways state RPO and RTO numbers first, then justify each infrastructure component against those targets. This shows that your design decisions are driven by business requirements, not just technical preference.",
    tags: ["Disaster Recovery", "RPO", "RTO", "Backup", "Multi-Region", "Failover", "Business Continuity", "High Availability"],
  },
  {
    id: 61,
    section: "Foundation",
    title: "How to Design a Scalable System",
    tagline: "10 key principles for building systems that grow with you",
    description:
      "Scalability is the ability to handle growing load — more users, more data, more requests — without degrading performance or reliability. Every scalable system applies a core set of principles.\n\n(1) Load balance traffic: distribute requests across multiple servers. A single server is both a bottleneck and a single point of failure. Add a load balancer in front of a horizontally scaled server pool.\n\n(2) Cache aggressively: serve frequently accessed data from memory (Redis, Memcached) to avoid repeated database queries. Apply caching at multiple layers: CDN edge, API gateway, application, database query cache.\n\n(3) Shard data: split large datasets across multiple database nodes using consistent hashing or range partitioning. No single database node should hold all data or handle all writes.\n\n(4) Use async processing: decouple slow or non-critical operations using message queues (Kafka, SQS). Order confirmation emails, image resizing, analytics events — none of these need to block the user's request.\n\n(5) Separate concerns with microservices: decompose the application into independent services. Scale only the services that are under load instead of scaling the entire application.\n\n(6) Monitor and alert: use metrics (Prometheus), logs (ELK), and traces (Jaeger) to detect problems before users do. Set P99 latency and error rate alerts.\n\n(7) Plan for failures: assume components will fail. Add circuit breakers, retries with exponential backoff, bulkheads, and replica failover. Design for graceful degradation.\n\n(8) Use CDN for static content: serve images, CSS, JS, and videos from edge servers near users. Reduce origin load by 80–95%.\n\n(9) Optimize databases: use indexes, read replicas, connection pooling, and query optimization before reaching for scaling solutions. Most database problems are query problems.\n\n(10) Document architecture: maintain up-to-date architecture diagrams and runbooks. Systems that aren't documented are systems that can't be operated under pressure.",
    whyItMatters:
      "These 10 principles are the checklist that turns a single-server prototype into a production system capable of serving millions of users. Each principle addresses a specific class of failure at scale — missing any one of them creates a predictable bottleneck.",
    diagramNote:
      "Fig. 61.1 — Scalable system anatomy: load balancer → stateless app servers → cache layer → sharded databases → async queue → CDN for static assets.",
    example:
      "Instagram's early architecture applied all 10 principles: Django app servers behind a load balancer, PostgreSQL sharded by user_id, Redis for feed cache and session storage, Gearman for async photo processing, Akamai CDN for images, Graphite for metrics, HAProxy for load balancing — serving 30 million users with 13 engineers before the Facebook acquisition.",
    interviewTip:
      "Use these 10 principles as a checklist at the end of any system design: 'Let me review against scalability principles: am I load balancing? Yes — ALB. Am I caching? Yes — Redis for hot data. Am I sharding? Yes — user_id hash across 50 shards. Am I using async? Yes — Kafka for notifications and analytics. Do I have monitoring? Yes — Prometheus with P99 alerts.' Walking through this checklist shows systematic thinking.",
    tags: ["Scalability", "Load Balancing", "Caching", "Sharding", "Async", "Microservices", "CDN", "Monitoring", "Best Practices"],
  },
];

export const CONCEPT_COUNT = SYSTEM_DESIGN_CONCEPTS.length;
