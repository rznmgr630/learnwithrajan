import { normalizePastedLessonDay } from "@/lib/learn/normalize-pasted-lesson-day";

const PASTED_CONTENT = "# Day 3 — Navigation & Linking\n\n**Difficulty:** Beginner\n**Estimated time:** 75–90 minutes\n**Project:** Multi-page Documentation Site\n\n### Day Goal\n\nBy the end of Day 3, you should understand:\n\n* Why client-side navigation matters\n* The `Link` component\n* Internal vs external links\n* Dynamic routes\n* Dynamic route parameters\n* Catch-all routes\n* `useRouter`\n* `usePathname`\n* `useSearchParams`\n* Query strings\n* Programmatic navigation\n* Redirects\n* Navigation behavior\n* When to use each navigation API\n\n---\n\n# Section 1 — Understanding Navigation in Next.js\n\n## Explanation\n\nNavigation means moving from one URL to another.\n\nIn a traditional website, clicking a link may cause the browser to request a completely new document:\n\n```text\nBrowser\n   │\n   │ GET /about\n   ▼\nServer\n   │\n   │ HTML\n   ▼\nBrowser\n```\n\nNext.js can perform navigation without completely reloading the browser document.\n\nFor internal application navigation, Next.js can use the existing application and load the required route.\n\nThis is commonly called **client-side navigation**.\n\nThe important distinction is:\n\n```text\nTraditional navigation\n    ↓\nBrowser requests a new document\n\nNext.js client-side navigation\n    ↓\nNext.js handles the transition\n    ↓\nOnly the required route content is loaded\n```\n\nThis can make navigation feel faster and preserve parts of the existing UI.\n\n---\n\n## Visual Diagram\n\n```text\nTraditional Navigation\n\nClick /about\n     │\n     ▼\nBrowser\n     │\n     ▼\nNew HTTP Request\n     │\n     ▼\nServer\n     │\n     ▼\nNew Document\n     │\n     ▼\nBrowser\n```\n\n```text\nNext.js Navigation\n\nClick /about\n     │\n     ▼\nNext.js Router\n     │\n     ▼\nNavigate to /about\n     │\n     ▼\nLoad required route\n     │\n     ▼\nUpdate UI\n```\n\n---\n\n## Code Example\n\nA normal HTML link looks like:\n\n```tsx\n<a href=\"/about\">\n  About\n</a>\n```\n\nNext.js provides the `Link` component:\n\n```tsx\nimport Link from \"next/link\";\n\nexport default function Navigation() {\n  return (\n    <nav>\n      <Link href=\"/\">Home</Link>\n      <Link href=\"/about\">About</Link>\n      <Link href=\"/projects\">Projects</Link>\n    </nav>\n  );\n}\n```\n\nFor internal navigation, `Link` is normally the preferred choice.\n\n---\n\n## Key Takeaways\n\n* Navigation changes the current URL.\n* Next.js supports client-side navigation.\n* Internal links should generally use `Link`.\n* Client-side navigation can avoid a full document reload.\n* Navigation and rendering are related but different concepts.\n\n---\n\n## Common Mistakes\n\n### Mistake 1 — Using `<a>` everywhere\n\nFor internal Next.js routes, prefer:\n\n```tsx\n<Link href=\"/about\">\n  About\n</Link>\n```\n\ninstead of:\n\n```tsx\n<a href=\"/about\">\n  About\n</a>\n```\n\n### Mistake 2 — Thinking `Link` is required for external websites\n\nFor an external website:\n\n```tsx\n<a\n  href=\"https://github.com\"\n  target=\"_blank\"\n  rel=\"noreferrer\"\n>\n  GitHub\n</a>\n```\n\nis appropriate.\n\n---\n\n## Mini Quiz\n\n1. What is client-side navigation?\n2. Why does Next.js provide `Link`?\n3. Should you normally use `Link` for an external website?\n4. What is the difference between `<a>` and `Link`?\n\n---\n\n# Section 2 — The `Link` Component\n\n## Explanation\n\n`Link` is the primary navigation component provided by Next.js.\n\nImport it from:\n\n```tsx\nimport Link from \"next/link\";\n```\n\nBasic usage:\n\n```tsx\n<Link href=\"/about\">\n  About\n</Link>\n```\n\nThe `href` identifies where the user should navigate.\n\nYou can also place more complex content inside `Link`.\n\n```tsx\n<Link href=\"/projects\">\n  <div>\n    <h2>Projects</h2>\n    <p>View my projects</p>\n  </div>\n</Link>\n```\n\nThis makes the entire element clickable.\n\n---\n\n## Visual Diagram\n\n```text\n<Link>\n   │\n   ├── href=\"/projects\"\n   │\n   ▼\nNext.js Router\n   │\n   ▼\n/projects\n   │\n   ▼\nProjects Page\n```\n\n---\n\n## Code Example\n\nA navigation bar:\n\n```tsx\nimport Link from \"next/link\";\n\nexport default function Navbar() {\n  return (\n    <nav>\n      <Link href=\"/\">Home</Link>\n\n      <Link href=\"/docs\">\n        Documentation\n      </Link>\n\n      <Link href=\"/projects\">\n        Projects\n      </Link>\n\n      <Link href=\"/contact\">\n        Contact\n      </Link>\n    </nav>\n  );\n}\n```\n\nYou can also use:\n\n```tsx\n<Link href=\"/projects/123\">\n  View Project\n</Link>\n```\n\n---\n\n## Key Takeaways\n\n* `Link` comes from `next/link`.\n* Use it for internal navigation.\n* `href` determines the destination.\n* `Link` can contain text, buttons-like UI, cards, images, etc.\n\n---\n\n## Common Mistakes\n\n### Mistake 1\n\nWriting:\n\n```tsx\nimport { Link } from \"next/link\";\n```\n\nIncorrect.\n\nUse:\n\n```tsx\nimport Link from \"next/link\";\n```\n\n### Mistake 2\n\nUsing an absolute URL for a local route:\n\n```tsx\n<Link href=\"https://mywebsite.com/about\">\n```\n\nFor internal routes, use:\n\n```tsx\n<Link href=\"/about\">\n```\n\n---\n\n## Mini Quiz\n\n1. Where does `Link` come from?\n2. What does `href` specify?\n3. How would you link to `/docs/getting-started`?\n4. Can a `Link` contain a complex UI instead of just text?\n\n---\n\n# Section 3 — Dynamic Routes\n\n## Explanation\n\nSometimes you don't know the exact URL ahead of time.\n\nFor example:\n\n```text\n/projects/1\n/projects/2\n/projects/3\n```\n\nYou don't want to create:\n\n```text\napp/projects/1/page.tsx\napp/projects/2/page.tsx\napp/projects/3/page.tsx\n```\n\nInstead, Next.js provides **dynamic route segments**.\n\nCreate:\n\n```text\napp/\n└── projects/\n    └── [id]/\n        └── page.tsx\n```\n\nThe `[id]` means:\n\n> This part of the URL is dynamic.\n\n---\n\n## Visual Diagram\n\n```text\napp/projects/[id]/page.tsx\n              │\n              ▼\n         Dynamic segment\n              │\n       ┌──────┼──────┐\n       ▼      ▼      ▼\n    /1       /2      /3\n```\n\nOne page can handle:\n\n```text\n/projects/1\n/projects/2\n/projects/100\n```\n\n---\n\n## Code Example\n\n```tsx\ntype ProjectPageProps = {\n  params: Promise<{\n    id: string;\n  }>;\n};\n\nexport default async function ProjectPage({\n  params,\n}: ProjectPageProps) {\n  const { id } = await params;\n\n  return (\n    <main>\n      <h1>Project {id}</h1>\n    </main>\n  );\n}\n```\n\nFor example:\n\n```text\n/projects/42\n```\n\nproduces:\n\n```text\nid = \"42\"\n```\n\nNotice that route parameters are URL values, so you should not automatically assume they are numbers.\n\n---\n\n## Key Takeaways\n\n* `[id]` creates a dynamic route segment.\n* One page can handle many URLs.\n* Route parameters come from the URL.\n* URL parameters are strings unless you explicitly convert them.\n\n---\n\n## Common Mistakes\n\n### Mistake 1 — Assuming the ID is a number\n\nThis:\n\n```tsx\nconst id: number = params.id;\n```\n\nis incorrect if `params.id` is a string.\n\nConvert it when appropriate:\n\n```tsx\nconst id = Number(params.id);\n```\n\n### Mistake 2 — Creating hundreds of static folders\n\nDon't create:\n\n```text\n1/\n2/\n3/\n4/\n...\n```\n\nUse:\n\n```text\n[id]/\n```\n\n---\n\n## Mini Quiz\n\n1. What does `[id]` mean?\n2. What URL does `app/products/[id]/page.tsx` handle?\n3. Is `id` automatically a number?\n4. Why are dynamic routes useful?\n\n---\n\n# Section 4 — Dynamic Route Parameters\n\n## Explanation\n\nA dynamic route gives the page access to the value captured by the URL.\n\nConsider:\n\n```text\napp/\n└── products/\n    └── [id]/\n        └── page.tsx\n```\n\nVisiting:\n\n```text\n/products/123\n```\n\nmeans:\n\n```text\nid = \"123\"\n```\n\nThe parameter can then be used to retrieve data.\n\n---\n\n## Visual Diagram\n\n```text\nURL\n\n/products/123\n       │\n       ▼\nDynamic segment [id]\n       │\n       ▼\nparams.id\n       │\n       ▼\n\"123\"\n       │\n       ▼\nDatabase/API lookup\n```\n\n---\n\n## Code Example\n\n```tsx\ntype ProductPageProps = {\n  params: Promise<{\n    id: string;\n  }>;\n};\n\nexport default async function ProductPage({\n  params,\n}: ProductPageProps) {\n  const { id } = await params;\n\n  return (\n    <main>\n      <h1>Product {id}</h1>\n    </main>\n  );\n}\n```\n\nYou can then use the parameter to retrieve a product:\n\n```tsx\nconst product = await getProduct(id);\n```\n\n---\n\n## Key Takeaways\n\n* Dynamic route parameters come from URL segments.\n* `[id]` captures one segment.\n* Parameters can be used for data fetching.\n* Treat URL parameters as untrusted input and validate them before sensitive operations.\n\n---\n\n## Common Mistakes\n\n### Mistake 1\n\nTrusting a URL parameter without validation.\n\nFor example:\n\n```text\n/products/abc\n```\n\nmay be invalid if your database expects numeric IDs.\n\n### Mistake 2\n\nConfusing route parameters with query parameters.\n\nThese are different:\n\n```text\n/products/123\n       ↑\nroute parameter\n```\n\nand:\n\n```text\n/products?page=2\n          ↑\nquery parameter\n```\n\n---\n\n## Mini Quiz\n\n1. What is a route parameter?\n2. What is the difference between `/products/123` and `/products?id=123`?\n3. Why should route parameters be validated?\n\n---\n\n# Section 5 — Catch-All Routes\n\n## Explanation\n\nSometimes a route needs to capture multiple URL segments.\n\nFor example, a documentation system might have:\n\n```text\n/docs/react\n/docs/react/hooks\n/docs/react/hooks/use-state\n```\n\nA normal `[slug]` only captures one segment.\n\nFor multiple segments, use:\n\n```text\n[...slug]\n```\n\nExample:\n\n```text\napp/\n└── docs/\n    └── [...slug]/\n        └── page.tsx\n```\n\n---\n\n## Visual Diagram\n\n```text\n/docs/react\n       ↓\n[\"react\"]\n\n/docs/react/hooks\n       ↓\n[\"react\", \"hooks\"]\n\n/docs/react/hooks/use-state\n       ↓\n[\"react\", \"hooks\", \"use-state\"]\n```\n\n---\n\n## Code Example\n\n```tsx\ntype DocsPageProps = {\n  params: Promise<{\n    slug: string[];\n  }>;\n};\n\nexport default async function DocsPage({\n  params,\n}: DocsPageProps) {\n  const { slug } = await params;\n\n  return (\n    <main>\n      <p>{slug.join(\" / \")}</p>\n    </main>\n  );\n}\n```\n\nFor:\n\n```text\n/docs/react/hooks/use-state\n```\n\nthe value becomes:\n\n```ts\n[\"react\", \"hooks\", \"use-state\"]\n```\n\n---\n\n## Key Takeaways\n\n* `[...slug]` captures multiple segments.\n* The parameter is an array.\n* Catch-all routes are useful for documentation and hierarchical content.\n\n---\n\n## Common Mistakes\n\n### Mistake 1\n\nExpecting:\n\n```tsx\nslug: string\n```\n\nFor catch-all routes, the value is an array:\n\n```tsx\nslug: string[]\n```\n\n### Mistake 2\n\nUsing a catch-all route when a normal dynamic route is sufficient.\n\nUse:\n\n```text\n[id]\n```\n\nfor one segment.\n\nUse:\n\n```text\n[...slug]\n```\n\nfor multiple segments.\n\n---\n\n## Mini Quiz\n\n1. What does `[...slug]` do?\n2. What type is `slug`?\n3. What would `slug` contain for `/docs/react/hooks`?\n4. Give one real-world use case for catch-all routes.\n\n---\n\n# Section 6 — `useRouter`\n\n## Explanation\n\nSometimes navigation needs to happen because of an action rather than a normal link.\n\nFor example:\n\n```text\nUser submits form\n       ↓\nSave data\n       ↓\nNavigate to dashboard\n```\n\nFor this, Next.js provides `useRouter`.\n\nImport:\n\n```tsx\nimport { useRouter } from \"next/navigation\";\n```\n\nBecause `useRouter` is a client-side hook, the component using it needs to be a Client Component.\n\n---\n\n## Visual Diagram\n\n```text\nUser Action\n    │\n    ▼\nSubmit Form\n    │\n    ▼\nSave Data\n    │\n    ▼\nrouter.push(\"/dashboard\")\n    │\n    ▼\nDashboard\n```\n\n---\n\n## Code Example\n\n```tsx\n\"use client\";\n\nimport { useRouter } from \"next/navigation\";\n\nexport default function CreateButton() {\n  const router = useRouter();\n\n  function handleCreate() {\n    // Create something...\n\n    router.push(\"/dashboard\");\n  }\n\n  return (\n    <button onClick={handleCreate}>\n      Create\n    </button>\n  );\n}\n```\n\n---\n\n## Key Takeaways\n\n`useRouter()` is useful for programmatic navigation.\n\nCommon methods include:\n\n```text\nrouter.push()\nrouter.replace()\nrouter.back()\nrouter.forward()\nrouter.refresh()\n```\n\nUse it when navigation is controlled by application logic.\n\n---\n\n## Common Mistakes\n\n### Mistake 1 — Importing from the wrong package\n\nFor the App Router:\n\n```tsx\nimport { useRouter } from \"next/navigation\";\n```\n\nDo not use the old Pages Router import:\n\n```tsx\nnext/router\n```\n\nfor App Router code.\n\n### Mistake 2 — Using `useRouter` for normal links\n\nDon't do:\n\n```tsx\n<button onClick={() => router.push(\"/about\")}>\n  About\n</button>\n```\n\nwhen a normal link is appropriate.\n\nPrefer:\n\n```tsx\n<Link href=\"/about\">\n  About\n</Link>\n```\n\n---\n\n## Mini Quiz\n\n1. What is `useRouter` used for?\n2. Is `useRouter` a server-side or client-side hook?\n3. What does `router.push()` do?\n4. When should you prefer `Link` instead?\n\n---\n\n# Section 7 — `router.push()` vs `router.replace()`\n\n## Explanation\n\nBoth methods navigate to another route, but they behave differently with browser history.\n\n### `push`\n\nAdds a new entry to browser history.\n\n```tsx\nrouter.push(\"/dashboard\");\n```\n\n### `replace`\n\nReplaces the current history entry.\n\n```tsx\nrouter.replace(\"/dashboard\");\n```\n\n---\n\n## Visual Diagram\n\n### `push`\n\n```text\nHistory\n\n/login\n   ↓\n/dashboard\n```\n\nPress Back:\n\n```text\n/dashboard\n      ↓\n/login\n```\n\n### `replace`\n\n```text\nHistory\n\n/login\n   ↓\nreplace\n   ↓\n/dashboard\n```\n\nThe `/login` history entry is replaced.\n\n---\n\n## Code Example\n\nAfter login:\n\n```tsx\nrouter.replace(\"/dashboard\");\n```\n\nThis can prevent the user from returning to the login page using the browser's Back button.\n\nNormal navigation:\n\n```tsx\nrouter.push(\"/projects\");\n```\n\nadds a new history entry.\n\n---\n\n## Key Takeaways\n\n```text\npush\n ↓\nAdd history entry\n\nreplace\n ↓\nReplace current history entry\n```\n\nChoose based on the desired browser-history behavior.\n\n---\n\n## Common Mistakes\n\nDon't automatically use `replace` everywhere.\n\nFor ordinary navigation:\n\n```tsx\nrouter.push(\"/about\");\n```\n\nis generally more appropriate.\n\nUse `replace` when the current URL should not remain in the history stack.\n\n---\n\n## Mini Quiz\n\n1. What does `router.push()` do to browser history?\n2. What does `router.replace()` do?\n3. Why might a login flow use `replace()`?\n\n---\n\n# Section 8 — `usePathname`\n\n## Explanation\n\n`usePathname()` allows a Client Component to read the current pathname.\n\nImport:\n\n```tsx\nimport { usePathname } from \"next/navigation\";\n```\n\nFor:\n\n```text\nhttps://example.com/docs/react\n```\n\nthe pathname is:\n\n```text\n/docs/react\n```\n\n---\n\n## Visual Diagram\n\n```text\nURL\n\nhttps://example.com/docs/react?tab=hooks\n       │\n       ├── pathname\n       │      ↓\n       │   /docs/react\n       │\n       └── query\n              ↓\n           tab=hooks\n```\n\n---\n\n## Code Example\n\n```tsx\n\"use client\";\n\nimport { usePathname } from \"next/navigation\";\n\nexport default function CurrentPath() {\n  const pathname = usePathname();\n\n  return (\n    <p>\n      Current path: {pathname}\n    </p>\n  );\n}\n```\n\n---\n\n## Key Takeaways\n\n* `usePathname()` reads the current pathname.\n* It is a Client Component hook.\n* It does not include the query string.\n\nFor:\n\n```text\n/products?page=2\n```\n\nit returns:\n\n```text\n/products\n```\n\n---\n\n## Common Mistakes\n\nDon't expect:\n\n```tsx\nusePathname()\n```\n\nto return:\n\n```text\n/products?page=2\n```\n\nIt returns:\n\n```text\n/products\n```\n\n---\n\n## Mini Quiz\n\nFor this URL:\n\n```text\n/products/123?tab=reviews\n```\n\nwhat does `usePathname()` return?\n\n---\n\n# Section 9 — `useSearchParams`\n\n## Explanation\n\nQuery parameters are values after `?` in a URL.\n\nExample:\n\n```text\n/products?page=2&sort=price\n```\n\nHere:\n\n```text\npage=2\nsort=price\n```\n\nare search parameters.\n\nNext.js provides:\n\n```tsx\nuseSearchParams()\n```\n\nto read them inside Client Components.\n\n---\n\n## Visual Diagram\n\n```text\n/products?page=2&sort=price\n        │\n        ├── page\n        │    ↓\n        │    2\n        │\n        └── sort\n             ↓\n            price\n```\n\n---\n\n## Code Example\n\n```tsx\n\"use client\";\n\nimport { useSearchParams } from \"next/navigation\";\n\nexport default function ProductFilters() {\n  const searchParams = useSearchParams();\n\n  const page = searchParams.get(\"page\");\n  const sort = searchParams.get(\"sort\");\n\n  return (\n    <div>\n      <p>Page: {page}</p>\n      <p>Sort: {sort}</p>\n    </div>\n  );\n}\n```\n\nFor:\n\n```text\n/products?page=2&sort=price\n```\n\nyou get:\n\n```text\npage = \"2\"\nsort = \"price\"\n```\n\n---\n\n## Key Takeaways\n\n* Query parameters appear after `?`.\n* `useSearchParams()` reads them in Client Components.\n* Values are strings.\n* Use validation/conversion when you expect another type.\n\n---\n\n## Common Mistakes\n\n### Mistake 1\n\nAssuming:\n\n```tsx\nsearchParams.get(\"page\")\n```\n\nreturns a number.\n\nIt returns:\n\n```text\n\"2\"\n```\n\nnot:\n\n```text\n2\n```\n\nConvert when necessary:\n\n```tsx\nconst page = Number(searchParams.get(\"page\"));\n```\n\n### Mistake 2\n\nConfusing query parameters with route parameters.\n\n```text\n/products/123\n         ↑\ndynamic route parameter\n```\n\n```text\n/products?page=2\n          ↑\nsearch parameter\n```\n\n---\n\n## Mini Quiz\n\n1. Where do query parameters appear in a URL?\n2. What does `useSearchParams()` return?\n3. Is `page` in `?page=2` automatically a number?\n4. What is the difference between a route parameter and a query parameter?\n\n---\n\n# Section 10 — Programmatic Navigation with Search Parameters\n\n## Explanation\n\nSometimes you want to change the URL based on user interaction.\n\nFor example:\n\n```text\nUser selects:\nSort = Price\n\n        ↓\n\n/products?sort=price\n```\n\nYou can construct a new URL and navigate to it.\n\n---\n\n## Visual Diagram\n\n```text\nDropdown\n   │\n   ▼\nUser selects \"price\"\n   │\n   ▼\nCreate query string\n   │\n   ▼\n/products?sort=price\n   │\n   ▼\nrouter.push()\n```\n\n---\n\n## Code Example\n\n```tsx\n\"use client\";\n\nimport { useRouter } from \"next/navigation\";\n\nexport default function SortButton() {\n  const router = useRouter();\n\n  function sortByPrice() {\n    router.push(\"/products?sort=price\");\n  }\n\n  return (\n    <button onClick={sortByPrice}>\n      Sort by price\n    </button>\n  );\n}\n```\n\nA more flexible approach:\n\n```tsx\nconst params = new URLSearchParams();\n\nparams.set(\"sort\", \"price\");\n\nrouter.push(`/products?${params.toString()}`);\n```\n\n---\n\n## Key Takeaways\n\n* Query strings are useful for filters, sorting, searching, and pagination.\n* `URLSearchParams` helps construct query strings.\n* `router.push()` can navigate to URLs containing query parameters.\n\n---\n\n## Common Mistakes\n\nAvoid manually building complex query strings like:\n\n```tsx\n`?search=${search}&page=${page}&sort=${sort}`\n```\n\nwithout considering URL encoding.\n\n`URLSearchParams` is safer and easier to manage.\n\n---\n\n## Mini Quiz\n\n1. Give two use cases for query parameters.\n2. What does `URLSearchParams` help with?\n3. How could you navigate to `/products?sort=price` programmatically?\n\n---\n\n# Section 11 — `router.back()` and `router.forward()`\n\n## Explanation\n\nNext.js exposes browser-history navigation methods.\n\n```tsx\nrouter.back();\n```\n\nmoves backward in browser history.\n\n```tsx\nrouter.forward();\n```\n\nmoves forward.\n\n---\n\n## Visual Diagram\n\n```text\nHistory\n\n/home\n  ↓\n/projects\n  ↓\n/projects/123\n```\n\nCalling:\n\n```tsx\nrouter.back()\n```\n\nmoves:\n\n```text\n/projects/123\n       ↓\n/projects\n```\n\nCalling:\n\n```tsx\nrouter.forward()\n```\n\nmoves forward again.\n\n---\n\n## Code Example\n\n```tsx\n\"use client\";\n\nimport { useRouter } from \"next/navigation\";\n\nexport default function NavigationButtons() {\n  const router = useRouter();\n\n  return (\n    <div>\n      <button onClick={() => router.back()}>\n        Back\n      </button>\n\n      <button onClick={() => router.forward()}>\n        Forward\n      </button>\n    </div>\n  );\n}\n```\n\n---\n\n## Key Takeaways\n\n* `router.back()` navigates backward.\n* `router.forward()` navigates forward.\n* They interact with browser history.\n\n---\n\n## Common Mistakes\n\nDon't assume `router.back()` always goes to a specific route.\n\nIt depends on the user's actual browser history.\n\n---\n\n## Mini Quiz\n\n1. What does `router.back()` do?\n2. Does it always navigate to `/`?\n3. What does `router.forward()` do?\n\n---\n\n# Section 12 — `router.refresh()`\n\n## Explanation\n\n`router.refresh()` asks Next.js to refresh the current route's server-rendered data without performing a full browser reload.\n\nThis is useful when something has changed on the server and the current UI needs to reflect that change.\n\nFor example:\n\n```text\nCreate post\n    ↓\nServer data changes\n    ↓\nrouter.refresh()\n    ↓\nCurrent route gets refreshed data\n```\n\n---\n\n## Visual Diagram\n\n```text\nCurrent Page\n     │\n     ▼\nServer data changes\n     │\n     ▼\nrouter.refresh()\n     │\n     ▼\nServer-rendered route data refreshed\n     │\n     ▼\nUpdated UI\n```\n\n---\n\n## Code Example\n\n```tsx\n\"use client\";\n\nimport { useRouter } from \"next/navigation\";\n\nexport default function RefreshButton() {\n  const router = useRouter();\n\n  return (\n    <button onClick={() => router.refresh()}>\n      Refresh data\n    </button>\n  );\n}\n```\n\n---\n\n## Key Takeaways\n\n`router.refresh()`:\n\n* Does not mean \"reload the entire browser page\".\n* Refreshes the current route's server-side data/rendering.\n* Is useful after server-side mutations.\n\n---\n\n## Common Mistakes\n\nDon't treat:\n\n```tsx\nrouter.refresh()\n```\n\nas equivalent to:\n\n```js\nwindow.location.reload()\n```\n\nThey are different operations.\n\n---\n\n## Mini Quiz\n\n1. What problem does `router.refresh()` solve?\n2. Is it the same as a complete browser reload?\n3. When might you use it after a mutation?\n\n---\n\n# Section 13 — Redirects\n\n## Explanation\n\nSometimes a request should automatically send the user somewhere else.\n\nExamples:\n\n```text\n/login\n   ↓\nauthenticated\n   ↓\n/dashboard\n```\n\nor:\n\n```text\n/old-page\n   ↓\n/new-page\n```\n\nNext.js provides redirect functionality for server-side navigation logic.\n\n---\n\n## Visual Diagram\n\n```text\nRequest /dashboard\n       │\n       ▼\nCheck authentication\n       │\n       ├── authenticated\n       │       ↓\n       │    Dashboard\n       │\n       └── not authenticated\n               ↓\n             /login\n```\n\n---\n\n## Code Example\n\nFor server-side logic:\n\n```tsx\nimport { redirect } from \"next/navigation\";\n\nexport default async function DashboardPage() {\n  const user = await getCurrentUser();\n\n  if (!user) {\n    redirect(\"/login\");\n  }\n\n  return <h1>Dashboard</h1>;\n}\n```\n\nThe important idea is:\n\n```text\nCheck condition\n      ↓\nredirect()\n      ↓\nStop rendering this route\n      ↓\nNavigate elsewhere\n```\n\n---\n\n## Key Takeaways\n\nUse `redirect()` when navigation depends on server-side logic.\n\nCommon examples:\n\n* Authentication\n* Authorization\n* Resource existence\n* Workflow state\n\n---\n\n## Common Mistakes\n\nDon't use client-side `useRouter()` when the decision can be made directly on the server.\n\nFor example, if a server component checks authentication:\n\n```tsx\nif (!user) {\n  redirect(\"/login\");\n}\n```\n\nis usually cleaner than rendering the page and then trying to navigate from the browser.\n\n---\n\n## Mini Quiz\n\n1. What does `redirect()` do?\n2. Give one authentication-related use case.\n3. Why might a server-side redirect be preferable to client-side navigation?\n\n---\n\n# Section 14 — Choosing the Right Navigation Tool\n\n## Explanation\n\nNext.js gives you several navigation tools.\n\nThe important skill is knowing **which one to use**.\n\n---\n\n## Visual Diagram\n\n```text\nNeed navigation?\n       │\n       ├── Normal internal link\n       │       ↓\n       │      Link\n       │\n       ├── Navigation after an action\n       │       ↓\n       │    useRouter()\n       │\n       ├── Need current pathname\n       │       ↓\n       │    usePathname()\n       │\n       ├── Need query parameters\n       │       ↓\n       │  useSearchParams()\n       │\n       ├── Server-side redirect\n       │       ↓\n       │   redirect()\n       │\n       └── Need browser history\n               ↓\n        router.back()\n        router.forward()\n```\n\n---\n\n## Code Example\n\n### Normal navigation\n\n```tsx\n<Link href=\"/about\">\n  About\n</Link>\n```\n\n### Programmatic navigation\n\n```tsx\nrouter.push(\"/dashboard\");\n```\n\n### Replace history\n\n```tsx\nrouter.replace(\"/dashboard\");\n```\n\n### Go backward\n\n```tsx\nrouter.back();\n```\n\n### Read pathname\n\n```tsx\nconst pathname = usePathname();\n```\n\n### Read query parameters\n\n```tsx\nconst params = useSearchParams();\n```\n\n### Server-side redirect\n\n```tsx\nredirect(\"/login\");\n```\n\n---\n\n## Key Takeaways\n\nThe most important rule is:\n\n```text\nNormal link\n    → Link\n\nAction-based navigation\n    → useRouter\n\nServer-side redirect\n    → redirect\n\nCurrent pathname\n    → usePathname\n\nQuery parameters\n    → useSearchParams\n```\n\n---\n\n## Common Mistakes\n\nThe most common mistake is using `useRouter()` for everything.\n\nFor example:\n\n```tsx\n<button onClick={() => router.push(\"/about\")}>\n  About\n</button>\n```\n\nis unnecessary for a normal navigation link.\n\nPrefer:\n\n```tsx\n<Link href=\"/about\">\n  About\n</Link>\n```\n\n---\n\n## Mini Quiz\n\nChoose the appropriate API:\n\n1. User clicks \"About\".\n2. User submits a form and should go to `/dashboard`.\n3. Server determines the user isn't authenticated.\n4. A component needs the current pathname.\n5. A component needs `?page=2`.\n6. User clicks a custom Back button.\n\n---\n\n# Day 3 — Project\n\nUpdate your documentation project from Day 2.\n\n## Required routes\n\n```text\n/\n /docs\n /docs/getting-started\n /docs/getting-started/installation\n /docs/concepts/routing\n /docs/concepts/components\n /docs/api/authentication\n /docs/api/users\n```\n\nAdd navigation:\n\n```text\nNavbar\n  │\n  ├── Home\n  ├── Docs\n  └── Projects\n```\n\nAdd a documentation sidebar:\n\n```text\nDocumentation\n│\n├── Getting Started\n│   └── Installation\n│\n├── Concepts\n│   ├── Routing\n│   └── Components\n│\n└── API\n    ├── Authentication\n    └── Users\n```\n\n---\n\n## Dynamic Documentation Route\n\nAdd:\n\n```text\napp/docs/[...slug]/page.tsx\n```\n\nThen support URLs such as:\n\n```text\n/docs/react\n/docs/react/hooks\n/docs/react/hooks/use-state\n```\n\nDisplay the captured path.\n\n---\n\n## Search\n\nAdd:\n\n```text\n/docs?search=react\n```\n\nUse:\n\n```tsx\nuseSearchParams()\n```\n\nto read:\n\n```text\nsearch=react\n```\n\n---\n\n## Custom Navigation\n\nAdd a button that:\n\n```text\nCreate Project\n      ↓\nrouter.push(\"/projects\")\n```\n\nAdd a Back button:\n\n```text\nBack\n ↓\nrouter.back()\n```\n\n---\n\n## Authentication Redirect Simulation\n\nCreate a dashboard route:\n\n```text\n/dashboard\n```\n\nIf a simulated authentication check fails:\n\n```text\n/dashboard\n      ↓\nnot authenticated\n      ↓\n/login\n```\n\nUse:\n\n```tsx\nredirect(\"/login\");\n```\n\n---\n\n# Day 3 — Final Mental Model\n\nBy the end of Day 3, you should have this mental model:\n\n```text\n                    Navigation\n                        │\n        ┌───────────────┼────────────────┐\n        │               │                │\n     Link            Router           Redirect\n        │               │                │\n Normal links     User actions      Server logic\n        │               │                │\n        ▼               ▼                ▼\n     /about        /dashboard         /login\n```\n\nAnd URL structure:\n\n```text\nURL\n │\n ├── Path\n │     │\n │     ├── Static segment\n │     │\n │     └── Dynamic segment\n │\n └── Query string\n       │\n       ├── search\n       ├── page\n       └── sort\n```\n\nFor example:\n\n```text\nhttps://example.com/products/123?sort=price&page=2\n                              │\n                              ├── Query parameters\n                    ┌─────────┘\n                    │\n              /products/123\n                    │\n                    └── Dynamic route\n```\n\nThe key distinction to remember is:\n\n```text\n/products/123\n      ↑\nRoute parameter\n\n/products?page=2\n          ↑\nSearch/query parameter\n```\n\nThat distinction will become important when we move into **dynamic routes, data fetching, loading states, and server-side data handling** in the following days.";

export const NEXTJS_DAY_3_LESSONS = normalizePastedLessonDay({
  day: 3,
  title: "Day 3",
  totalMinutes: 90,
  difficulty: "Beginner",
  lessons: [{ id: "nextjs-day-3-pasted", title: "Pasted lesson", durationMinutes: 90, explanation: PASTED_CONTENT, diagram: "", codeExample: { title: "Code example", code: "" }, keyTakeaways: [], commonMistakes: [], quiz: [] }],
  finalQuiz: [],
});
