import { pastedLessonDay } from "@/lib/learn/pasted-lesson-day";

const PASTED_CONTENT = "# Day 8 — Forms, Server Actions & Mutations\n\nDay 7 focused mostly on **reading data**.\n\nDay 8 focuses on **changing data**.\n\n```text\nDay 7\nRead data\n   ↓\nGET / fetch\n\nDay 8\nChange data\n   ↓\nPOST / PUT / PATCH / DELETE\n```\n\n---\n\n# 1. Forms in Next.js\n\n### Explanation\n\nForms are one of the most common ways users send data:\n\n```text\nLogin\nRegistration\nCreate project\nUpdate profile\nAdd comment\nUpload information\nSearch\n```\n\nA basic React form might use client-side state.\n\nBut with the App Router, forms can also integrate directly with **Server Actions**.\n\n---\n\n### Visual Diagram\n\n```text\nUser\n ↓\nForm\n ↓\nSubmit\n ↓\nServer Action\n ↓\nValidation\n ↓\nDatabase/API\n ↓\nUpdated UI\n```\n\n---\n\n### Code Example\n\n```tsx\nexport default function CreateProjectPage() {\n  return (\n    <form>\n      <input\n        name=\"name\"\n        placeholder=\"Project name\"\n      />\n\n      <textarea\n        name=\"description\"\n        placeholder=\"Description\"\n      />\n\n      <button type=\"submit\">\n        Create Project\n      </button>\n    </form>\n  );\n}\n```\n\nThe important part is:\n\n```tsx\nname=\"name\"\nname=\"description\"\n```\n\nThese names allow the submitted values to be represented in `FormData`.\n\n---\n\n### Key Takeaways\n\n* HTML forms remain important in Next.js.\n* Forms can work without turning the entire page into a Client Component.\n* Server Actions provide a server-side mutation mechanism.\n* Validation is still required.\n\n---\n\n### Common Mistakes\n\nDon't assume:\n\n> \"Server Actions mean I don't need validation.\"\n\nYou absolutely still need validation.\n\nNever trust:\n\n```text\nBrowser input\n```\n\nas valid simply because TypeScript says it should be valid.\n\n---\n\n### Mini Quiz\n\n1. What is `FormData`?\n2. Why should server-side validation still happen?\n3. Can a form be submitted without putting the entire page into a Client Component?\n\n---\n\n# 2. Server Actions\n\n### Explanation\n\nA **Server Action** is an asynchronous server-side function that can be invoked from supported React/Next.js patterns, including forms.\n\nFor example:\n\n```tsx\n\"use server\";\n\nexport async function createProject(\n  formData: FormData\n) {\n  const name = formData.get(\"name\");\n\n  console.log(name);\n}\n```\n\nThe important idea is:\n\n```text\nClient\n  ↓\ninvoke action\n  ↓\nServer function\n  ↓\ndatabase/API\n```\n\nThe function executes on the server.\n\n---\n\n### Visual Diagram\n\n```text\n┌──────────── Browser ────────────┐\n│                                 │\n│             Form                │\n│               │                 │\n│               ▼                 │\n│         Submit action           │\n└───────────────┼─────────────────┘\n                │\n                ▼\n        ┌───────────────┐\n        │ Server Action │\n        └───────┬───────┘\n                │\n        ┌───────┴────────┐\n        ↓                ↓\n    Database           API\n```\n\n---\n\n### Code Example\n\n```tsx\n// app/projects/actions.ts\n\n\"use server\";\n\nexport async function createProject(\n  formData: FormData\n) {\n  const name = formData.get(\"name\");\n\n  if (typeof name !== \"string\" || !name.trim()) {\n    throw new Error(\"Project name is required\");\n  }\n\n  // Save to database\n}\n```\n\nThen:\n\n```tsx\n// app/projects/page.tsx\n\nimport { createProject } from \"./actions\";\n\nexport default function ProjectsPage() {\n  return (\n    <form action={createProject}>\n      <input\n        name=\"name\"\n        placeholder=\"Project name\"\n      />\n\n      <button type=\"submit\">\n        Create\n      </button>\n    </form>\n  );\n}\n```\n\n---\n\n### Key Takeaways\n\n* Server Actions execute on the server.\n* They are useful for mutations.\n* Forms integrate naturally with Server Actions.\n* Server Actions are not a replacement for your entire backend architecture.\n* Authentication and authorization must still be enforced.\n\n---\n\n### Common Mistakes\n\nDon't think:\n\n```text\nServer Action = automatically secure\n```\n\nA Server Action is still an entry point into your server.\n\nYou must verify:\n\n```text\nWho is calling?\nAre they authenticated?\nAre they authorized?\nIs the input valid?\n```\n\n---\n\n### Mini Quiz\n\n1. Where does a Server Action execute?\n2. What type of work are Server Actions particularly useful for?\n3. Does a Server Action automatically authorize a user?\n\n---\n\n# 3. FormData and Input Validation\n\n### Explanation\n\nWhen a form submits:\n\n```tsx\n<form action={createProject}>\n```\n\nthe action can receive:\n\n```tsx\nFormData\n```\n\nYou can read values using:\n\n```tsx\nformData.get(\"name\")\n```\n\nBut the returned value isn't automatically your desired TypeScript type.\n\n---\n\n### Code Example\n\n```tsx\n\"use server\";\n\nexport async function createProject(\n  formData: FormData\n) {\n  const name = formData.get(\"name\");\n\n  if (typeof name !== \"string\") {\n    throw new Error(\"Invalid project name\");\n  }\n\n  if (name.trim().length < 3) {\n    throw new Error(\n      \"Project name must contain at least 3 characters\"\n    );\n  }\n\n  // Continue with validated data\n}\n```\n\nThe server should validate:\n\n```text\ntype\nrequired fields\nlength\nformat\nauthorization\nbusiness rules\n```\n\n---\n\n### Visual Diagram\n\n```text\nForm Input\n    ↓\nFormData\n    ↓\nType Validation\n    ↓\nBusiness Validation\n    ↓\nAuthorization\n    ↓\nDatabase\n```\n\n---\n\n### Key Takeaways\n\nNever jump directly from:\n\n```text\nFormData\n```\n\nto:\n\n```text\nDatabase\n```\n\nAlways validate first.\n\n---\n\n### Common Mistakes\n\n```tsx\nconst name = formData.get(\"name\");\n\nawait db.project.create({\n  name,\n});\n```\n\nis dangerous if you haven't validated what `name` actually contains.\n\n---\n\n### Mini Quiz\n\n1. What does `formData.get()` return?\n2. Why can't TypeScript alone validate user input?\n3. What should happen before inserting data into a database?\n\n---\n\n# 4. Pending UI and Form Submission State\n\n### Explanation\n\nUsers need feedback when an operation is running.\n\nFor example:\n\n```text\nClick Create\n      ↓\nCreating...\n      ↓\nCreated\n```\n\nWithout feedback:\n\n```text\nClick Create\n      ↓\n.............\n      ↓\n\"Did it work?\"\n```\n\nReact provides APIs such as `useFormStatus` for form submission state in Client Components.\n\n---\n\n### Code Example\n\n```tsx\n\"use client\";\n\nimport { useFormStatus } from \"react-dom\";\n\nexport function SubmitButton() {\n  const { pending } = useFormStatus();\n\n  return (\n    <button type=\"submit\" disabled={pending}>\n      {pending ? \"Creating...\" : \"Create Project\"}\n    </button>\n  );\n}\n```\n\nThen:\n\n```tsx\n<form action={createProject}>\n  <input name=\"name\" />\n\n  <SubmitButton />\n</form>\n```\n\n---\n\n### Visual Diagram\n\n```text\nBefore submit\n     ↓\nCreate Project\n\nSubmit\n     ↓\npending = true\n     ↓\nCreating...\n     ↓\nServer Action finishes\n     ↓\npending = false\n     ↓\nCreate Project\n```\n\n---\n\n### Key Takeaways\n\nGood mutation UX should communicate:\n\n```text\nIdle\n↓\nSubmitting\n↓\nSuccess / Error\n```\n\n---\n\n### Common Mistakes\n\nDon't allow users to repeatedly submit expensive operations without considering duplicate submissions.\n\nFor example:\n\n```text\nCreate payment\nCreate order\nSend invitation\n```\n\nmay require stronger duplicate-protection than a simple UI disable.\n\n---\n\n### Mini Quiz\n\n1. What does `pending` represent?\n2. Why should submit buttons provide feedback?\n3. Is disabling a button alone enough to guarantee duplicate-operation safety?\n\n---\n\n# 5. Updating the UI After a Mutation\n\n### Explanation\n\nSuppose you create a project.\n\nThe database changes:\n\n```text\nDatabase\nold: 5 projects\nnew: 6 projects\n```\n\nBut the current UI may still contain the previous data.\n\nYou need a way to refresh or invalidate the relevant server-rendered data.\n\nNext.js provides APIs such as:\n\n```tsx\nrevalidatePath()\nrevalidateTag()\n```\n\nfor invalidating cached data according to the application's caching strategy.\n\n---\n\n### Code Example\n\n```tsx\n\"use server\";\n\nimport { revalidatePath } from \"next/cache\";\n\nexport async function createProject(\n  formData: FormData\n) {\n  const name = formData.get(\"name\");\n\n  if (typeof name !== \"string\" || !name.trim()) {\n    throw new Error(\"Name is required\");\n  }\n\n  await saveProject(name);\n\n  revalidatePath(\"/projects\");\n}\n```\n\nThis tells Next.js that the relevant route should be revalidated.\n\n---\n\n### Visual Diagram\n\n```text\nForm\n ↓\nServer Action\n ↓\nDatabase updated\n ↓\nrevalidatePath(\"/projects\")\n ↓\nFresh server data\n ↓\nUpdated UI\n```\n\n---\n\n### Key Takeaways\n\nA mutation often has this lifecycle:\n\n```text\nValidate\n   ↓\nAuthorize\n   ↓\nMutate\n   ↓\nInvalidate/revalidate\n   ↓\nDisplay updated data\n```\n\n---\n\n### Common Mistakes\n\nChanging the database doesn't automatically mean every existing UI representation magically updates.\n\nYou need to understand how the data was fetched and cached.\n\n---\n\n### Mini Quiz\n\n1. Why might the UI contain stale data after a mutation?\n2. What does `revalidatePath()` help with?\n3. Should you revalidate unrelated pages unnecessarily?\n\n---\n\n# 6. Redirecting After Mutations\n\n### Explanation\n\nAfter a successful mutation, you may want to send the user somewhere else.\n\nExample:\n\n```text\nCreate project\n      ↓\nSuccess\n      ↓\n/projects/123\n```\n\nYou can use:\n\n```tsx\nredirect()\n```\n\nfrom `next/navigation`.\n\n---\n\n### Code Example\n\n```tsx\n\"use server\";\n\nimport { redirect } from \"next/navigation\";\n\nexport async function createProject(\n  formData: FormData\n) {\n  const name = formData.get(\"name\");\n\n  if (typeof name !== \"string\" || !name.trim()) {\n    throw new Error(\"Name is required\");\n  }\n\n  const project = await saveProject(name);\n\n  redirect(`/projects/${project.id}`);\n}\n```\n\n---\n\n### Visual Diagram\n\n```text\nSubmit Form\n    ↓\nValidate\n    ↓\nSave Project\n    ↓\nProject Created\n    ↓\nredirect()\n    ↓\n/projects/123\n```\n\n---\n\n### Key Takeaways\n\n`redirect()` is useful when the result of an action should naturally lead to another page.\n\nExamples:\n\n```text\nCreate account → dashboard\nCreate project → project details\nComplete checkout → confirmation\n```\n\n---\n\n### Common Mistakes\n\nDon't redirect before the important mutation succeeds.\n\nBad flow:\n\n```text\nredirect()\n ↓\nsave data\n```\n\nThe redirect should generally follow successful server-side work.\n\n---\n\n### Mini Quiz\n\n1. Why redirect after creating a resource?\n2. Where does `redirect()` come from?\n3. Should you redirect before the database mutation succeeds?\n\n---\n\n# Day 8 Project — Project Creation Flow\n\nBuild a complete project creation flow.\n\n### Routes\n\n```text\n/projects\n/projects/new\n/projects/[id]\n```\n\n### Requirements\n\nThe `/projects/new` page should contain:\n\n```text\nProject Name\nDescription\nCreate Project\n```\n\nImplement:\n\n* Server Action\n* `FormData`\n* server-side validation\n* authentication/authorization check simulation\n* database/API mutation\n* pending button state\n* error handling\n* revalidation\n* redirect after successful creation\n\n---\n\n### Architecture\n\n```text\n                Create Project\n                       │\n                       ▼\n                     Form\n                       │\n                       ▼\n                Server Action\n                       │\n              ┌────────┴────────┐\n              │                 │\n          Validate          Authorize\n              │                 │\n              └────────┬────────┘\n                       ▼\n                  Save Project\n                       │\n                       ▼\n                Revalidate Data\n                       │\n                       ▼\n                   Redirect\n                       │\n                       ▼\n                /projects/:id\n```\n\n# Day 7 + Day 8 Mental Model\n\n```text\n                 Next.js Data Layer\n                        │\n          ┌─────────────┴─────────────┐\n          │                           │\n       Reading                     Writing\n          │                           │\n          ▼                           ▼\n    Server Components          Server Actions\n          │                           │\n          ▼                           ▼\n      Fetch Data                 FormData\n          │                           │\n          ▼                           ▼\n       Caching                    Validation\n          │                           │\n          ▼                           ▼\n     Revalidation                Authorization\n                                      │\n                                      ▼\n                                  Mutation\n                                      │\n                         ┌────────────┴────────────┐\n                         ▼                         ▼\n                  revalidatePath()             redirect()\n```\n\n### What you should be comfortable with after Day 8\n\nYou should be able to explain:\n\n1. **Why Server Components are useful for data fetching.**\n2. **When data can be fetched in parallel.**\n3. **When requests must remain sequential.**\n4. **What caching and revalidation mean.**\n5. **Why personalized data requires special care.**\n6. **What Server Actions are.**\n7. **How `FormData` reaches a Server Action.**\n8. **Why server-side validation is mandatory.**\n9. **How to show pending form state.**\n10. **How to update/revalidate UI after a mutation.**\n11. **When to redirect after a successful mutation.**";

export const NEXTJS_DAY_8_LESSONS = pastedLessonDay(8, "Day 8", PASTED_CONTENT);
