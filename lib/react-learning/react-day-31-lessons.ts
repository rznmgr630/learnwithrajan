import type { LessonDay } from "@/lib/learn/lesson-types";

export const REACT_DAY_31_LESSONS: LessonDay = {
  day: 31, title: "Authentication and Protected Applications", totalMinutes: 60, difficulty: "Beginner",
  lessons: [{ id: "day1", title: "Tokens, sessions, and protected routes", durationMinutes: 60, explanation: "Authentication identifies a user. Keep authorization on the server, use short-lived tokens or secure httpOnly cookies, and make protected client routes a user-experience layer rather than a security boundary.", diagram: "Login → session/token → API request → server authorization", codeExample: { title: "Protected route", code: "return user ? <Dashboard /> : <Navigate to=\"/login\" replace />;" }, keyTakeaways: ["Server authorization is the security boundary.", "httpOnly cookies reduce token exposure to JavaScript.", "Protected routes improve navigation UX."], commonMistakes: ["Trusting a client guard as authorization.", "Storing secrets in client code."], quiz: [{ question: "Where must authorization be enforced?", options: ["Server", "CSS", "Browser history", "Router only"], correctIndex: 0, explanation: "Client checks can be bypassed, so the server must enforce access." }] }],
  finalQuiz: [{ question: "What is a protected route primarily for?", options: ["User experience", "Replacing server authorization", "Styling", "Caching"], correctIndex: 0, explanation: "It directs users to an appropriate screen, while the server protects data." }],
  project: { name: "Secure Workspace", goal: "Add a login-aware application shell.", brief: "Practice authentication boundaries without treating the client as the authority.", steps: ["Add login and logout UI.", "Guard a dashboard route.", "Model loading and signed-out states."], acceptance: ["The server remains responsible for authorization.", "Signed-out users receive a clear route outcome."] },
};
