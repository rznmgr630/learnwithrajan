import type { LessonDay } from "@/lib/learn/lesson-types";

export const REACT_DAY_33_LESSONS: LessonDay = {
  day: 33, title: "Build, Deployment, and Environment Configuration", totalMinutes: 60, difficulty: "Beginner",
  lessons: [{ id: "day1", title: "Shipping a production build", durationMinutes: 60, explanation: "A production build optimizes and bundles your application for deployment. Environment variables configure public client values, but client builds must never contain private secrets. Inspect bundle size, test the production build locally, and use deployment previews before release.", diagram: "Source → production build → preview → deployment → monitoring", codeExample: { title: "Public configuration", code: "const apiUrl = import.meta.env.VITE_API_URL;\n// Never expose private server keys in a client build." }, keyTakeaways: ["Build and preview before release.", "Only public configuration belongs in client environment variables.", "Bundle analysis helps find bloat."], commonMistakes: ["Putting secrets in VITE variables.", "Deploying without testing the production build."], quiz: [{ question: "Can a client environment variable contain a secret?", options: ["No", "Yes", "Only on Friday", "Only in CSS"], correctIndex: 0, explanation: "Client values are delivered to the browser and can be inspected." }] }],
  finalQuiz: [{ question: "What should happen before deployment?", options: ["Test the production build", "Delete tests", "Add secrets to client code", "Skip previews"], correctIndex: 0, explanation: "A production build can behave differently from development." }],
  project: { name: "Release Checklist", goal: "Prepare an application for deployment.", brief: "Practice safe configuration and production verification.", steps: ["Create a production build.", "Preview it locally.", "Document required public variables."], acceptance: ["No private secret is exposed.", "The production build works locally."] },
};
